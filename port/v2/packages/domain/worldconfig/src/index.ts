/* @cf/domain-worldconfig — MODULE 2 of 14.
   main.js `@module WorldConfig [domain]` (v1.8.9, tag v1.8.9). Values VERBATIM.
   ⚠ These are the universe's anchors (CLAUDE.md "Key anchors"). A changed digit
   here relocates the home galaxy. Parity: baseline.json `constants` probe. */
export const UCELL = 400;
export const OBS_R = 5200;
export const HOME_GAL_SEED = 999;
export const HOME_POS = Object.freeze({ x: 90, y: -60 });
export const GR = 1200;
export const GCELL = 42;
export const SOL_SEED = 424242;
export const SOL_POS = Object.freeze({ x: 560, y: 170 });
export const SYS_R = 320;
