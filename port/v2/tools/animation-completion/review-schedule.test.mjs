import{test}from'node:test';import assert from'node:assert/strict';import{createFullRowSchedule,requireFullRowMedia}from'./review-schedule.mjs';
test('full review retains every row and complete faint duration before settling',()=>{const timelines=Object.fromEntries(['idle','approach','pinch','hit','alert','scuttle','cast','dodge','faint','victory','tame','feed'].map(id=>[id,{durationMs:id==='faint'?1800:520}])),s=createFullRowSchedule(timelines);assert.deepEqual(s.rows.map(r=>r.id),Object.keys(timelines));for(const r of s.rows)assert(r.endMs-r.startMs>=timelines[r.id].durationMs+280);assert(s.durationMs>=10000);assert.throws(()=>createFullRowSchedule({}),/empty/);assert.throws(()=>createFullRowSchedule({faint:{durationMs:NaN}}),/invalid/);});

test('encoded full-row duration rejects a truncated ten-second film and excessive recorder padding',()=>{assert.equal(requireFullRowMedia(18.1,18000),18.1);assert.throws(()=>requireFullRowMedia(10,18000),/complete row/);assert.throws(()=>requireFullRowMedia(19,18000),/complete row/);assert.throws(()=>requireFullRowMedia(NaN,18000),/complete row/);});
