import fs from'node:fs';import path from'node:path';import os from'node:os';import{pathToFileURL}from'node:url';import{rolldown}from'rolldown';import{hashBytes,hashJSON}from'../creature-animation/quadruped-template.mjs';import{checkFamilyGeometry}from'../creature-animation/family-record.mjs';import{familyContract}from'../creature-animation/family-contracts.mjs';
const out=path.resolve(process.argv[2]),producer=path.resolve(process.argv[3]);if(fs.existsSync(out))throw Error('New directory required');fs.mkdirSync(out,{recursive:true});
const source='audits/ART_KIT_ENGINE_FIRST_20260912/masters/frog.png',bytes=fs.readFileSync(source),snapshot=JSON.parse(fs.readFileSync('audits/ART_KIT_ENGINE_FIRST_20260912/canonical-snapshot.json')),genome=snapshot.roster.view.all.find(g=>g._earthName==='Frog');if(!genome)throw Error('Actual Earth identity missing');
// Anatomical observation draft. Absent parts deliberately have no coordinates.
const landmarks={root:[.48,.46],pelvis:[.33,.45],spine:[.46,.41],chest:[.62,.43],neck:[.68,.40],head:[.74,.30],jaw:[.82,.38],hindNearRoot:[.32,.49],hindNearKnee:[.17,.48],hindNearAnkle:[.15,.68],hindNearPaw:[.25,.74],hindFarRoot:[.42,.46],hindFarKnee:[.50,.54],hindFarAnkle:[.62,.63],hindFarPaw:[.69,.665],foreNearRoot:[.55,.445],foreNearKnee:[.44,.59],foreNearAnkle:[.50,.73],foreNearPaw:[.595,.785],foreFarRoot:[.725,.45],foreFarKnee:[.79,.57],foreFarAnkle:[.825,.68],foreFarPaw:[.88,.73]};
const body={kind:'hopper',identity:{speciesVisualKey:JSON.stringify(genome),seed:genome.seed,ownerId:'authored:Frog/accepted-master',earthName:'Frog'},template:{id:'hopper',version:1},geometry:{cutoutAssetHash:await hashBytes(bytes),width:1254,height:1254,groundLineY:.84,depthLayers:[{id:'far',order:0},{id:'near',order:1}]},landmarks,materials:{surface:'smooth skin'},clipSetId:'hopper-land-v1',source,anatomyObservation:{absent:['tail','external ears'],visibility:{hindFar:'partly occluded; landmarks provisional'},status:'draft, not admitted animation asset'}};const record={...body,recipeHash:await hashJSON(body)};
const scratch=fs.mkdtempSync(path.join(os.tmpdir(),'cf-frog-anatomy-')),bundle=await rolldown({input:path.join(producer,'motion/index.ts'),platform:'node'});try{await bundle.write({dir:scratch,format:'es',entryFileNames:'producer.mjs'});}finally{await bundle.close();}const{compileBodyCard}=await import(pathToFileURL(path.join(scratch,'producer.mjs')));
let consumerError=null,producerError=null;try{checkFamilyGeometry(record);}catch(e){consumerError=String(e);}try{compileBodyCard(record,genome);}catch(e){producerError=String(e);}if(!consumerError||!producerError)throw Error('Expected absent-appendage refusal no longer reproduced');
const missing=familyContract('hopper').joints.filter(j=>!Object.hasOwn(landmarks,j)),report={status:'REPRODUCED_CONTRACT_DEFECT',scope:'actual accepted Earth Frog; no dummy tail/ear landmarks and no generated pixels',sourceSha256:record.geometry.cutoutAssetHash,missingRequiredJoints:missing,consumerError,producerError,recordStatus:'draft observation; not a runtime asset',needed:'Anatomically absent appendages must be represented by an explicit optional inventory shared by producer and consumer. Missing mandatory legs must still refuse.'};
fs.writeFileSync(path.join(out,'observation.json'),JSON.stringify(record,null,2)+'\n');fs.writeFileSync(path.join(out,'report.json'),JSON.stringify(report,null,2)+'\n');fs.rmSync(scratch,{recursive:true,force:true});console.log(JSON.stringify(report));
