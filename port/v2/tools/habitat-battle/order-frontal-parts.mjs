/** Front-view feathers pass behind the axial face/chest instead of covering
 * the eyes on the upstroke. Masks, UVs, bones and curves remain unchanged. */
import fs from'node:fs';import path from'node:path';import{hashJSON}from'../creature-animation/quadruped-template.mjs';
const [from,to]=process.argv.slice(2);if(fs.existsSync(to))throw Error('new output');const record=JSON.parse(fs.readFileSync(path.join(from,'record.json')));if(record.projection!=='frontal-wings')throw Error('source view');fs.cpSync(from,to,{recursive:true});const {bindingHash,...binding}=JSON.parse(fs.readFileSync(path.join(to,'binding.json')));const owner=new Map(binding.parts.map(p=>[p.id,p.joint])),priority=p=>/^wing/.test(owner.get(p.id))?0:/^leg/.test(owner.get(p.id))?1:/^head|^beak|^neck/.test(owner.get(p.id))?3:2;binding.paintSkin.parts.sort((a,b)=>priority(a)-priority(b));fs.writeFileSync(path.join(to,'binding.json'),JSON.stringify({...binding,bindingHash:await hashJSON(binding)},null,2)+'\n');fs.writeFileSync(path.join(to,'depth-receipt.json'),JSON.stringify({sourceBindingHash:bindingHash,rule:'frontal wing surfaces behind axial face/chest',changed:'draw order only',qualityAccepted:false},null,2)+'\n');
