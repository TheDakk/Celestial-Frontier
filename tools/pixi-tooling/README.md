# Parked Pixi authoring setup

Installed under Nick's earlier side-install instruction; not connected to the game.
AssetPack remains deferred until after U4. The DevTools helper and downloaded extension
are excluded from the required terminal-only workflow; the extension was never loaded.
Do not run conversion or activate the helper without the later bounded instruction.

Read `../../UI_TOOLCHAIN.md` for the maintained inventory, status and update policy.
`pack.mjs` is an unexercised preparation entrypoint requiring a source working copy and
a new output directory. No asset conversion or runtime integration is claimed here.
