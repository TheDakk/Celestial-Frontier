# Source review and preparation notes

Root reviewed the complete material helper, uniform/view/main diff, test delta and native
observer before the runs. Analytic smoothstep gradients use octave chain scales10/20/40
and the existing1.4longitude mapping; tangent-space slope/relief remain bounded. The
helper returns final lit RGB and is not lit twice. Default material0 and strict option1
reset on each accepted atlas; the program/group remain application-owned. No domain/art
package, legacy renderer, atlas sampling, geometry or disposal owner was edited.

A preparation script refused a nonunique `count: 84` match before writing the copied draft
pin script. Only the already-completed release-text edit preceded that refusal. The new
script then targeted the exact regex owner and retained83rows; this was a preparation
assertion, not a failed product test. No test or native retry occurred in this batch.

The initial delegated helper proposed screen derivatives; it was revised before the first
build to analytic gradients, avoiding a new extension/context requirement. Native shaders
compiled/rendered in both fresh Edge sessions. No unconditional WebGL2 dependency was added.
The browser-free profile was focused, with explicit producer/draft bindings refreshed before
the checks; there is no claim of a new full aggregate certificate.

The old U2/turn profile and navigation failures remain preserved. Neither adding pixel noise
nor passing tests establishes the approved painted style. The visual-inspection record is
explicitly below that bar. Signed commit remains blocked by the prior1Password failure;
no signing retry, unsigned fallback or hosted operation was attempted.
