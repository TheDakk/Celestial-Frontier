# S2 — unchanged accepted output

Signed producer `58f81e545cf7d40798bddbb0592506681c124073`. One ordered six-subject sweep PASS, with no retry. Every full subject receipt and every decompressed per-support sample byte equals BEAR_CONTACT_DIAGNOSTICS_20260922/s2. Five original crabs remain R2c′ row-identical; Civet maximum 0.16525637288346104 px. Exact rest and presentation PASS.

The accepted legacy path is retained bit-for-bit because the analytic branch activates only after that path fails the unchanged 0.25 px residual gate, and only if every active support is endpoint-only. Mixed-weight handling is unchanged. This is a generic gate-driven mathematical branch, with no species/fit/size selector.

Evidence: s2/static.json; s2/identity.json; per-subject static and support sample files. The same signed historical source-join sampling instrument is retained for byte comparison; current runtime modules and their hashes appear in s2-controls.sources.json. No gate or source/input record/binding was changed for S2.
