# Analytic rigid-support IK

Nick authorized the single proposed fix from BEAR_CONTACT_DIAGNOSTICS_20260922. The existing three-pass result is retained exactly when it passes. If the painted residual fails0.25px and **every active support is endpoint-only**, the solver directly solves the rigid observed support and reconstructs the anatomical ankle from the unchanged lower-bone vector. Mixed-weight contacts stay on their existing path. No species, record ID, anatomy declaration or size-specific branch.

Root accommodation is recomputed from the authored root using the same0.08×motion-scale bound. Anatomical source lengths and supports are unchanged; the support point is a target, not a replacement joint. Contact angular limits, terminal orientation, endpoint1e-8 and painted0.25px checks run after the analytic solve too. Earlier reach/compression/joint-limit refusals remain refusals. The analytic branch does not promise to repair gallop or tail compression.

Controls: before the change, the real retained sample failed at0.26350408495550587px. After:10/10 focused tests pass, including all fifteen retained support-failure samples, anatomical bone lengths, terminal orientation, exact rest, diffused-support refusal, independent gallop compression refusal, weighted-support controls and existing limit mutants. App typecheck PASS. No old input/master/binding/declaration changes.

Sign producer, then one ordered six-subject S2 sweep using the retained receipt instrument. Only after S2 passes, run the full bear observed static rows and presentation once, into this folder. Rest mode is unchanged and its earlier table is retained rather than rerun. Native CPU/film are not inferred from static evidence. No fetch/sync/push/PR/merge/release/deploy; signed commits only.
