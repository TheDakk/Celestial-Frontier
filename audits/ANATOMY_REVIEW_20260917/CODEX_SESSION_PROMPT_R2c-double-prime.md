# Codex new-session prompt — R2c″ (issued by Claude 2026-09-19 after the R2c′ S2 stop)

```text
Continue Celestial Frontier in /Users/nick/Projects/celestial-frontier-openai-mac, branch openai/mac.

Verify first: `git rev-parse --show-toplevel` and `git branch --show-current`; stop and report if either differs.
Read ROADMAP.md, PROCESS_LAWS.md, PARALLEL_GIT_PROTOCOL.md, then your own packet
audits/ANATOMY_SINGLE_RUN_20260919/R2c-prime/README.md (last signed HEAD 7dd884f0; producer dd33865c;
halted R2c packet ca851fb6). Do not repeat completed work.

Then read Claude's review, read-only, no sync, no copy into this worktree:
/Users/nick/Projects/celestial-frontier-anthropic-mac/audits/ANATOMY_REVIEW_20260917/CLAUDE_R1BR2B_REVIEW.md
§9 (R2c verdict), §10 (R2c′ verdict and the R2c″ direction — the authority for this session).

STATE: both S2 stops were right; nothing is loosened; 0.25 px, <2 ms and exact rest stay. Your 10,515 retained
support samples show the five crabs' ARAP residual at their (R1b-locked) supports is 0.0000 px in every row, while
the unpinned candidate-10 Civet carries 0.02–0.20 px even in passing rows, plus up to 0.11 px of covariance.

R2c″ — one bounded correction, signed producer + static checks + evidence folder, then CONTINUE the §8 run:
 1. Predict each painted support with the exact per-vertex LBS interpolated at the support (your "exact triangle
    LBS"); drop the weighted-point model. Five crabs must reproduce R2c′ rows bit-for-bit.
 2. Regenerate the Civet binding through the CURRENT shared split with contact-endpoint locks, from the unchanged
    candidate-10 inputs (same masks, atlas, record). Candidate-10 stays byte-for-byte as history. Receipt: weights
    identical everywhere except the support locks (crab-style contactPins receipt), restChanged 0, atlas hash unchanged.
 Controls, both directions: covariance term ≤ 1e-6 px on all six; the UNPINNED candidate-10 under the new model
 still fails (proves the lock closes it, not the model); crab ARAP residual stays 0.0000; diffused mutant fails.
 Acceptance: Civet, all rows + presentation, ≤ 0.25 px, family solver, regenerated binding.
 If it still fails after both: genuine shared-path red — stop, no third variant, report.

Then resume §8 from the R2c static sweep without review stops: R2d (persimmon parent-frame leaf groups; foliage
shape non-negotiable) → R3 (+T1 continuity gate with the 42–57 px snap as negative control; ContactPhase.travel
'solver'|'stage'; contactJoint on compileAnatomyAttack; pinch reachable; brachyuran attack row + crustacean profile
split so a crab has an admitted melee) → R4 → ONE native re-capture (eight subjects + Civet on the regenerated
binding, faint-inclusive full-row films) → R9 five crabs (accepted finisher, masked 0.35, per
R9_ADDENDUM_FINISHED_TEXTURES.md; sheet with one arena-scale composite; Q1 gape candidates on the same sheet)
→ R5/R6/R7 → R8 phone delivery (D1) → roster by source family until exhausted → PR42 split prepared locally →
ONE stop with the accumulated packet. Pre-answered decisions are in §7; S2 (Civet sentinel or any crab regressing
after a solver/skin change) is the only halt; leaf reds are recorded and the run continues.

Two E1 interfaces the anthropic lane consumes from R3 (unchanged): ContactPhase.travel?:'solver'|'stage' (stage
mode = zero solver root dx, stance targets unchanged) and contactJoint on compileAnatomyAttack.

Also, in this session's toolchain step (already on Node 26.9.0): make tools/quadruped-proof/export-current-poses.mjs
and turn-contact-transition.test.mjs resolve the producer REPO-RELATIVE (they hard-code an absolute path into the
anthropic worktree and pin its old gsap-adapter bytes; that file has changed there), and point
tools/creature-blender-export.mjs's route span at paintOverrideCanvas (your 0b885751 refactor moved the route;
the identical one-token edit is already applied on anthropic/mac).

Git: Nick authorized ONE branch push of both lanes on 2026-09-19 (done from the anthropic worktree in chunks; a
branch push triggers no workflow). Do not fetch, sync or merge anything into openai/mac; do not push, open a PR,
label, merge, release or deploy. Commit signed (1Password unlocked; verify each signature); report actual local
HEAD/ahead only. PR42 stays parked. No unsigned fallback.
```
