# Retained verification corrections

The first210-test focused run passed209 and failed one existing source-mutation control in
biome-vista-surface.test.ts. The new painted branch duplicated the environment assignment.
The existing source.replace removed only the first copy, leaving the canonical worker binding
present, so its expected missing-binding rejection did not occur. No static/browser step ran
after the red.

Consolidated world/environment publication into one common owner before the painted/native
branch. The actual guard and existing negative-control assertion are unchanged. A fresh
producer binding and corrected focused/static chain follow; the first report stays FAIL.

The earlier read-only fact extraction also refused unresolved audit-root package imports;
Vite unexpectedly attempted its default websocket in middleware mode. Corrected extraction
disabled ws/hmr/discovery and used exact facade paths. No game/browser behavior was inferred
from that diagnostic failure, and no extra network/listener was authorized.

The corrected210-test/static/art/root chain passed. A subsequent source review found that
the existing cache-hit path returned before publishing the current world/environment binding.
Hoisted the one common publication before cache lookup, and strengthened the existing source
control to reject publication after lookup. This fixes stale/null cache-hit diagnostics while
preserving one binding owner. It was found before native execution, not a native failure.
A final narrowed binding/cache/loader/producer check plus static/art/root validation follows.
