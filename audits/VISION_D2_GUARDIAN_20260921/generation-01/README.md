# D2 Brown Bear — generation 01

One built-in `image_gen.imagegen` call used the exact bytes of `prompt.txt`, with the approved Discovery Atlas and retained side-on painter guide attached. No retry, intake, hand landmarks, masks, fit or rig. G2 declaration is complete. **G1 format is RED:** requested 1536 × 1536; tool delivered **1254 × 1254 RGBA**. The delivered alpha and all channels are preserved byte-for-byte. This is not a 1536-qualified guardian master. D2 permits alpha, so absence of magenta is not a format red.

- [Master](brown-bear-master.png)
- [Exact sent prompt](prompt.txt), [compiler](compile-prompt.mjs), [request receipt](request.json), [result receipt](result.json)
- [Painter guide](anatomy-guide.png), [catalogue source](subject-source.json)
- [Presence input](presence.json): four visible walking legs; hidden=[], absent=[], folded=[] (cf.anatomy-presence/v2).

The guide is retained at its native 440 square; it was not resampled to 1536. The actual Brown Bear catalogue has headFrac 0.27, rather than the design note’s 0.22. The prompt uses catalogue anatomy. Sol’s retained source card explicitly has no biome hazard; the guardian’s threatening face and more elaborate focal fur/claw detail do not invent a hazard motif. GUARDIAN RULE is included verbatim with that source application stated.

Visual inspection: right-facing bear, four readable paws, shoulder hump, two rounded ears, short tail, threatening muzzle. Output turns the head toward the viewer; it is not a strict orthographic drawing. Art acceptance is not asserted. No landmark coordinates were authored. Tool model and seed are not exposed.

Nick’s decisions remain 5 ms desktop-only guardian CPU and a 60 px bound at 1536; neither was tested or changed in G1–G2. The 1254 output does not silently redefine either gate.

## Handoff

Codex holds. Claude receives the master and declaration for G3 compiler diagnostics and G6 titanic-fill work; G1’s 1536 size requirement remains unmet, so downstream diagnostics must not be labelled a full D2 acceptance. No new Nick decision is requested in this packet; the requested size is already decided. The tool’s output mismatch is retained as a finding, not an altered threshold or a resampled original.

Master SHA256: `1fdbef97b7c612d85ebe4a3ed7b580c31d7671a379c92e46ba3f200f655d035d`.
