# C-lane repairs without external review — September 14, 2026

Nick asked Codex to fix the outstanding failures locally before returning to Claude.
This continues the uninterrupted session and September 12 toolchain receipt on
OpenAI/Codex, macOS, `/Users/nick/Projects/celestial-frontier-openai-mac`, `openai/mac`.
Starting commit: a1cdeeb62a2e753e1b5e53c70377c463bfa005de. No new branch, kit edit,
GitHub write, history rewrite, model run, painted asset change or native capture.
Claude-owned modules remain read-only.

## Three test failures repaired

The two Compendium-budget failures and one current-producer failure came from stale
live authorities. `authorities-before.json` preserves the mismatches. The package
lock comparison (`dependency-delta.json`) finds only added packages and additive
root/app dependency declarations: no pre-existing dependency node changed. GSAP
3.15.0 and the pinned authoring packers are the approved additions. The workspace
lock change publishes a complete private inode atomically and refuses unsafe stale
owner stealing; it does not change the collector's measurements or outcome rules.
The collector, evaluator, fixture, browser transport, outcome inventory and fixed
ruler are unchanged. `authority-refresh.json` binds old/current authorities and
hashes all preserved ceilings, ruler, historical candidate samples, paired baseline
and browser policy. No historical sample or ceiling was updated to get green.

Current measurement: 6a829fb18eab4c171afaace0f49ad2a987cfcdc520d33dbc05337c379df85ee2.
Current producer: ed8823bcaec0e22f47fe1d6554fc06c282fb30edda2e61de393fea620cd129c8.
This is source binding, not a fresh native memory certificate. SceneMemory remains
quarantined from dev/develop and required for production; its stale authority was
not hidden or rebound. Existing stale-source, stale-build, forged-constant,
service-worker, owner and index mutation controls remain active.

## No checkout lock inside unit tests

`npm --prefix port/v2 test -- <Vitest arguments>` runs Node tool tests, then a
standalone locked evidence build, releases its lease, and only then starts Vitest.
The build receipt travels through an environment variable to read-only unit tests.
They independently hash source and dist and reject missing, source-stale or
dist-stale receipts. The source digest includes the port, root authoring tools,
legacy sources, Art Kit, package manifests/locks and the actual ignored runtime
files emitted by the kit Vite plugin. The plugin allowlist is read fail-closed.
A bare Vitest invocation including the authority test has no fresh build receipt
and refuses; use the npm runner for focused tests too. No test builds or acquires
the real checkout lock. Temporary-path lock race tests remain independent.

The build-order test executes the actual collector wrapper with injected side
effects: build failure must release ownership and never observe dist; removing the
build makes its ordering assertion fail. The read-only reader still checks the
emitted evidence marker. These controls replace an obsolete adjacency-only regex,
not the behavior it guarded.

## C2: better diagnosis, still an explicit stop

`cut-seam.mjs` measures the exposed bridge between the two posed copies of each
shared base-ownership edge. Exterior notches have no shared edge, so rigid motion
cannot manufacture a seam just by entering a fixed rest-pivot disc. It does not
replace the agreed native gate or prove general shape quality. Controls cover
intact rest/motion, a known 240-pixel opening, closing that opening with support,
rotated support, empty/overlapping ownership, malformed matrices and off-canvas
measurements. No region erosion, tolerance increase or morphology is used.

`audit-cut-seams.mjs` verifies the original pose-code, record, binding, atlas and
declaration hashes, checks all packed part bytes, and applies this diagnostic to
retained native frames. `cut-seams.json` preserves exact inputs and missing pixel
indices. Only head--ear-far was captured before the prior stop:

| Pose | Disc-only uncovered | Bands uncovered |
| --- | ---: | ---: |
| Rest | 0 | 0 |
| Hit recoil | 170 | 0 |
| Strike | 0 | 0 |
| Stride quarter | 91 | 3 |
| Stride three-quarter | 77 | 3 |

The remaining band samples have alpha exactly zero, not merely low alpha. At
1254 square they are quarter (998,197), (999,198), (1000,198); three-quarter
(1004,202), (1004,203), (1005,203). They are not waived as rim tolerance. The old
morphological counts and old FAIL remain unchanged. No later pair or creature
capture is authorized by this diagnostic. Further C2 work must resolve these
samples while keeping rest pixel equality, descendant-only ink, the prescribed
band caps, record anatomy and the 8% contact bound. A new diagnostic passing one
pair would not establish the whole rig's shape acceptance.

## Verification and remaining work

See `checks.json` and `checks/*.log.gz` for actual results; gzip preserves the raw logs verbatim. The complete v2 suite, Node
controls and TypeScript checks are the code acceptance boundary here; native
memory certification and C2 captures are not claimed. During command setup two
root npm invocations were stopped before their browser stage after a cwd mistake;
those partial chains are not claimed as completed root batteries. Root validate
was then run explicitly. There were zero browser launches or hosted attempts.

Weather S/V/P/SV/SP/SVP remain review candidates; E remains active. C3 source
recordings, C4 library, smaller phone finisher and C5 promotion retain their prior
holds. Nick does not need to open Claude now. Codex can continue independent
repairs; when Nick resumes external review, include this addendum with the previous
consolidated C-lane package rather than treating its three red tests as current.
