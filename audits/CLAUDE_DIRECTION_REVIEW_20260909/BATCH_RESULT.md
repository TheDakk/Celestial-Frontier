# Queued landing and fixed-shape result — September 9, 2026

The working browser AI proof now supports Land → real progress/ETA → completion notice →
explicit View landfall, while a read-only canonical Field journal remains usable. The job remains
owned by this page when changing panels. The normal game's Land/save flow is unchanged.
This is not persistent multiworld scheduling, model delivery or accepted production artwork.

One actual1024×576 identity-only/Q8-block32 generation completed in **69,034.55ms**, versus
69,948.09ms for the retained matched baseline. The0.914-second/1.31% difference is too small
in one run to establish a meaningful new speedup. Fixed shapes remain opt-in, with no default
adoption or further unchanged rerun. Largest demonstrated improvement remains the earlier
Q8 repack's same768 comparison239s→69s. Device, cold-cache and gameplay-frame-rate claims
remain unqualified. Initial download and pre-run hash verification are outside these times.

The candidate's rawPNG is **byte-for-byte identical** to the baseline, SHA256
`2b0fea42177e2eeaf94e3c71480d6f0502177c3d1a7f9879c5149ef58ef370b7`.
The real session reports resolved inputs hidden_states[1,2904,128],
encoder_hidden_states[1,512,7680], timestep[1], img_ids[1,2904,4], txt_ids[1,512,4].
Complete recipe JSON matches after removing the explicit opt-in flag; weights, references,
resolution, four steps, seed and arithmetic remain unchanged. [Comparison](COMPARISON.json).

The same run performed five trusted mouse clicks: Land, Field journal, Earth during step2,
Field journal again, and View landfall after completion. Visible progress/ETA replaced Land;
the actual sampled ETA was about20seconds **plus final processing**. Completion stayed in the
journal until the explicit return. Each sample required the same complete job key and one start.
Actual DOM controls deliberately hid the progress ancestor and completion notice; the same
native sampler/validator rejected each exact outcome, and restoration passed before continuing.
All five clicks, controls,88 unchanged source rows and browser/server cleanup are retained in
[the native result](fixed-shapes-1024-01/result.json). No model-worker substitute was used.

Root inspected the progress, returned desktop painting and completed320×568 layout screenshots.
The bar/ETA and controls are legible; the finished image stays uncropped and phone-width contained.
That phone screenshot is desktop reflow after completion, not touch input, phone inference,
Safari/PWA or physical-device qualification. Successful sampled navigation is not a gameplay
FPS/latency certificate. Existing ORT CPU-placement warnings remain in all8browser-log entries;
they are warning text transported at Chromium log level error, not a claimed zero-log-error run.

The image is unchanged, so the prior rejection remains in force: Civet identity/proportions,
rodent-like Platypus and inaccurate Persimmon/Cranberry/Devil's Club are below the approved bar.
No additional visual-generation sweep, retouch or asset promotion. qualityAccepted:false.

## Checks and first failures retained

- The first aggregate had87tests:84PASS,3HTTP-server failures due to sandbox localhost binding
  EPERM. The preserved [first log](controls-01.log) is not relabeled green. The affected4server
  cases then passed outside Seatbelt in a scoped environment correction. Across these receipts,
  all87distinct behavior cases are covered, including33actual controller,26progress-owner,
  seven shape,17profile/parser and four server cases.
- Eight new shared native UI observer tests PASS, including bad visibility, false completion,
  wrong/missing identities, inert return, dimensions, overflow and covered/untrusted clicks.
  Combined distinct behavior checks:95. Both new runner/worker syntax checks PASS.
- Root validation PASS: legacy HTML remains unchanged,1010named renders, zero boot errors and
  50fingerprints identical. No V2 runtime source changed; no extra unchanged TypeScript/full
  profile rerun. Prior4,121tests are still not current-head full admission.
- Pre-execution source review caught the new shape helper's missing closing brace; repaired
  before its first seven controls. A later root exact-match insertion refused a changed anchor
  before any file write; its corrected unique insertion added the native visibility controls.
- Initial Git fetch failed with SSH agent communication error. After Nick reported1Password
  unlocked, one distinct recovery passed authentication as TheDakk, repository read/fetch and
  PR42 metadata. Both original failure and recovery receipts remain. No locked-vault inference.

No Guide/Training/draft-release changes: this batch changes the isolated proof and current docs,
not the player-facing normal game. All earlier ROADMAP verification/quality/hosted/device
blockers remain. Full Claude scope and the materially new direction are in [HANDOFF](HANDOFF.md).


## Final review correction — recipe loading

A final independent source review caught a real pre-plan gap: before landing.begin(), the
recipe fetch had no AbortSignal, and a failed retry could render an older completed owner.
The controller now supplies cancellation to that fetch and owns explicit preparation/failure
presentation before the plan exists. View landfall additionally requires current complete state
and an actual currentPNG. The actual-controller suite now passes35cases, including stalled
recipe cancellation and completed-run→503failure. Distinct focused checks therefore total97.
The original69-second native run is immutable evidence for its immediate predecessor controller;
it is not silently relabeled as a later-source certificate. A separate native preparation audit
uses real HTTP and explicitly synthetic model workers to assess this correction without another
ONNX inference or unaccepted-art generation. Its result is recorded below when complete.


The corrected [native preparation audit02](recipe-preparation-native-02/result.json) passes
all six cases: current cancellation, omitted-signal rejection, cancellation restoration,
current success→503→successful retry, omitted-overlay stale-state rejection, and restored
success→503→successful retry. The same live outcome predicate judges positives and mutants.
Real held responses are abandoned by Cancel before cleanup; workers are explicitly synthetic.
The original audit01 stopped after two outcomes because a cleanup-time request reintroduced
a live response into its JSON carrier. Audit02 stores only data in receipts, closes targets before
retiring held responses, refuses late cleanup requests, and passes a circular-response negative
serialization control plus restored positive. All five observed source files stay unchanged;
owned targets, browser and server close. Audit01 remains FAIL with its original source/receipt.
No ONNX inference was repeated to verify this correction. Root validation passes again after
the controller fix; no other production source, quality verdict or former blocker changed.


## Actual Claude response and bounded follow-up — completed September9

The requested installed-CLI Claude review returned successfully at12:49:06UTC for
signed8bdbea9a65b1f64c09906fd99589d75e5d8bc50a. It ran read-only from its verified
Anthropic/macOS workspace, with no tests/build/browser/source sync/hosted action.
[Original response](CLAUDE_RESPONSE.md), raw CLI JSON/request/receipt and artifact
hashes remain verbatim. Claude says no merge yet and explicitly limits inspected
scope. [All15finding dispositions](REVIEW_DISPOSITION.md) record confirmed issues,
qualified claims and next work. [Native visual recheck](NATIVE_VISUAL_RECHECK.md)
clarifies one visible head/tail but ambiguous body/legs; quality remains false.

The isolated controller now exposes friendly panel-independent failure/cancellation,
checks already-requested cancellation after awaited decode, and binds publication/
explicit return to the complete job, PNG and Earth world/environment/epoch/snapshot.
This introduces no normal-game hook, source-genome change, new model run or art acceptance.
Native input cannot interrupt the synchronous conversion/PNG span; the added guard
covers the exposed API's real resolved-decode/before-continuation ordering.

First focused run [42controller tests PASS](review-controller-01.json),35existing+
7new, including removed-guard rejection and restored positive behavior. This is104
distinct focused behavior cases across the packet when combined with the retained
other owners; do not add reruns as new cases. The42run records exact four source
hashes before/after and released foreground/workspace locks.

One [native follow-up](review-controller-native-01/result.json) passes three fresh
pages: actual HTTP503, actual held-request Cancel, and synthetic2×1RGB completion.
Sixteen trusted clicks cover both panels and explicit return. Two actual hidden-
ancestor mutations are rejected by the same friendly-outcome observer and restored;
six changed destination/identity bindings refuse return, retain the journal/notice/
PNG, and restored identity returns through a trusted click. The data-only serializer
again rejects a circular-response mutant and accepts restoration. Six observed
sources stay unchanged; owned targets, browser and HTTPserver close. Workers are
explicitly synthetic: no ONNX/GPU inference or model/image-quality claim.
Root inspected friendly failure in journal and cancellation on Earth screenshots;
messages are visible/readable with controls retained. No phone/touch qualification.

[Root validation after correction](root-validation-review-01.json) PASS:1010named
renders/0booterrors/50baseline fingerprints, legacyHTML unchanged. No normal-game
V2runtime edits or repeated broader profile. All previous reds/unknowns and current-
head admission remain open; the old4121tests do not certify this correction.
ROADMAP is refreshed with a self-contained handoff; aged live blocks were moved
verbatim to ROADMAP_ARCHIVE. All affected current art/UI/tool references agree.

GitHub step remains none: PR42parked Draft/unlabeled, no push/Ready/owner-label/run/
merge/release/deploy/version/automation. Claude's review did not install these local
source changes into its worktree. Final local signatures and exact implementation
checkpoint are recorded separately; successful signing is not integration.
