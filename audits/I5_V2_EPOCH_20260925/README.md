# I5 v2 guarded recalibration epoch — September 25, 2026

Nick authorized one recalibration epoch after Claude's I5 review and confirmed **three independent calibration runs plus one certification**, all without retry and on the same exact integrated source. No per-counter allowance was requested; this epoch declares **zero growth allowance for every v1 counter**. A breach is a regression and stops the epoch. No measurements are claimed by this preparation commit.

## Source and instrument

Product: signed, pushed `b4f191c3538f78318a183705f89272b38b88118e` on anthropic/mac, including painted-card bound `ffa1f126`. Read-only remote identity and clean local source agreed. Isolated product checkout `/private/tmp/cf-i5-v2-b4f191c3`; Claude's absolute lane remains read-only. `preparation.json` records hashes, Node26.9.0, identical dependency lock and workspace-link confinement. This uninterrupted session reuses its nick-game-toolchain startup receipt.

Instrument: this separate signed openai/mac preparation commit. `compendiummem-v2.mjs` measures the isolated product without installing its instrument code into that source. It records both identities independently. It materializes a versioned collector/evaluator from the unchanged v1 sources with exact counted authority edits, retaining the full generated bytes and their hashes. Tests prove the complete native collectProfile body and outcome evaluation block remain byte-identical. The legacy baseline runner is excluded from the v2 CLI; its historic evidence is not asserted to be a new v2 sample. The new policy, materializer, guard and runner are additionally bound by the signed instrument source digest, checked throughout the epoch.

`compendium-memory-v1.json`, its samples, v1 collector/contract and v1 tests are untouched. Its SHA-256 remains `c109b5845b12862bbd8fa069b6f8e394b48d9d8568d8537f20565b63c5d907fc`. No historical sample is assigned a new producer or measurement authority. The v2 budget starts empty at `apps/game/smoke/compendium-memory-v2.json` in the isolated checkout (ignored evidence). It binds the independently observed build producer, v2 measurement inputs, signed instrument and clean product identity before collection. This is a new epoch record, not an edit of the v1 producer.

## Activation and stopping

Every calibration uses the fixed v1 ceilings, not an unbounded evaluator. Only three independently launched, terminal-green and named-verified runs can activate v2; phone and desktop samples must reduce from raw evidence, have independent run IDs/timestamps and share exact source, input, producer and browser provenance. Every value must fit with strict headroom. Activation keeps the predeclared ceilings, so an observed increase cannot be absorbed into a new limit. The complete v2 record is emitted only after successful activation.

The single subsequent certification runs the same built producer and clean product head. A mismatched source/instrument, partial report, failed lifecycle, failed named verifier or numeric/behavioral refusal stops immediately. Exclusive output-directory creation, an existing-budget guard and a monotonic phase ledger prevent automatic retry/resume. Every report, image, sample, log and command exit is retained, including refusals. A failed calibration means certification does not run.

Invocation from port/v2, after signing this instrument:

```sh
node tools/compendiummem-v2.mjs --source=/private/tmp/cf-i5-v2-b4f191c3 --head=b4f191c3538f78318a183705f89272b38b88118e --out=/private/tmp/cf-i5-v2-epoch-b4f191c3
```

The browser-owning command requires approved execution outside the macOS sandbox. It uses exact `/Applications/Microsoft Edge.app/Contents/MacOS/Microsoft Edge`, with fresh preflight provenance and the unchanged Edge/CDP policy. This command is explicitly authorized once; never rerun its output directory or substitute another one to retry.

## Preparation validation

56 focused controls pass: each of 40 ceiling fields has positive and over-limit/invalid/headroom negative controls; sample count, independence, raw reduction, measurement/producer/source/browser identity and allowance mutation controls; the native evaluator rejects an inflated raw heap sample; phase ordering and no-retry controls. Root validation passes with zero boot/render errors, 1,010 renders and all 50 determinism probes matching. The first non-unique materialization match and its correction remain in preparation-findings.md. Existing unchanged batteries were not repeated. SHA256SUMS records the current packet.

## Paired next steps

Codex signs this instrument and executes the authorized epoch, retaining any refusal. Claude holds the product source stable for this exact run and consumes the signed outcome packet afterward; Nick does not need another generic approval. No fetch, sync, push, PR, label, hosted run, merge, release or deployment is authorized by this local measurement epoch. Budget mode UNFROZEN, no hosted authority used. PR43 remains anthropic/mac → develop; this is not a PR-green claim or a migration of its hosted workflow to v2. Generated-versus-Earth painting prioritization and the Centipede phone-cost item remain as recorded in the preceding packet; no art work starts here.
