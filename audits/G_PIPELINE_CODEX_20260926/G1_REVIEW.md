# G1 contract review — Codex, 2026-09-26

Read-only review of Claude's measured v2, bound by g1-review-sources.json. No admission or native acceptance is claimed. Intake is byte-identical to ours. The static harness diff changes twelve import paths, ROOT and the path-refusal message only; numerical gates and verdict logic are identical. That relocation is acceptable.

## Contract answers and findings

- Copying subject-source.json is acceptable as canonical species/genome identity input, never as anatomy evidence. Verify name, family, genome and visualKey agree; retain its hash. For a new species produce it from canonical sources. The runner reads the target authoring to select `id` (`s.authoring.id ?? s.id`), despite the README saying the author never reads it. Use the corpus id instead so the evaluation boundary is literal and testable.
- `presence` unconditionally emits absent=[], hidden=[], folded=[]. That asserts visible maximum anatomy without an independent visible count, and cannot represent species absences such as a tail-less primate. This is a blocking acceptance issue, consistent with the erased/duplicated-limb leaks. Output measured visible inventory with evidence, refuse unresolved counts, and never infer a hidden/folded explanation to rescue a miss. No intake or threshold relaxation.
- `materials.surface` family defaults are acceptable descriptive metadata only if accurate. Quadruped='fur or hide' misdescribes salamander/lizard scales or skin; serpent='scales' misdescribes eel skin. Use canonical species material when available, otherwise neutral 'painted surface, material unclassified'; never convert that fallback into an observed material claim or a finisher's permission to add fur/scales.
- Habitat from the pinned Earth profile is acceptable biological provenance. Keep profile id/media/hash and distinguish capability from painted pose and observed supports. `ground+air → land` must not imply no adult flight, and a water profile cannot manufacture observed swimming geometry. Unresolved profile refuses or remains explicitly unknown.
- The coverage provenance says 'median of 1 registered references' for topK=1; more significantly, the unchanged intake always writes manualAuthoring=true and sourceLandmarksReused=false. Registration is automatic and transfers reference landmarks. Preserve intake unchanged as requested, but attach an outer provenance envelope that explicitly records automatic transfer/reference hashes and identifies those legacy intake fields as not an automatic-origin attestation. Never present the inner receipt as proof of manual observation or independently located anatomy.
- Current ADMIT+PASS_STATIC=12/40, below >=30. The requested reference set was38; publish the exact identity delta that made40 and retain results on the original38 as a named subset. Do not change the acceptance denominator implicitly.
- New unlabelled G2 masters are independent tests, not new hand references. Admit their auto-authored packets only after independent anatomy, intake, static and native evidence; do not train and score the same subject or bootstrap unchecked output into the reference pool. Sparse-family expansion is a separate follow-on, not a reason to hand-author new packets.

S4 remains parked while G1 is red. G2 and derived masks continue. This review changes no source in Claude's lane and no intake/conservation gate.
