# Compact counted anatomy review

Scope: current local `openai/mac` compact myriapod count, motion, attack, painter-topology and fixed-socket integration. This is source review plus focused synthetic controls, not acceptance of a painted centipede or a native CPU certificate.

The explicit fourteen walking pairs plus one ultimate pair resolve to 63 named joints: five core joints, 56 real walking-leg joints and two ultimate controllers. The resolved graph has 62 parent edges, 31 fixed source sockets and 28 walking contact chains. The two ultimate appendages have declared nonwalking roles. Each walking chain retains a real Knee and Foot; its Knee pivots about its source socket, with no extra joint or fabricated intermediate articulation. The rigid trunk deliberately has no legacy segment-wave channels.

The proposed texture-part definition contains 32 parts: body, head, 28 whole walking limbs and two ultimate appendages. This count is definition-level evidence. Exact source masks, regional skin influences, published paint and actual part counts still belong to the separate intake/static/native qualification.

The focused controls compare intake and motion graphs and joint limits, preserve the original body/bone/leg ratio bounds, reject malformed count objects and a fifteenth walking pair (67 joints against the unchanged 64-joint cap), and require exact observed leg feature identities. They check every supported count from one through fourteen for valid finite action keys within the original limits and active joints on every walking leg. Indexed folded declarations, absent legacy count/socket identity paths, complete timeline tracks, and actual attack contact joints are covered.

The first run found a real attack inventory defect: the compact body attack required `root` as a painted body-card part, while body-card parts intentionally exclude the structural root. The parent changed only that compact attack requirement to `head`; the structural root remains separately required by the graph. The changed-source run also includes a missing-head negative control. Exact failing source and test bytes are in `compact-count-before-02/`; their hashes match the first receipt.

Measured results:

- `compact-count-tests-01.log`: 7/8 passed; the body attack refusal above is retained.
- `compact-count-tests-02.log`: 8/8 passed, 59ms test execution, 293ms Vitest duration. All 25 explicitly inventoried source hashes were unchanged during execution.
- The earlier fixed-socket results (7 Node tests and 3 Vitest tests) were reused without another run. Their 19 source hashes still matched at this review's first execution.

Command: `npx vitest run apps/game/src/motion/compact-myriapod.test.ts`, from `port/v2`. Each numbered source receipt identifies the exact command, source hashes and scope. The inventories are explicit relevant files, not a claimed complete transitive import closure.

No further actionable correctness defect was found in the reviewed source. Codex retains the first refusal and proceeds with actual paint qualification, final legacy/S2 checks and native proof under the parent task. Claude consumes the eventual signed result; Nick reviews the art. This review does not authorize or claim any GitHub action.
