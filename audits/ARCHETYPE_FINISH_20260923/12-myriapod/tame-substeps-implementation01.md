# Explicit compact tame cadence

Static07 fit11 passed all twelve 121-sample action rows and exact rest, but its presentation failed while fading the completed tame translation into idle. The exact first-failure replay in `static-contact-diagnosis04.json` demonstrated a physically excessive single backward return step. Both anatomical and rest-support candidate targets exceeded leg0Far reach; the raw and travel-adjusted root dx were identical. That failed measurement remains retained.

The compact model now explicitly declares `travelSubsteps:{hit:2,dodge:2,tame:2}`. Only tame was added to the declaration, its type, and the existing runtime admission/application lists. The existing `base + stride * progress` body expression is still cached before feet are subdivided. No geometric adaptation, new degrees of freedom, root-curve reduction, bounds, amplitudes, easing, weights, pins or binding edits were introduced. The existing final signed return step receives the same declared subdivision; zero-travel holds and stage-owned travel keep their prior behavior. Families without this compact declaration remain outside the opt-in path.

Focused verification ran once per owner:

- `npx vitest run apps/game/src/creature-rig-contact-travel-substeps.test.ts`: 19/19 passed. Synthetic outcomes cover exact old/new root arithmetic at source waypoints and substep positions for hit/dodge/tame, signed alternating steps, unchanged amplitudes, forward holds, return continuity/endpoints, stage exclusion, malformed declarations, old no-opt-in behavior and existing impossible-offset/joint-limit refusals. The unsupported-action mutant is now `feed`; it retains the original out-of-scope assertion.
- The single changed declaration-expectation test in `creature-rig-contact-swing-lift.test.ts`: 1 passed, 11 deliberately not selected. It still omits the entire cadence declaration to test the old swing-only behavior.
- `tame-substeps-return-01.mjs`: the actual retained fit11 raw presentation pose passed with all 28 contacts. Independent reconstruction checked anatomical bone lengths, joint limits, each endpoint and original full-LBS painted support. Maximum endpoint error: 2.482534153247273e-16; maximum painted-support error: 0.00031604918256235444 px; compression: 0. Root dx is exactly the prior expression. The explicit old-declaration negative control reproduces both the original reach error and nested support-candidate error. This real-asset check is packet-local; no new large production test fixture was introduced.

The real-pose harness changes only an in-memory policy declaration for its negative control, with an exact inverse recorded in `tame-substeps-return-01.sources.json`. `tame-substeps-checks01.json` retains commands, logs, exit codes and source/input identities. Exact before bytes are under `tame-substeps-before-01/`; `tame-substeps-final-source01.json` identifies the frozen three production files, two affected test files and two updated current references.

These are focused contact results. They do not establish complete presentation, mesh, film, native CPU, signed-source or PR43 acceptance. The parent owns fresh final-source static, TypeScript, S2 and native qualification.
