# Fit06 contact refusals beyond the first ARAP stop

Two contact-only samples reproduced the exact retained target-scan failures and raw attempted poses. The runner rebuilt the real GSAP/performance/contact path and inserted only invertible observation hooks; 47 source files and all inputs remained unchanged. No ARAP, full row scan, native film, or alternative model was executed. Exact trace, source snapshot, hashes and measurements are in the adjacent numbered JSON, TS, MJS and log files.

- **Hit, 356.25 ms, leg13Far:** group 1 is almost finished swinging through the recovery step (`at=0.9583333333333334`); the eased foot has nearly reached the final waypoint while linear body travel is only `0.4791666666666667` through the segment. The first anatomical endpoint check measures **104.10069283467372 px** against **104.0497743848867 px** maximum reach. The target is above its socket, where the existing downward compression path cannot repair this extension.
- **Dodge, 46.666666666666664 ms, leg0Far:** group 0 is still planted. Its endpoint remains at source while the body retreats **20.067455231864486 px**. The first endpoint check measures **97.94602914724727 px** against **97.75666971315175 px** maximum reach. Again the endpoint is above its socket. This failure precedes the painted-offset fixed-point iterations, so it is not iterative divergence.

The source-step compiler uses the correct keyframe intervals. The authored easing is intentionally replaced by the existing linear body / smooth alternating-foot schedule; retained external residuals here are only GSAP rounding (measured in findings JSON), not a clock or source-offset error. `toward-socket` correctly retracts the swinging hit foot and does not move the planted dodge foot. The mismatch is the schedule's required span versus the authored chain reach; no limit increase is justified by these measurements.

The anatomical endpoint precheck is not proof that every painted-support solve is impossible. Current all-contact analytic fallback remains ineligible because leg0Far, leg8Far and leg12Far have nonzero mixed support contributors. The separate geometric painted-target comparison in findings JSON does not execute an alternative solver or certify one. A repair must preserve actual contacts and bone/joint/paint gates; the evidence does not justify discarding tiny contributors, clamping a target, or retuning uncertain source landmarks.
