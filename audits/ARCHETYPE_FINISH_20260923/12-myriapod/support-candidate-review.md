# Support-candidate fallback: bounded read-only review

No material finding. Reviewed the current contact owner against `support-candidate-before-01/creature-rig-contact.ts`, its exact replacement manifest and the focused test source/log. No tests, replay, browser or runtime edits were performed for this review. Reversing all declared changes exactly recovers the retained predecessor bytes.

- Successful iterative results still return through the same measurement/return path. The new branch runs only after an iterative refusal or paint residual above the existing0.25px limit, with the existing offset eligibility. Endpoint-only candidate arithmetic remains unchanged.
- A mixed-support fallback restores the complete authored/travel-adjusted local pose before constructing a candidate, then resets accommodation to the original uncompressed root. The shallow baseline is safe here: pose keys are replaced, not mutated in place. Input pose, record and support models remain untouched.
- The geometric support point supplies only a provisional two-bone candidate. Every active chain is posed before the final `measure()` reads the original full weighted support ensemble, including tiny foreign contributors. No weight is dropped, rounded to zero, or relabelled endpoint-only.
- Candidate endpoint targets are derived by rotating the original anatomical lower-bone vector from the solved knee. Original source pivots/bone lengths and joint limits remain in force. The same1e-8 endpoint check,0.25px paint limit,8%compression cap and existing accommodation arithmetic are unchanged.
- A failed candidate throws before any pose is returned or published. Contacts and working pose are per-call state; the sole retained candidate-chain cache depends only on immutable source anatomy. All candidate endpoint targets are overwritten together before successful measurement. If the iterative path failed first, its original error remains outermost and the candidate refusal is retained as `cause`.

The existing focused result is9/9 PASS (`support-candidate-tests-01.log`, six new controls plus three rigid controls). The new controls cover both retained actual failures with all28 original weighted supports, unchanged successful predecessor results, a heavy foreign-weight residual refusal, impossible external translation, original joint-limit refusal and clean recovery after a failed candidate. Those tests substantiate contact/FK/LBS admission; this review does not claim ARAP, rendered mesh, complete-row static or native acceptance. Root owns those changed-source qualifications.

Reviewed predecessor SHA-256: `320a106834b3bba32e727b95ad13abdf13ff7f74723d40841a464d2b62a040be`.
Reviewed current SHA-256: `b6b7e745c61c6e9634ab4b8fd5c7411913ca383cb7772f5b143b49bcb0149abc`.
