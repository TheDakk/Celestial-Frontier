import fs from'node:fs';import path from'node:path';import os from'node:os';import{createHash}from'node:crypto';import{spawnSync}from'node:child_process';import{rolldown}from'../../../port/v2/node_modules/rolldown/dist/index.mjs';
const [input,out]=process.argv.slice(2),scratch=fs.mkdtempSync(path.join(os.tmpdir(),'cf-r1c-')),sources=new Map(),bundle=await rolldown({input:path.resolve(input),platform:'node',plugins:[{name:'source-hashes',transform(_,id){if(path.isAbsolute(id)&&fs.existsSync(id)&&fs.statSync(id).isFile())sources.set(id,createHash('sha256').update(fs.readFileSync(id)).digest('hex'));}}]});try{await bundle.write({file:path.join(scratch,'run.mjs'),format:'es'});}finally{await bundle.close();}fs.writeFileSync(out+'.sources.json',JSON.stringify([...sources].map(([path,sha256])=>({path,sha256})),null,2)+'\n',{flag:'wx'});const r=spawnSync(process.execPath,[path.join(scratch,'run.mjs')],{encoding:'utf8',timeout:240000,maxBuffer:4*1024*1024});fs.writeFileSync(out+'.log',r.stdout+r.stderr,{flag:'wx'});console.log(r.stdout);console.error(r.stderr);process.exitCode=r.status??1;fs.rmSync(scratch,{recursive:true});
