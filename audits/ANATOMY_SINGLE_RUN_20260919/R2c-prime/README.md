# R2c′ — bounded weighted-support correction

Status: candidate prepared; actual sweep next. Nick authorized §9 after reviewing the S2 stop.
The prior halted packet is now signed/verifiedca851fb6 as evidence, not admission;
[direction hash and signing receipt](direction-and-signing.json). Node26.9.0 unchanged.

Each support reads barycentric-blended bone weights from the unchanged binding. The shared
solver predicts the weighted transformed rest point and adds paintedTarget−predicted to the
endpoint target within the existing3correction passes. Endpoint-only supports preserve the
previous arithmetic exactly, to require bit-identical five-crab rows. No binding/pin/landmark/
limit/threshold or terminal-rule change. Default tuple support API remains endpoint-weighted.

Every sampled support logs exact triangle LBS versus target (kinematic), published−exact LBS
(ARAP), and the weighted-point model versus exact LBS separately. With spatially varying
weights, barycentric blending and transformation do not commute; that covariance must not
be mislabeled as ARAP. The exact-triangle probe is independent of the solver prediction.
Full per-sample vectors are retained as compressedJSONL for each actual subject.

Controls: terminal100%→zero first correction;50/50→half rigid correction; unpinned diffused
mutant2.64794px fails, pinned0.000025px passes.23contact tests (2new+21existing), independent
probe controls, app typecheck, native-entry syntax and rootvalidate pass. Actual five-crab
bit identity and Civet0.25px admission remain pending the single signed-producer sweep.

Next: static sweep; S2 remains the only run halt. If green, resume §8 at R2d→R3→R4 then native
recapture→R9/Q1→integrity/phone→roster→local PR42split. No stage is silently skipped.
Codex continues; Claude read-only, no sync or app switch required. No GitHub write.
