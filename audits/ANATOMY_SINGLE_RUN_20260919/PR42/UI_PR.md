# Update production UI and field interaction surfaces

Base: `develop`  
Source: `openai/mac`  
Promotion: normal merge commit after prior tiers; no squash.

Update the production UI surfaces and their interaction wiring represented in the UI path manifest. Keep native control reachability, small-screen layout, protected persistence and development identity under their existing owners.

Validation on the assembled source: root validation passes. Both local phone Glass canaries retain instrument failures in the anatomy packet. This path projection still has forward engine dependencies from main.ts and requires hunk extraction before standalone admission; it is not Ready.
