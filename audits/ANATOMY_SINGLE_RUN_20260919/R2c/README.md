**S2 STOP:** five crabs pass; Civet idle261.948177ms fails0.260840176px. No retry.
R2c signing was refused; candidate/evidence staged, not a signed producer.
[Complete packet](../README.md).

# R2c painted-support candidate

Candidate: observed source support → corrected endpoint; initial IK plus at most3fixed-point
corrections. Vertical root accommodation bounded by8% declared scale. Endpoints<=1e-8,
paint<=0.25px, limits, source pixels, joins and all input bindings remain unchanged.
Native gate uses actual published paint, separately from solver prediction.
Existing hip ownership retained: R1c demonstrated missing accommodation, not an independent
requirement to transfer authored gait hip rotations into the contact owner.

[Controls](controls-02.log): exact previous zero-offset pose; doubled-offset model residual
below0.000076px; actual synthetic compiled-field/ARAP pinned control0.000025px vs unpinned
root-diffused mutant2.64794px (correctly fails0.25). No real-subject acceptance from controls.
First instrument preparation omitted required paint-skin schema; three vertices were valid.
Original source/log retained as controls-instrument-01.ts and controls.log. Corrected fixture
adds only schema, with fresh output. No runtime fix/retry was prompted by that instrument error.
App TypeScript and root validation PASS. Actual static sweep ran against retained candidate source hashes after signing refusal; no certification claim.
Any shared red halts the run, no retry. Native pixels/CPU remain unqualified by a static sweep.

Codex stopped at S2. Claude read-only; no app switch, sync or GitHub operation.
