# Playable landing and battle checkpoint — review notes

Local campaign authorized by Nick September7; source starts at signed837db4a on openai/mac.
This batch implements presentation over existing actions and repairs failures discovered in
native play. It does not modify saved genome identity, combat outcomes, rewards, canonical
species artwork, authored audio recordings, caches, Actions workflows or production identity.
Final check/source/package records are in README.md and manifest.json.

## Implementation and deliberate limits

- Native Land captures shared audio activation before persistence. One doLand call must finish
  with the same runtime, exact committed revision and fresh rendered destination/epoch receipt.
  Training, refusal, protected/replaced state and unpublished recovery cannot stage success.
- Optional `?avpilot=1` Earth/eligible-Scout presentation waits for the matching asynchronous
  vista and decoded images within the original10s admission window, then ends after1.8s.
  Phone uses a72×44px slot in Survey's existing header; Close/refill cancels it. Reduced motion
  uses a static pose; Effects Off and ineligible routes preserve ordinary presentation.
- The existing1.4s landing recording obeys explicit pilot sound opt-in. A route-bound activation
  ticket promotes only the original trusted gesture. Prior game playback retires without
  closing the shared context; consumed old ecology/expression activation continuations are
  invalidated by the existing gesture serial and cannot mute the new sound.
- Native Combat Chronicle stages at most two132px canonical portrait leases. The actual
  registered settlement supplies creature genomes; Explorer uses a nameplate. The existing
  Chronicle cue clock drives finite200ms attacks/recoil/dodge/stun, exact cue captions and
  static reduced-motion feedback. Close/hide/replacement/dispose release resources; Skip
  keeps the static result. It creates no second combat simulation or reward owner.
- This is whole-portrait motion. Connected creature anatomy, skeletal movement, biome
  locomotion, new universe materials and wider authored-audio coverage are subsequent work.
  Read CREATURE_BLENDER_FINDINGS.md before that implementation: the existing complete genome,
  named whole-form owner and compatible morphology are authoritative.

## Native findings and why each retained result matters

1. `landing-scene-covered`: frame/audio diagnostic passed, but visual inspection found Survey
   hiding the central ship on a phone. Instrument PASS alone did not establish useful paint.
2. `landing-header-overflow`: header placement exposed the ship, but early motion protruded
   2.13px above its header. The retained geometry check failed; its boundary was not weakened.
3. `landing-header-contained`: changed initial displacement from−8px to−4px within the same
   44px slot. Movement, loaded art, opacity, header containment/hit ownership, cleanup, trusted
   Land and running1.4s PCM source passed. Desktop landscape/Shipyard captures are included.
4. `battle-wait-timeout`: native Land and Challenge completed, but the diagnostic lost its
   intended diagnosis because a25s browser promise exceeded the15s CDP command cap. No battle
   presentation PASS. The corrected observer uses a12s internal bound and retains failed DOM,
   trace and screenshot before closing its disposable browser.
5. `battle-native-state`: durable Earth loss was visible (100→72HP, receipt1/revision5), while
   the Chronicle stayed hidden/empty and no actor frame existed. This establishes a real
   presentation failure, not lost gameplay commitment or an assumed missing click.
6. `battle-caught-exception`: bounded debugger capture, immediately auto-resumed, identified
   `TypeError: combat ability theme is invalid` before Chronicle opening. The only other pause
   is the diagnostic's own terminal promise rejection; resume error list is empty. Canonical
   Explorer Frontier Resolve lacks creature theme/label/color fields. Unit fixtures had used
   creature battleStats as a player and therefore missed the real player projection.

7. `explorer-landing-battle-pass`: changed canonical player metadata projection, then reran the
   ordinary fresh game journey without debugger instrumentation. The Chronicle opened with
   loaded canonical defender art, real attack/reaction frames and its correct final result;
   finite landing/audio checks also passed. The completed scene retained zero animations.
   Phone and desktop screenshots were inspected. This closes the bounded presentation failure.

Native visual follow-ups: the current Explorer nameplate and whole-portrait translation are
placeholders for later coherent character staging. Existing second-person transcript grammar
(e.g. “You burns”) and the upfront settled-outcome toast can be polished with the future battle
presentation work; neither is repaired or passed as final creative quality here.

Earlier reports remain unchanged; later success cannot certify their source or erase them.
These are local, dirty-source diagnostics, not Slice/Glass/Compendium certificates. Their
workingPatch hash does not inventory untracked new files; the final source manifest and clean
preview bind the completed source. Built service-worker identity and retained PNG hashes bind
these diagnostic captures. An AudioBuffer source starting is not speaker-output verification
or human listening. Headless Chromium is not physical iPhone/Safari/PWA acceptance.

## Static preparation findings

The first complete current `develop` profile stopped after3604 tests passed (1 skipped): six
failures in four test files still expected the old81-bullet release inventory or the old one-line
Chronicle callback. These are retained in `logs/cf-av-develop-static-20260907.log.gz`. The current
release has83 actual bullets; all independent count readers and missing-one controls are updated
together (83/82), preserving fixed authorities and mutation coverage. Main's audio callback still
runs after optional visual presentation. No production count or threshold is relaxed.
Earlier focused failures include unavailable TypeScript7 transpile API, test fixture ordering,
strict test type errors and the first Explorer negative-control helper using that old API.
The existing Rolldown transformer now executes those helper controls; no dependency changed.

## Claude review focus

Review exact durable-to-presentation ownership, stale activation resolve/reject controls,
player-only Frontier Resolve metadata adaptation and preserved creature rejection controls,
Chronicle timing/participant fidelity, phone header containment, resource cancellation and the
native screenshots. Preserve U2's independent verification blockers. Then review the planned
complete-genome→morphology→Blender bridge and rendered coherent-canid comparisons when produced.
No review message has been sent. Human art direction and matched-level listening remain open.
