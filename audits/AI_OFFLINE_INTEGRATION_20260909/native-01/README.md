# First offline integration attempt — FAIL before generation

The real static package booted, accepted trusted Install and started its first exact model
request through the measured service worker. While the write was active, the detached diagnostic
inventory threw NotFoundError during directory/file enumeration. The exact entry was not captured;
this does not prove model loss or identify a product storage defect. Native Pause, full installation,
offline verification and Land were not reached. The mirror transmitted a partial first request.

All 31 source hashes stayed unchanged, and target/browser/owned profile/servers/locks closed.
The exact failed runner/helper are preserved in failed-observer-source and match this run's hashes.
The successor changes only the early observer: wait for the named fourth committed chunk, then
pause normally and retain the full strict quiescent inventory/readback checks. No test result,
product storage path or original model bytes are changed or relabelled.
