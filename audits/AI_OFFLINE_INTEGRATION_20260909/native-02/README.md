# Native offline normal Land — retained FAIL

The changed observer completed full install/pause/reload/resume and offline verification, then
actual normal Land reached portable inference and failed at the unchanged 600,000 ms model-stage
timeout. No completed denoising step, image, Ready, Inspect/View or retained-original reload was
reached. This is not an offline inference PASS or phone qualification.

[Raw result](result.json), SHA256
`690edcd99b5ea7af1d03e9604f2219734ddff6f6cf9cda75b4d19d5ca9594d39`, and the
[derived summary](summary.json) preserve the exact boundary. Source hashes bind all 31 measured
files unchanged. The exact optional package remained 97 files / 56,529,354 bytes with manifest
`5dc7a6e41eebe0e71c399db7c526d87ca47a43d850bb1fc3111dcf8c94aa0d5c`.

Positive subresults within the failed run:

- Trusted Install, named committed fourth chunk, native Pause, new-document reload without
  automatic resume, then trusted Resume into the same attempt. All 6,691,020,416 bytes / twenty
  files persisted in 6,392 chunks. Resume-to-Ready took 103.635 s on the local exact-byte mirror;
  this is not a CDN or phone download timing.
- Both origin servers physically closed before offline mode. The actual native full-model Verify
  took 79.326 s. A detached build of the production storage owner separately rehashed all bytes,
  read all twenty native Blobs and rejected a shifted actual Blob slice through the same acceptor.
  The controlled static cache held 99 entries / 56,490,756 payload bytes.
- The real module-only worker replied through its pre-GPU invalid-profile guard. Normal Land
  committed canonical Earth durably and released the gameplay transaction before model work.
  Native Notifications opened during preparation, while the job stayed independent and unmounted.
- Text preparation and all six reference encodings completed. Portable `q8Block32:false` entered
  “Drawing the landfall” at 31.706 s after Land. The complete full-identity recipe is retained in
  [actual-landfall-recipe.json](actual-landfall-recipe.json), bound by recipe key
  `43d256089080d69bdea6bcf882dd080f95af43d8a6084f60c12204cb7523a06d`.

The product then reported `Local AI stage timed out`, with no original ID. The diagnostic's
`modelExecuted:false` means no completed denoising-step message was observed; it does not mean
text/reference models did not execute. `modelExecutionAttempted:true` remains explicit. The
600 s stage deadline includes denoiser loading and all four steps; progress does not reset it.
No stage or whole-run latency is fabricated from the last progress timestamp.

Failure capture also timed out waiting for `Page.captureScreenshot`. That secondary failure is
retained, so responsiveness is proven only at the earlier native Notifications action, not over
the full inference or failure. Inspection of screenshots 04 and 06 confirms their narrower scope:
04 shows the controlled Sol screen with model storage collapsed; 06 shows the early Landing row,
“Loading model · ETA estimating”, during the existing Earth transition. Neither is a completed
painted landfall, numeric drawing ETA, final error screenshot or art/placement acceptance.

All owned browser/profile, target, two servers and workspace-lock cleanup fields passed. The
first native01 observer failure remains separate. No unchanged retry, model download, timeout
increase, package promotion, quality acceptance, physical-phone claim or hosted action followed.
