# Claude handoff — offline normal-game AI and V2 species review

This continuation starts at signed `8c10463a7b0330e12f5ca42f958c85ab9b7eb6a3` on `openai/mac`.
HEAD remains signed `8c10463a7b0330e12f5ca42f958c85ab9b7eb6a3`; this completed continuation is
staged locally. Both signature attempts failed with an agent communication error, so no new
commit exists yet. Nick asks to keep developing and update Claude's handoff. No Claude invocation,
hosted action or schedule.

## Game direction and accumulated review scope

The intended review in Nick's Claude app has not happened. Read the accumulated
[direction and changes](../CLAUDE_DIRECTION_REVIEW_20260909/HANDOFF.md),
[normal-game integration](../AI_GAME_INTEGRATION_20260909/HANDOFF.md),
[full-painting viewer](../AI_LANDFALL_CONTINUATION_20260909/HANDOFF.md), and
[individual references/mobile package](../AI_SPECIES_MOBILE_20260909/HANDOFF.md) alongside this
packet. These cover the responsive UI, graphics, audio, painted-world direction, local model,
Land/progress/ETA/Notifications/View, retained originals and inspection work awaiting review.

Celestial Frontier remains a browser proof of concept for a later engine game. Landfalls should
be large cohesive natural-history paintings generated locally on demand, with multiple canonical
flora/fauna, shared lighting/materials/atmosphere, grounded anatomy and foliage overlap. Compendium
retains seeded individuals and flora effects. Future articulated 2D battle rigs cover land, flying
and aquatic families; universal objects use the same painted finish. Favorable finish feedback
never waives Earth taxonomy, complete genomes/lineage, biome mapping, exact counts, deterministic
identity, saves or accepted control placement. Art quality must match the approved painted quality and art direction; current output is not accepted.

## New in the staged continuation

- Additive `buildLandfallFidelityV2` and `assessLandfallFidelityV2` bind the exact six-reference
  compiler recipe, full nineteen-resident roster, image digest, ordered source/prepared geometries,
  source hashes and full visual identities. Each observation row binds its reference index/SHA.
  Explicit flora-count evidence prevents unresolved/cropped motifs from counting as complete
  individuals. V1 builder remains text-identical, its eighteen existing controls remain, and
  version-specific contract brands refuse mixing. These are reported evidence, not image
  recognition, observer authentication or automatic art approval; all such approval flags stay false.
- The exact earlier V2 PNG and its independent review now have a reproducible formal V2 contract
  and `species-rejected` assessment. Its original native aggregate FAIL remains unchanged.
  See [species-review](species-review/README.md), including the manual transcription's limitations.
- Optional `--landfall` on the native delivery runner tests actual installed-model inference through
  ordinary Land after the origin servers close. The new helper checks durable landing, independent
  gameplay transactions, live Notifications/ETA, explicit Inspect/View, verified original PNG and
  normal offline reload. It uses the original portable Q8 graph, not the faster desktop derivative.
  It reads the real Inspect viewer Blob without importing unavailable /src files or writing storage.
- Dedicated-worker debugger attachment is removed from this diagnostic. The existing pre-GPU
  message guard proves actual module loading. Only the service worker receives download interception.
  Empty worker-event lists cannot certify no startup: a positive startup observation must exist
  before a no-reload-inference claim; otherwise that field is null, with the narrower zero-job
  observations recorded separately. See [independent runner review](runner-review.md).
- The first native attempt's live directory inventory encountered NotFoundError during installation.
  Its exact entry/cause was not captured, so no product data-loss conclusion is drawn. The corrected
  early observer waits for named committed chunk f0-c3, then retains strict full inventory after
  native Pause and full readback afterward. Missing/not-yet-committed is pending only in that early
  observer; corrupt markers/chunks and unrelated errors still fail. The first run stays FAIL.

No normal player UI, Guide, Training, release note, production version, runtime package or model
bytes changed in this checkpoint. The fidelity owner is detached QA; the other edits add diagnostic
coverage. Existing gameplay placement, timeout and deterministic/saved data owners remain intact.

## Verification

Species review: corrected 28/28, all three TypeScript programs and root validation PASS
(1,010 renders, zero boot errors, 50 unchanged original deterministic fingerprints). All 906
measured sources/fixtures remained unchanged during that chain. First 27 PASS/1 fixture FAIL
stopped before later stages: undefined is hostile input, whereas an actually omitted floraCount
is a complete-JSON missing field. The changed fixture covers both outcomes without changing the
product guard. Exact first sources and logs remain in controls-01.

Runner controls: initial thirteen passed; after the early-prefix observer change, fifteen pass,
including actual PNG content/identity mutations and missing-versus-corrupt prefix controls.
Root validation passes again on that changed source. These remain focused receipts, not a full
profile or Compendium/Slice/Glass admission. All old failures, source hashes and cleanup evidence
remain accessible. Native results follow.

Native02 remains **FAIL** at `Local AI stage timed out`. It completed trusted install/pause/
reload/resume into the same 6,392-chunk attempt (20 files, 6,691,020,416 bytes), offline native
full verification (79.326 s), separate production-owner rehash/Blob reads, shifted-slice rejection
and the actual pre-GPU module reply. Resume-to-Ready took 103.635 s on the local byte mirror,
not a CDN. Both servers were closed; the controlled cache held 99 entries / 56,490,756 bytes.
Normal Land durably committed Earth and native Notifications responded during preparation.

Text and all six reference encodings completed; portable Q8 entered Drawing at 31.706 s after
Land, then hit the unchanged 600 s denoising-stage timeout without a completed first-step report.
No PNG, numeric drawing ETA, Ready, Inspect/View or retained-original reload was reached.
`modelExecuted:false` is the diagnostic's completed-step threshold, not absence of model work.
Failure capture also timed out on Page.captureScreenshot; later responsiveness is unqualified.
All 31 source hashes remained unchanged and owned cleanup passed. The raw aggregate stays FAIL;
there is no retry or timeout increase. [Native02 details](native-02/README.md) and
[source-bound result](native-02/result.json) preserve both achieved and unreached scopes.

## Next bounded implementation

Codex should implement the [first-step diagnostic](native-02/diagnosis.md): exact six-reference
portable workload, before/after execution and output-readback markers plus device-loss/error
capture, retaining the four-step schedule but stopping after one measured step. The timeout is
localized after session creation; GPU cost/allocation/stall remain hypotheses. Do not repeat the
full installation/render or increase deadlines unchanged. No such diagnostic ran in this batch.

## Remaining acceptance boundaries

Six-reference guidance is still global: the pinned graph provides no hard boxes, masks or per-
species attention routes. Full generated anatomy/botany/count/placement acceptance remains open.
The target iPhone/iOS is unknown. Real phone/CDN install, Safari/PWA model allocation, inference,
latency, foreground FPS, memory/thermals and storage durability require actual device evidence.
The experimental twenty-file model is 6,691,020,416 bytes / 6.23 GiB; no mandatory phone model,
cache-budget increase or additional player installer was selected. The optional desktop derivative
is separate. Distribution/SBOM/reproduced-build qualification remains open.

Cross-model original discovery, outside-origin backup/export/exact-image sharing, reload-durable
jobs, adaptive eviction/reservations, broad worlds and physical seasons/orbits remain unfinished.
All-family rigs and universal objects remain future work; audio HUMAN listening remains pending
with its existing 268 focused checks/six DSP renders. ROADMAP preserves all older blockers verbatim,
including SceneMemory quarantine, D-9e gating, prior native/certificate failures and the two rejected
iCloud backup attempts. No retry of that backup or cloud write. No release or deployment.

## Fresh-session and paired Git handoff

Current side: OpenAI/Codex · macOS · /Users/nick/Projects/celestial-frontier-openai-mac · openai/mac ·
upstream origin/openai/mac. Signed HEAD 8c10463a remains 12 ahead/0 behind upstream9bfec7dc and
123 ahead/0 behind developc1791e21 at the last verified refs. Completed source/docs/evidence are
staged, awaiting a working local signature. A later successful commit will add one; 13/124 ahead
is not the current state. No fresh fetch or hosted write.
SSH origin is git@github.com:TheDakk/Celestial-Frontier.git; prior TheDakk/read proof is established.
Ambient .DS_Store stays untouched/untracked. Local signing uses the command-scoped configured
1Password SSH agent and ssh-keygen, followed by an independent temporary allowed-signers check.
The configured inline public key may omit key::; do not reject that valid format or print key blobs.
First local signature failed with an agent communication error; HEAD stayed at 8c10463a and no
unsigned fallback occurred. The configured agent subsequently answered its identity-list query,
and caffeinate PID 93550 was observed. That does not prove signing permission or app lock state.
[First signing receipt](signing-attempt-01.json) and [retry](signing-attempt-02.json) both remain
FAIL. The retry used the responding configured agent but returned the same signing communication
error. Do not describe this batch as committed. Next Git step is restore the existing local signing
path, commit the staged batch with SSH signing and independently verify it; no unsigned fallback.
Do not rerun passed tests or native rendering solely to retry signing on unchanged sources.

Read ROADMAP first, PROCESS_LAWS before UI/tests and PARALLEL_GIT_PROTOCOL before batch/handoff.
Reuse audits/TOOLCHAIN_STARTUP_20260908_CIVET/manifest.json only within the uninterrupted session;
fresh sessions follow UI_TOOLCHAIN startup. Terminal/files/isolated native CDP only, with browser
owners outside Seatbelt and shared foreground/checkout locks. No security or lock-screen changes.

GitHub step: none. PR42 remains parked Draft/unlabeled at remote9bf; base develop, source openai/mac.
No new PR is needed. Its existing title/body still require an accumulated-head refresh before any
future authorized push/Ready/owner label. Current title: “Refine responsive UI and add bounded
audiovisual and painted-world prototypes.” Prepared future title:
“Expand the browser slice with responsive UI, audiovisuals and local AI landfalls”. The prior
handoff contains the accumulated copy-ready description; add this checkpoint's strict V2 review
and actual native outcome below before any future exact-head admission. No current hosted authority.

Claude side: Nick need not open Claude now. Its checkout does not have local Codex changes. Review
this packet when wanted. Only after verified develop integration may clean anthropic/mac fetch and
merge origin/develop; finish/commit dirty work first and never manually copy across worktrees.
Develop/main/live site remain unchanged. Actions budget: recorded UNFROZEN/PUBLIC, private fallback
3,000, zero new exact hosted/owner-label attempts. No labels, Actions, Ready, merge, release, deploy,
automation or scheduled prompt.
