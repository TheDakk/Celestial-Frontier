# Focused impact polish — September 16

Nick praised the connected-throat version and requested another refinement. This bounded pass improves impact readability while preserving the painted anatomy, source masters, motion curves, contact constraints, timing, effects, seeds and outcomes. The previous comparison remains at `/throat/`.

## Change

The diagnostic presentation previously applied 65% full-scene white at a strike. `impact-focus.mjs` now projects the existing compiled flash envelope into a 10% scene flash and a temporary 1.45× brightness on the receiving rig. The attacker remains neutral. A pooled Pixi ColorMatrixFilter per rig is disabled at neutral exposure; no replacement textures or per-creature animation clips are added. Both turn roles use the same side-based rule, independent of anatomy. The helper supports a reduced-motion neutral result; this proof is not new ordinary-game accessibility wiring.

No change to Claude-owned motion/, effects/, battle2/, soundkit/ or worldlife/ modules. No main.ts, kit, original master, inference, GitHub or deployment change. This is a review candidate; it is not ordinary-game promotion or universal anatomy completion.

## Evidence

- `native-01/report.json`, `impactWitness`: actual rendered exposure changes only the receiving creature; zero changed pixels outside its padded bounds with scene flash disabled. Highlighting the wrong actor fails in both roles (over 18,000 outside-target pixels). Mean sky-channel change drops from about 58 to 8.8 at impact, preserving substantially more arena contrast.
- Target/local, neutral and old full-flash PNGs are retained for both impacts. These are actual renders, not conceptual mockups.
- The existing dense gates remain: 601 throat samples × 17 ribbons; visible source joins for Civet, fox and procedural quadruped over 1,803 poses; the Platypus's 23 joins in each pairing. Original sources and source-view fitting are unchanged.
- `presentation-parity.json`: all three turn plans are byte-identical to the prior version; twelve named non-flash PNGs match byte-for-byte and all three strike PNGs change as expected.
- `motion-01/` measures about 60.002 fps for all three clips; whole-frame CPU p95 is 2.8/2.5/1.8 ms. It contains the new actual ten-second films, exact source inventory, frame/CPU measurements and codec checks. Only full-frame timing includes the added head, opponent and render/filter work; the selected-left-rig timing is not their independent qualification.
- `focused-tests.txt`: 31 tests pass, including target-side reversal, neutral miss/dodge, history-free fading, reduced-motion projection and invalid-input refusal, plus the existing head/intake/attachment/source-join controls. `game-typecheck.txt` and `root-validate.txt` remain separate passing receipts.

The before/after player is at http://127.0.0.1:49816/impact/. Review both attack directions at normal and half speed. Nick owns the visual decision. The generic presentation is anatomy-independent; full universal painted-motion coverage still follows `BATTLE_THROAT_JOIN_20260916/UNIVERSAL_COVERAGE_REVIEW.md`.

Signed HEAD remains `1a6dd61d`: 118 ahead cached origin/openai/mac, 229 ahead cached origin/develop. Continuations remain staged pending 1Password signing; no bypass or repeat signing attempt in this batch. GitHub step none, PR42 parked. Codex can continue local work; Claude need not open or sync now.
