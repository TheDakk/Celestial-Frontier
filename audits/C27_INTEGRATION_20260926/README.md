# C27 lane integration / startup

Incoming signed Claude1199e6b498f09646061ca7b608e4e0ede378cfde, own8c41803c. All incoming commits verifyG (retained log). Explicit Nick-authorized --no-ff merge, no conflict markers; hand review retains Codex pin-loader/type boundary/global-Pixi-pool disposal fixes and Claude's newer declaration/contact-support/phase/relay/morph/audio/care wiring. No source was rebound to old memory evidence.

One develop profile:5450pass,6fail,2expectedfail,2skip. The six failures are parkedI5, the known Guide release SHA, and four5000ms timeouts (Arc4 source inventory; two SceneMemory negative controls; painted-card outcome). A concurrent Claude Vitest process was observed on the same managed Node runtime. This is timing evidence, not proof that contention alone caused every timeout. No unchanged retry or allowance change. Root/app/worker typechecks, artaudit, overridecheck, speccheck and overridecontrol each independently exit0. Overridecontrol restored speciesoverrides bytes. No S2 shared-path failure reported by this run.

Startup official metadataPASS. Node26.10.0_1 available but26.9.0 active in Claude's Vitest process93710/worker93714 at startup, so deferred under the idle-only rule; no interruption or runtime change. REAPER suffix resolved current via scoped brew outdated; other authoring tools current. Node26.9.0 retained throughout this batch.

C28–C30 consumed: forecast memo fixed; masters removed; one card/stage tint; water envelope;38wiredarchetypes; audio/care-bond/D18/CFB. D17 is DECIDED but the incoming production settlement still bypasses phase for soloBalancedAuto at combat-settlement.ts714. Claude has an explicit wiring request in our mailbox; S4's phase-enabled raw resolver must not be misrepresented as that current dispatch.

Codex continues S4, then the ordered rig/performance/mask, pack diet, paintings/capabilities, economy, batched Guide, and I5 items. Guide SHA stays explicit until the batched remeasurement; additional timeout owners must be addressed before a green-final claim. No PR/label/hosted/develop/main/release/deploy. Claude reads our mailbox directly, no Nick relay.
