# Complete-genome Wolf → connected Blender candidate — September 8, 2026

**Offline implementation and scoped verification complete; art unaccepted, runtime integration
absent, signing and independent cloud backup pending.** This is one bounded Wolf-family bridge
and candidate within Nick's 24-hour local campaign. It does not complete a whole family or the
universe upgrade. The current playable Scout/Earth/Chronicle preview remains unchanged.

Open [the standalone comparison](comparison/review.html). It preserves actual132/300/440 sizes,
current canonical versus V1/V2, plus five selectable rendered poses. The final document passed
isolated1440px desktop and390px phone checks, ten decoded images and the frame81 selection;
its screenshots were inspected. Large rows scroll without scaling the phone layout.

## What changed

- `port/v2/tools/creature-blender-export.mjs` and its declarations export a complete detached
  genome, exact `speciesVisualKey`, source hashes, actual final painter owner/proportions,
  fixture derivation and explicit supported/static-fallback status. Fixed owned declarations
  are evaluated through existing Rolldown tooling; no dependency or runtime import was added.
- The four frozen inputs are the actual pure Wolf (seed792844710/catalogue105/heat1), both
  actual ordered Wolf-lineage crosses and the existing procedural quadruped control. Wolf's
  zero-based hybrid-matrix row is2. All fields survive, including qualified lineage. Known
  derivations are rederived/verified. Seed/name alone are insufficient identity.
- `port/v2/tools/blender/creature_canid.py` admits only the pure named Wolf and its actual
  `QUAD_SPEC.Wolf` → `faunaResetCanidC` final proportions. Other routes retain static fallback.
  Normalized pre-fit440px coordinates drive the silhouette; authored depth, tissue joins,
  skeleton and weights supply the3D construction. Generation, breeding, saves, protected
  painters and canonical runtime portraits are unchanged.
- V1 used coarse source surfaces. Its rendered facets prompted one bounded V2 correction:
  48 tube sides,32×64 ellipsoids and five monotone Hermite subdivisions per loft segment.
  Original rings remain exact, channels stay within segment extrema, and no source proportion,
  bone, pose, voxel size or material changed. Both private masters are retained.
- `port/v2/tools/blender/audit_canid.py` independently reopens the saved master, verifies actual
  mesh/rig/identity, checks all97 evaluated frames and runs four scratch-memory negative controls.
  It closes without saving and proves the original master hash remains unchanged.

## Final candidate evidence

| Property | Observed result |
| --- | --- |
| Main skin | 1 connected component;68,890 vertices /68,888 polygons;0 nonmanifold edges |
| Weight sum maximum error | 4.47035e-8 |
| Planted paw contacts | 1,364 vertices across all4 paws;maximum drift6.03279e-8 scene units |
| Actual finite motion | 97 frames at24fps;48 curves/480 keys;no drivers,NLA or cyclic modifiers |
| Movement / rest | maximum skin displacement0.073987 scene units;frame97 delta from frame1 exactly0 |
| Negative controls | disconnected skin, wrong phenotype, wrong key and moved paw all rejected;restored model passed |
| Render | Blender5.2.1LTS build9e2066aef7ef;M4Pro16-core Metal only,CPU disabled;440×440,16 samples,seed0 |
| Five rendered poses | frames1,25,49,81,97;V2 process6.468s;master untouched |
| Structural audit | fresh saved-model process7.193s;PASS;human visualAcceptance remains UNREVIEWED |

Final master SHA256 `a5e635a7587e1e8b8be7a3bd3636f56375ba84654989e696cf251b4a1c188e8a`.
Recipe SHA256 `aab9af8cf6b0d310bf9d53414e3c895321e47b2787f423f4981b533c8225d771`.
Complete Wolf input SHA256 `757478b4b85981601e3b2c0077828893bf7fe184813710f039866e1b9bd37476`.
Actual canonical reference images and all3 extracted visual keys match this exact genome.

The surface is visibly smoother, but still clay-like. Limb junctions remain abrupt; fur/coating,
face anatomy and material detail need art work. Inspection/idle movement is subtle and grounded;
this is neither a walking cycle nor a skeletal attack. Do not install this candidate as accepted
art or substitute it for descendants/procedural creatures. All eight family goals remain open.

## Checks and retained first findings

19 focused exporter tests passed, including source/identity/derivation/admission and rejection
controls. All three TypeScript programs and root validate passed (1010 species and50 unchanged
deterministic fingerprints). The corrected bridge chain is retained under logs/. Final batch
root validation is recorded separately; the prior327-file/3610-test develop profile belongs to
the preceding playable batch and was not repeated for this offline authoring-only change.
No new Compendium/Slice/Glass certificate, physical iPhone/Safari/PWA or human art/listening result.

- Initial focused setup stopped with18 skipped: Rolldown emits inert `export {};` after erasing
  exported types. The bounded source reader now accepts exactly that empty marker while still
  rejecting actual unsupported export/import forms. Original red log and corrected PASS remain.
- Source review corrected the Wolf matrix row, complete child lineage and fixture provenance
  before frozen export. These are review findings, not invented executed-test failures.
- First canonical300px observer incorrectly expected a native300 source. Current game law
  displays440 at300; corrected observer requires native440/display300 and records browser resampling.
  Original timeout/red and all resulting source-reference PNGs remain.
- First actual Metal qualification took151.113s compiling kernels. A bounded process sample
  observed Cycles Metal compilation/XPC waits, then the render completed. This was not a crash
  or measured CPU comparison. Candidate render receipts separately prove their actual backend.
- First standalone review falsely passed a body-overflow check at requested390 while innerWidth
  had expanded to466. Its page, screenshots and PASS remain under comparison-initial/ as
  insufficient evidence. The final pose figure uses the existing scroll container; new observer
  explicitly requires innerWidth/clientWidth/scrollWidth390. Old row466 fails that new predicate.
- Inkscape's fresh outside-sandbox export succeeded with a nonfatal CVDisplayLink warning. The
  exact PNG matches its prior success. Its first controller misparsed ImageMagick `0 (0)` as
  nonzero; direct byte equality established the artifact PASS without rerunning Inkscape.
  See [separate confirmation](../INKSCAPE_CONFIRMATION_20260908/README.md).

## Preservation and Claude review

Logical private source bundle: `cf-creature-canid-sources-20260908`. Ninety original source,
model,input,recipe,render and evidence files are hash/mtime-preserved in existing private durable
storage. The full prepared package has94 files/24,022,413 bytes. Its deterministic archive SHA256
is `d258b60cfbfbf013cef0da8af8a956e4437aed0a8992b9bc5a442697b6ac5516`.
Locations remain in the private index. No editable .blend enters public Git.

**Independent cloud backup remains pending.** Automatic approval review rejected the copy twice:
it requires explicit authorization for this new payload/destination and did not accept the
repository's recorded iCloud selection as that authorization. Nick has a concrete pending
question for the prepared24MB copy into existing Files/Celestial Frontier Backups. No upload,
eviction or download began. First local payload inspection omitted `.js` from its extension
allowlist; all8 are known project authority files and the corrected inspection passes. The
private package is unchanged. See backup-pending.json; previous pilot backups remain verified.

Claude should review: completeness of source/lineage ownership; normalized silhouette versus
3D anatomy; whether the connected mesh/grounding audit proves its claims; unfinished visual
junctions/materials; and how an eventual optimized sprite/rig adapter would preserve exact
identity, visibility/reduced-motion/resource ownership before runtime integration. Do not broaden
all families from the strength of this one structural PASS. No review message was sent.

Required SSH signing still fails on the previous1Password connection attempt; no retry or
unsigned fallback occurred in this batch. Current source parent remains837db4a, not a commit of
this new work. Historical U2 blockers and native-certification boundaries remain in ROADMAP.
Codex owns this local openai/mac checkpoint; GitHub step none, PR not needed. Claude need not
open/sync now and has not received these unmerged files. After a later authorized openai/mac→develop
merge, Claude may fetch/merge origin/develop into its clean anthropic/mac branch, preserving its
unmerged173c806. No develop/main/dev-site/production change. Actions UNFROZEN/PUBLIC, private
fallback3000, exact hosted authority0, attempts0, cost0.
