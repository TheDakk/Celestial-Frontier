# Painted variation and motion review — September 16

Work in progress. Nick approved the painted direction of seed10271 candidate02, then
requested variations and motion checks. This batch keeps that source, adds a second
painting of the same genome and a crystalline seed10032 candidate. These are three
paintings of two genomes, not three new procedural species. Source genomes remain
unchanged. Each painting has independently observed, hash-bound landmarks and parts;
all use the same quadruped template and motion compiler, with no clip overrides.

Generated masters and prompts remain unchanged. The new crystalline image delivered
alpha despite the opaque-key request; the broader rosette candidate and earlier
approved-direction candidate miss the requested safe margins. Animation diagnostics
do not grant kit intake or final image acceptance. Existing accepted game artwork
and kit wording remain unchanged.

The old cut-out loader overwrote native alpha with a colour-only mask, resurrecting
invisible RGB. Authored intake now preserves delivered alpha, refuses blank and mixed
opaque-border exports, and leaves opaque keyed intake byte-identical. The negative
control turns a 256-pixel subject into 900 opaque pixels with the former loader.

Preview diagnosis: first sandboxed curl failed; the external loopback check returned
200 for the actual MP4 and JPEG. The initial claim that the server was down was
incorrect. The user's blank embedded player needs presentation/fallback verification.

Final diagnostic results and exact candidate directories are in README.md/results.json.
Embedded Play verified;12focused tests and root validation pass. No final art acceptance.
