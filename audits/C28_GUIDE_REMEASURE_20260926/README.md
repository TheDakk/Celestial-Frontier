# C28(f) / C17 / D19 — integrated Guide and release reconciliation

Integrated Claude’s signed 832dc18b ancestry through a hand-reviewed no-fast-forward merge, including C32’s solo Balanced Auto Guardian/Titan phase dispatch, care, A5 and the companion XP mirror. Candidate03 remains rejected and unwired. The combat constants, memory v1 budgets/samples, accepted bindings and S2 sentinels are unchanged by this batch.

The cumulative development release now has **106 measured ordered bullets**, SHA-256 **9ca05cf7d271f1a93567861037e8f8ce0841af97bb7afa2f814e16b34719ebe5**. It includes the existing 7ea28e02 motion sentence, C17, D13 care/bond, D15 audio, A5/CFB/D18, D16 parity and D19. `added-bullets.json` retains the19 additions. `measured-copy.json` includes exact rendered rows, source hashes, section counts, current topic inventory and briefing bytes. `measured-copy-initial.json` preserves the earlier measurement before a sentence-boundary clarification. Historical legacy Guide/release bytes and production version are untouched.

Five read-only Advanced Briefings use the actual Main Guide toolbar, lazy publication guards, native Previous/Next/Finish and topic buttons, and heading focus. Browser-free Main outcome tests traverse all pages and links, check unchanged expedition state, and reject disconnected start/next/link mutations plus an invalid page index. Current care, Rest, bond, duels, Harvest and Guardian Recovery replace stale unavailable/permanent-loss claims. The41-topic inventory is37 partial/4 unavailable. Existing required-copy, removal, reorder, contradiction and restoration controls remain active; care controls now reject free healing, closed-game Rest and bond decay instead of rejecting the newly shipped care features.

## Native review

`native-02/report.json`: PASS in exact local Edge154.0.4258.37, actual Vite app, isolated fresh-save contexts at390×844 and1440×900. Both traverse five pages and finish; desktop Enter and a cross-link work. Disabled-ingress controls refuse and restore. Ten screenshots retained. The390px companions page was visually reviewed: readable wrapped text, bounded scrolling, visible Close and no page overflow. This is scoped UI review, not a Slice, Glass, phone performance or I5 certificate.

`native-01` is retained FAIL: both pointer/touch tours passed, but the driver’s Enter omitted Chromium’s renderer text fields on macOS. Its frozen runner is retained beside its report. The corrected driver uses the established Slice Enter protocol (text/unmodifiedText carriage return); there was no product mutation or unchanged retry.

## Verification and retained findings

- Current Guide/release contracts64 PASS; secondary Glass/crafting/Training contracts31 PASS after stale controls were repaired. New Main briefings6 PASS. Exact full-battery results follow in the final handoff below.
- Root validation PASS, including50 deterministic baseline probes and1010 renders. Root/app/worker TypeScript, artaudit, overridecheck, speccheck and overridecontrol all exit0; see `check-results.json` and logs. No allowance was raised.
- The first profile exposed five more stale copy checks, plus the known I5 producer drift:5466 PASS/6 FAIL/2 expected failures/2 skipped. Intermediate diagnostic logs preserve every assertion failure. Glass’s Settings paragraph controls required their correct shifted targets after the new preferences paragraph; the checks still mutate the actual required copy.
- Toolchain: official metadata/idle-process/shared-lock receipt; Node26.10.0_1 and inspected simdutf9.2.1_1 installed, seven capabilities PASS. Workspace dependency locks unchanged. All preparation outcomes retained.

## Paired next steps

Codex: push only openai/mac after the repaired local gate is confirmed, using measured packs below2,000,000,000 bytes. Then declare a fresh S4 production-route corpus before tuning; never reuse the rejected candidate03 held-out seeds. Continue the remaining ordered sprint items.
Claude: consume the signed merge/Guide result from the shared object store; retain C32 and the other integrated wiring, and keep candidate03 unwired. Read Codex’s mailbox for each following packet; no Nick relay or app switch is needed. Native review does not authorize a hosted attempt or battle2 default. No PR, label, hosted run, develop/main merge, release or deploy in this batch.

## Final local result

The repaired full profile (`develop-final.log`) passed5467 tests, with2 expected failures and2 ordinary skips; its5 producer-authority cases were refused together because a port reference was edited during the authority read. This is an instrument refusal, not a memory result. Source was frozen; only that refused suite was rerun with a fresh producer build receipt (`current-authorities-final.log`):4 PASS,1 known I5 producer mismatch. Together these checks leave only I5 red; no whole-battery unchanged retry was used. The7 manual owners remain PASS. The last test-only care controls reject free healing, offline Rest, bond decay and companion poison. No v1 sample was rebound.

GitHub preflight: PUBLIC, HTTPS origin, remote openai/mac8c41803c; workflow/policy diff empty and triggers remain manual or PR-label only. No push-triggered Actions. Budget UNFROZEN; Nick authorizes own-branch pushes despite parked I5. Pack measurements and actual delivery receipts follow separately after the signed commit exists.
