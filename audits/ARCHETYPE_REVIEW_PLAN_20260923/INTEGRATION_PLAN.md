# Integration plan for the reviewed sprint and accumulated branch

This is a plan, not an integration authorization or mergeability verdict. Reviewed code head: `755f52a7d686715657e4ec5924b3e96e5ec8626c`. GitHub’s read-only metadata and local object inventory are retained in [integration-snapshot.json](integration-snapshot.json). No fetch, synchronization, branch creation, PR mutation, hosted attempt, merge, release or deployment occurred in this review.

## Current boundary

- GitHub `develop` is `c1791e210158de864fdd475323c3091d9ecbae58`; it is also the local merge base. The reviewed `openai/mac` head is358 commits ahead and0 behind that exact base.
- The comparison contains22,148 changed paths:21,128 under `audits/` and1,020 elsewhere. [The exact path inventory](changed-paths.txt) records the comparison. There are no `.github/` path changes in it. Counts describe this snapshot, not an approved scope or a line-by-line audit.
- Existing [PR42](https://github.com/TheDakk/Celestial-Frontier/pull/42) is a draft from `openai/mac` to `develop`, at the reviewed head. Do not create a duplicate sprint PR or treat its current mixed backlog as a narrow sprint change.
- Existing [PR43](https://github.com/TheDakk/Celestial-Frontier/pull/43) is `anthropic/mac` → `develop`, head `5154c64f6f5fe0b9a75bc3f7a51e7da56b6b2e7f`. Its open/non-draft metadata is not evidence that required checks pass. No PR43-green claim follows.
- Sprint technical results remain historical, source-specific evidence. Python’s newly documented visual findings hold visual acceptance; other item art choices remain Nick’s. These local archetype proofs do not establish production roster admission or current integrated-source certification.

## Recommended sequence

1. **Resolve the Python visual finding first.** Follow ART-PY-01/02 in [ART_REVIEW.md](ART_REVIEW.md). Diagnose exact retained geometry/ownership before choosing a repair; preserve the failing images and historical measurements. Run affected checks only after a real change, with no gate loosening or unchanged retry.
2. **Complete the promotion-readiness inventory on `openai/mac`.** The earlier C5/pruning plan is a starting list, not permission to delete modules by stale filename. Recheck each module’s current importers, runtime reachability, tests, release-note claims and pack/PWA inclusion before proposing removals. Keep all required source evidence. Establish current evidence for Civet2D, the weather choice and phone-tier decision; this art review did not verify those separate readiness conditions.
3. **Resolve promotion topology and evidence storage before Git writes.** The current protocol requires three ordered tiers—production UI/gameplay, painted landfall engine, research tooling—and merge commits. It also says no new branches and no history rewrite. Three independently scoped heads cannot be obtained merely by opening three PRs from today’s same mixed head. Nick must choose an explicit implementation of the tier plan, or explicitly revise it to one reviewed combined PR. No temporary review branch, reset, force push or cherry-pick reconstruction is implied. Bulky-audit LFS migration remains an in-principle approval awaiting the explicit go required by the protocol; do not perform it as ordinary pruning.
4. **Reconcile shared ownership against the actual integration order.** The sprint carries shared rig/contact and narrow motion changes plus local derivatives of Claude’s stage modules. Compare exact files and source hashes against the selected PR43/develop bytes once the order is chosen. Claude’s worktree remains read-only. A clean ancestry relationship to today’s develop does not prove compatibility with a later PR43 merge. If the base changes, refresh the scope and exact-source admission rather than reusing stale claims.
5. **Freeze one concrete candidate and run its admission once.** Commit the scoped candidate with a good signature. From `port/v2`, run `node tools/tracked-input-preflight.mjs --profile=develop`; it owns one complete develop profile, so do not also run a duplicate aggregate battery. Follow its changed-input owners and preserve the first red result. Check actual current producer authorities; never rebind old Compendium calibration/certificates to newly integrated bytes. If a fresh full-chain certificate is required for the chosen boundary, use exact Edge on clean unchanged committed source and run the owned chain once. This review itself triggers no certificate.
6. **Request one exact hosted attempt only when the candidate is concrete.** Refresh the chosen PR’s title/body for its actual scope and full head/base. Nick supplies the exact owner label/attempt authorization. Default `actions-budget-approved` is the bounded agent-to-develop lane, including its phone canaries; `actions-full-chain-approved` adds the full develop chain. Current documented cap is122 total runner-minutes, one attempt/no retry. Authorization must name the actual workflow, PR, head/base and cap. Historical local proof does not satisfy a missing required `battery` context.
7. **Merge normally only after the exact candidate is clean, mergeable and terminal-green.** Use a merge commit, per the later September13 override. Then Claude may fetch/merge the resulting develop into its own clean branch under a separately applicable sync instruction. `develop` → `main`, production SceneMemory activation, versioning and deployment remain separate authorization boundaries.

## Which older instructions are superseded

The September12 CLEAN_PROMOTION_PLAN.md contains squash-merge language and describes Claude’s branch as documentation-only. The later PARALLEL_GIT_PROTOCOL.md/WORK_ORDER.md override squash with merge commits, and today’s PR43 is implementation work. Preserve that historical plan but do not execute its obsolete statements. No new branch, LFS migration or broad rework is authorized by this review.

## PR planning details

No new PR is needed now; PR42 stays draft/parked. Future base is `develop` and current source is `openai/mac`, subject to the explicit topology decision above. A final copy-ready PR title/body must be written after pruning fixes its actual scope. Writing a narrow “archetype sprint” title for the current358-commit backlog would misdescribe the PR. The eventual description must identify changed owners/default-reachable behavior, exact tests and source authorities, remaining art/device limitations, head/base, Claude’s synchronization consequence, and the no-release boundary.

## Current paired handoff and budget

Codex has the completed review and plan staged for a signed documentation commit; both permitted attempts returned `agent refused operation`. See signing-refusal.json. One renewed attempt awaits Nick’s until-quit SSH approval. This follow-up has no additional push authorization. The previously completed sprint push was verified at755f52a7 by tool0e2f2b. Claude does not receive this local follow-up automatically and should not copy files manually. Nick may open Claude to discuss the already-published sprint evidence; no sync is required now. GitHub step: none. PR creation/update: none. No release or deployment.

Budget mode UNFROZEN; last verified repository visibility PUBLIC (66d200), private fallback3,000. Zero exact hosted attempts authorized. The previously inspected workflows have four manual triggers and one pull_request:labeled trigger; a normal branch push starts no hosted job, but this plan does not authorize another push or label.
