# Route-control chain — preserved instrument red

`browser-free-audit.json` stopped on overridecontrol exit1; no nominal/spec/root/native stage.
The exact named dispatcher controls exposed the next previously unsupported art boundary:
`earth-resident-plan.ts@6247 trusted built-in Object member escapes its approved direct-call context`.
The plain-data snapshot compares Object.prototype and creates detached null-prototype output
with Object.setPrototypeOf, Object.create and Object.defineProperty. The existing art-source
sentinel does not admit these operations generally. This run is FAIL, including its nominal/
restored controls, regardless of individual expected-rejection controls. Source restoration was
verified against786 frozen carriers. No runtime product change or browser retry occurred.

Review the entire exact snapshot function together and admit only its safe detached-output
contract, preserving global method restrictions and adding accepted/restored plus mutation
controls. Do not remove prototype checks or alias the built-in to evade the sentinel.
