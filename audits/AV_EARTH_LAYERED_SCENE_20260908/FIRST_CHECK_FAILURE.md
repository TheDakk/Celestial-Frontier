# First focused verification — retained FAIL

The first chain reached285 passing tests and1failure across18files, then stopped. No TypeScript,
art/static/root or native stage ran after that failure. A source mutation that removes the first
environment publication stopped detecting late cache binding because the new Earth branch added
a duplicate assignment. Fixed by restoring one common world/environment publication before any
variant or cache branch, not by weakening the existing mutation control.

Independent source review also found the new worker was omitted from sealed-entry graph checks;
its exact prefix and generated import/nested-worker rejection controls now join the existing two.
Earth pending publication now compares captured ecologyEpoch directly with currentEcologyEpoch(),
so a hidden deferred scene refresh cannot mount an old epoch. Retryable texture-release failures
are being contained per-resource before any backing canvas can shrink. These are scoped new-code
corrections; all previous evidence stays immutable. The documented D-9e global biome-fauna bug is
being cross-checked for this scene's mapping, not silently changed in deterministic generation.
