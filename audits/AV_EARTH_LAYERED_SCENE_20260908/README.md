# Earth layered riverbank — September 8, 2026

**Bounded implementation and local preview checks PASS.** Final351 focused tests, static checks
and all four native modes pass. Every earlier red remains unchanged below. This is not full
admission or art acceptance. Codex owns the local macOS candidate on `openai/mac`.
[Final result and source/build binding](final-results.json); [final source review](FINAL_SOURCE_REVIEW.md).

The optional `?livingvista=1` scene combines one painted rainy Earth riverbank with a separate
transparent layer of existing named residents. **The residents retain their identities, but their
current flat/simple appearance remains below Nick's approved rich painted creature target.**
Preserving the current anatomy, markings and palette is a foundation, not visual acceptance.
The scene is static: no creature movement, articulated attacks or completed battle animation.

## Canonical identity and biome mapping

Read [canonical facts](canonical-earth.json), [mapping/source review](BIOME_MAPPING.md) and
[asset provenance](asset-provenance.json). Admission binds the complete request and full ordered
19-row canonical roster at epoch0 for `CF1|g:999@90,-60|s:424242@560,170|p:133#2`, environment
`cwe1:148:50c1b7d6`, profile `temperate`, schema `cf.domain.biome-profile.v1` and digest
`bpd1-6fce883d4d70e3b6bde0fb184b416e8e`. Complete JSON comparison supplements the weak roster
fingerprint; preview truncation cannot change the scene or remove selected residents.

The static scene selects Civet and Platypus (mammals), Frog (amphibian), Persimmon (tree), and
Cranberry/Devil's Club (shrubs), using current named-art owners. Each family/form must appear in
the actual profile's corresponding fauna/flora list. It does not use the unfiltered first-three
fauna preview as proof of habitat compatibility. Earth names and original full genomes stay
unchanged. Random habitat/loco gene labels are not a named-Earth habitat authority.

Global D-9e remains unresolved and decision-gated. This presentation gate changes no generated
roster/encounter, seed, genome, save, sharing identity or global biome mapping. Broad family
compatibility does not prove these species coexist in one real geographic region. Earth's
landing-policy biome is correctly `null`; its separate visual-profile fallback is `temperate`.

## Presentation, resources and art limits

The 960×430 WebP is 130,306 bytes, SHA256
`2993cd8054a2424f20ba24040717acdb17aa9c7157500cd5b945170cd1f625d8`.
Generation prompt, original source and conversion are retained. No runtime AI call is made.
The resident layer calls the current Compendium body owners on transparent ink, preserving their
anatomy, colors and markings. Earlier bare painters were insufficient for those named corrections.
Alpha-derived static ground contact and explicit relative widths provide composition; they are
not metre-accurate world scale, terrain collision or a locomotion model.

Both layers publish atomically and fit together at uniform scale into the measured free band
between visible top chrome and the existing Biosphere panel, with 12-pixel margins. The existing
controls and their placement remain. Only while the admitted pair owns a usable band are its
**own decorative globe and labeled surface cloud container hidden**; the world stays visible.
Canonical fallback retains its decorations. The option defers to `planetturn=1` and `avpilot=1`.
Failure/default paths retain the original opaque vista; both final native fallback checks pass.
The final tracked camera impulse refreshes the Earth pair after canvas translation ends, so a
transient DOM-to-canvas offset cannot remain in the resting artwork.

Two retained RGBA canvases total 3,302,400 bytes. Rendering adds one sequential 880×880 scratch
canvas and readback buffer; decode/GPU/browser overhead is additional. The optional resident
worker is 957,316 bytes, a recorded download/parse cost rather than a completed optimization.
There is no pair cache. Workers, deadlines, epoch fences and per-scene leases control lifetime.
Failed display/lease cleanup retains backing references for retry while attempting sibling
cleanup; backing canvases shrink only after ownership is released.

The final [96-file frozen evidence build](evidence-dist-settled.json) is
`port/v2/apps/game/smoke/earth-layered-settled-evidence-dist-20260908`. The separate verified
[human local preview](../../port/playtests/20260908_EARTH_LAYERED_LOCAL_PREVIEW.md) is available;
its distributable has no diagnostic API or corner badge. No physical iPhone, Safari, PWA, resize/re-entry,
native-driver heap, Reduced/Effects Off reload, animation or all-resident art acceptance follows.

## Final verification and local preview

[Final browser-free chain](browser-free-settled.json): **351 tests in23files**, all3TypeScript
programs, art-unused/art-audit/nominal override/spec, producer refresh and root validation PASS.
The prior134 guard controls remain valid for their unchanged owners; no full battery rerun is claimed.

All four modes use the same frozen source/build and final runner SHA256
`76ca9686c913f52568b2fc3b7268085818806c6de8edcb7590abc091e5b770cf`:

| Mode | Result | Native evidence |
| --- | --- | --- |
| Phone390×844@2 | PASS | [report](native-phone-settled/review.json), [capture](native-phone-settled/earth-layers-phone.png) |
| Desktop1440×1000@1 | PASS | [report](native-desktop-settled/review.json), [capture](native-desktop-settled/earth-layers-desktop.png) |
| Exact missing Earth image | PASS | [report](native-blocked-settled/review.json); one failed image request, original opaque fallback |
| Option off | PASS | [report](native-default-settled/review.json); no optional image/worker start |

Both layered modes pass independent DOM overlap/35hit samples, actual paint/hidden-layer/globe/
cloud/legacy-placement negative controls, exact restoration, native Survey reopen/Close and exit
retirement. Passive bounded journals record actual landing translation and then zero resting-pair
y error before any Survey Close. No motion cancellation or forced resync; tolerance unchanged.
Root inspected final screenshots: all six residents and the panorama are visible, with substantial
starfield margins. Flat bodies still need the approved rich painted treatment and convincing
scale/light integration. The existing “Listen to biosphere” control also remains a plain native
light button in these captures and needs a later UI-material pass. Its placement/function was not
changed here. Native pixel/alpha/cleanup proof is not individual-species art approval.

[Preview package build](preview-build.json) and [native boot/Skip/Guide check](preview-check.json)
PASS. Local URL **http://127.0.0.1:58517/?livingvista=1**, serverPID84380/exec99962.
Source parent837db4a, dirty-local-only, publishable:false; no remote dev/production update.
ContentSHA `3fbcdcf310d03e6904c40b76e73c29a68381cb7b2908e44570743b576ac878ed`, manifestSHA
`e3b12cd3c0264adc9b7727592b72c0b860bd1a710c39e0f87d0ce1d01bc8a5cb`.
The83-row draft remains `218f02b5130fe78a6fcf76e898b6372b8761b4ef4bb4590137668920980f0793`.
Final producer `b62563276984183b933e372242e5cd68426727094f57407f9057f1ff4d8e3ba6`;
measurement `4a93479b62b032155a4825bde6425ebd430ccb286979dc69e90064bb3c7f5e12` unchanged.

## Earlier scoped verification, preserved

- [Visible-composition browser-free chain](browser-free-visible.json): **PASS — 336 tests in
  21 files**, all three TypeScript programs, art-unused, art-audit, nominal overridecheck,
  spec check, producer/refresh and root validation. Root validation retained 1,010 clean Earth
  renders, zero boot errors and the original deterministic checks.
- [Prior guard correction chain](browser-free-audit-final.json): **PASS — 134 guard controls**,
  nominal route audit, spec and root validation. The [source join](source-verification-join.json)
  records unchanged product/tests from the earlier 308-test pass, with guard files alone changed
  at that boundary. That is a scoped evidence join; it does not turn the stopped earlier
  aggregate reports green or constitute a fresh full admission battery.
- [Phone visible execution](native-phone-visible-execution.json) and
  [native report](native-phone-visible/review.json): **PASS**, fixed 390×844 at DPR2 in isolated
  system Edge. [Captured phone composition](native-phone-visible/earth-layers-phone.png).
- [Desktop visible execution](native-desktop-visible-execution.json) and
  [native report](native-desktop-visible/review.json): **FAIL**, fixed 1440×1000 at DPR1.
  It stopped at `Vista y violates measured chrome band` during the first composition check.
  The failed comparison's actual/expected composition values were not serialized before the
  assertion. The report has the ready scene/resource state, but cannot establish the exact
  geometry cause. Later blocked/default stages did not run after this failure. The separately
  corrected observer's [diagnostic PASS](native-desktop-diagnostic/review.json) retained a small
  resting offset, revealing the canvas-shake completion mechanism. [Diagnosis](THIRD_NATIVE_FAILURE.md)
  records the source correction and the unavailable exact first-failure offset.

### What the successful phone run actually established

One real Earth Land produced one durable receipt with zero RNG draws and zero damage under the
existing safe-Earth policy. Native Survey reopen/Close retained the same scene; native Survey →
Leave world exercised exit. No navigation or persistence fixture writes supplied those outcomes.

Both actual 960×430 Sprites had identical CSS bounds **x12, y192.5, width366, height163.9375**,
uniform scale0.38125 and center(195,274.46875). Visible top chrome ended at129.4375; the existing
Biosphere panel began at419.5. Independent DOM measurements found no panorama overlap, and all
**35/35 elementFromPoint samples** hit the game canvas. The actual globe and the three-child
`surface-cloud-deck` container were hidden while the pair owned the scene.

With the ticker paused, removing the background changed226,837 full-stage pixels and removing
residents changed26,095; exact restorations and the no-op changed0. Forced visible globe and
cloud controls changed275,497 and52,450 pixels and were rejected by the same acceptor. Moving
both layers back to the old H/2 placement changed456,036 pixels, reproduced the Biosphere
intersection and failed19/35 hit samples; exact restoration passed. Controls stayed unchanged.

The resident source contained42,902 nontransparent pixels (24,538 opaque and18,364 partial),
with transparent top64 rows/corners and alpha bounds x24–911 / y122–392. This proves real
transparent rendered content, not sufficient richness, convincing individual anatomy at phone
scale, or final ground-contact art acceptance. Pixi pixels alone exclude DOM overlays; the
separate DOM rectangles/hit grid are required for the visible-placement claim.

On native exit, both actual Sprites, Textures and Sources were destroyed, their canvases shrank
to1×1, the old scene scope was absent, and the old cloud container plus all three children were
destroyed. Worker/deadline/backing ownership was settled. The report recorded no Runtime errors.
These are scoped scene-resource observations, not proof of native-driver memory release.

## Preserved stopped runs and corrections

Every first red remains linked and unchanged; successful successors do not erase it.

| Retained result | Finding and bounded correction |
| --- | --- |
| [First focused chain](browser-free.json), [failure note](FIRST_CHECK_FAILURE.md): 285 PASS / 1 FAIL | Duplicate environment publication hid an existing late-cache-binding mutation control. Restore one common publication. Additional source review covered epoch fences, worker graph sealing and retryable cleanup. |
| [Second focused chain](browser-free-corrected.json), [failure note](SECOND_CHECK_FAILURE.md): 306 PASS / 2 FAIL | Art test wrongly rejected a clipped canonical Civet ear rectangle; lifecycle test expected obsolete adjacency. Correct assertions without weakening outcomes. **The art assertion edit potentially overlapped the running test read after a freeze announcement. Product source stayed unchanged; immutable test-input timing is not claimed.** |
| [Third chain](browser-free-verified.json), [failure note](THIRD_CHECK_FAILURE.md): 308 focused PASS, then parser exit2 | New direct named-body dispatch exceeded the guard's two existing consumers. Add an exact six-name dispatcher/import/four-node contract and mutation controls; no alias bypass or global route-accounting change. |
| [First guard controls](browser-free-audit.json), [failure note](FOURTH_CHECK_FAILURE.md): FAIL, 102 failed / 21 passed | Nominal/restored paths then rejected the snapshot's Object operations. Admit only four exact AST nodes inside the sealed detached-descriptor function. Preserve global restrictions and original-input/getter/prototype mutants. Source restoration was verified. |
| [First native phone](native-phone/review.json), [failure note](FIRST_NATIVE_FAILURE.md): FAIL | Observer conflated Earth's null landing-policy biome with its temperate presentation profile. New immutable observer fixes only that distinction; readiness/pixels/exit had not been reached. |
| [Second native phone](native-phone-policy/review.json), [failure note](SECOND_NATIVE_FAILURE.md): FAIL | Center-point exit target was occluded; exact hit was not retained. Pixi extraction also missed real DOM coverage of residents. Successor uses native Survey/Leave world and measured free-band composition with DOM overlap/hit controls; its owned clouds are hidden. |
| [Desktop visible report](native-desktop-visible/review.json): FAIL | First measured-band comparison failed without serializing its values. Observer-only diagnostic passed yet retained a small offset; final product refreshes Earth layout after the last landing camera impulse. [Diagnosis](THIRD_NATIVE_FAILURE.md). |

[Source review corrections](SOURCE_REVIEW_CORRECTIONS.md) and [mapping review](BIOME_MAPPING.md)
retain the implementation/guard details. The existing full-profile, navigation, U2/U3/U4 and
physical-device blockers in ROADMAP.md remain open. There is no new full
Compendium → Slice → Glass → Recovery certificate or production SceneMemory activation.

## Session and Claude review boundary

The interrupting schedule was deleted and its stored removal verified:
[automation receipt](automation-delete.json). Do not recreate it. The authorized 24-hour local
campaign ends2026-09-09T03:11:15Z; Claude review is planned Thursday September10.

This bounded layer/composition batch is complete. Next work is one complete canonical rich-painted
creature treatment and supported movement, keeping this mapping, named identity and UI placement.
Do not multiply scenes or call this completed creature art. ROADMAP.md owns resumption and the
immutable staged recovery location; previous recoveries and every original failure remain.

Required 1Password signing has no restoration evidence after `failed to fill whole buffer`.
No signing retry, unsigned commit, push, hosted job, PR, merge, deployment or release occurred
in this batch. Codex retains local work in its owned Mac worktree; Claude need not open/sync
now. ROADMAP.md and the final recovery receipt remain the next-session entry point.
