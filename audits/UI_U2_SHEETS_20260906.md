# U2 sheets and stacking — 2026-09-06

Status: implementation complete; rendered diagnostic and clean checkpoint pending.
Nick explicitly authorized “Please proceed with U2-U4.” The approved U2 proposal preserves
Settings above Training, carries the known U1 overlap honestly and keeps the accepted U1 layout.
U3 remains ordered panel work plus the review-only icon study; U4 is a local layout gate.
No Phase2 audiovisual, integrated pilot, hosted action, PR, push, merge or release authority.

## Implementation

`sheet-layout.ts` measures visible guidance, caption and dock rectangles. It publishes their
highest edge and reserves the painted toast plus 8px gaps before bounding sheets. Hidden chrome
owns no lane; toast entry reserves space before its first paint and exit retains it through fade.
`ui-sheet-style.ts` owns the resulting sheet bounds, native sticky header/Close strip, internal
scrolling, layer roles and existing 150/200ms state/entry motion. OS and saved reduced-motion
preferences suppress it. The Planetside inline offset and duplicated portrait caps are removed.
The retained320×568 large-text hint starts at367.5px; the corrected sheet floor is359.5px,
instead of the earlier390px bottom. This is a focused measured-input test, not browser PASS.

Panel IDs/data selectors and direct-child Close ownership stay intact. Refill preserves the
same Close node and returns focus only if that Close owned focus. Existing heading text is
styled in place. Compendium retains its virtual scroller; landscape retains separate safe columns.
Settings is admitted during active lessons, above the card/current target; completion-pending
still locks it. Escape invokes its existing Close owner, and queued lesson/Atlas work cannot
steal or close newly opened Settings. The step list, copy, event advances and completion owner
are unchanged. Domain, saves, receipts/CAS, RNG, protected art and audio are unchanged.

## Preparation evidence

[Preparation manifest](UI_U2_PREPARATION_20260906/manifest.json) retains five raw-hashed gzip
carriers plus retained instrument/correction logs. The initial instrument-test invocation referenced a
nonexistent config and ran no tests; the corrected invocation passed6/6 source-executing tests.
The scoped renderer covers the first ten Glass viewports at default and A++/mono, includes
ineffective earlier-fix, real Settings/Training cover and toast/biosphere overlap controls, and
labels synthetic text fixtures separately from native gameplay. Panel/Training focused suites PASS31/31. Lane+AppChrome+Guide/budget suites PASS81/81;
all three v2 TypeScript configs and root validate PASS. Evidence-mode app build PASS.
Draft remains81 bullets: its existing Planetside entry now describes shared sheets/Settings.
Ordered draft SHA418b46f0e20ab16842c1926a0af3f6f0923d4132099fcab80d258c19ef1aa3a7.
Producer authority c821b3cbc3dcac586e59fccdde02a3310d85bc2490c1e74e05f432761f93cc7c;
measurement4a93479b62b032155a4825bde6425ebd430ccb286979dc69e90064bb3c7f5e12 and numeric ceilings
unchanged. Pins move together; historical Compendium samples are not rebound or recertified.

## First clean U2 attempt: static test stopped

Signed2642ddc stopped in full Vitest:316 files passed/1 failed;3390 tests passed/1 failed/1 skipped.
The failure is inventory-narrow-reflow’s old source-spelling predicate requiring `seatPnx(def)`;
U2 passes the retained Close as a second argument. Both original wrapping mutants remain red.
The bounded correction executes the actual shipped refill/Close functions through the existing
Rolldown transformer and checks one direct heading and one direct native Close instead of the
call’s spelling. [Immutable failed checkpoint](UI_U2_LOCAL_CHECKPOINT_2642ddc_20260906/manifest.json).
No browser was launched; subsequent stages were NOT RUN. The outcome-based correction passes5/5,
including both original wrapping mutants. Its first combined command then invoked tsc without
a project and printed help (no typecheck claimed); the explicit port/v2/tsconfig.json invocation
and root validate pass. Both command logs are retained. The next source needs a fresh attempt.

## Second clean U2 attempt: native scroll instrument stopped

Signed e88ea7b passed develop static317files/3391tests/1skip and normal build. The scoped
small-phone default row proved native Settings above Training, same-point reversed-stack
rejection/restoration, the ineffective earlier sticky fix, sticky Close and real scrolling.
It then stopped before Charts because its fixed240px wheel step oscillated between scrollTop568
and808, skipping the control. This is an instrument red, not a product refusal. Two PNGs and
all raw inputs/measurements are retained in
[the immutable manifest](UI_U2_LOCAL_CHECKPOINT_e88ea7b_20260906/manifest.json). Remaining rows,
phone canaries and Slice were NOT RUN. The correction reads the real header/content/target
rectangles and sends exactly the wheel displacement needed to center the control; it rejects
controls too tall for the available reading area. Seven source-executing tests, explicit v2
TypeScript, syntax and root validate pass. Labelled font variants are restored after native
Charts refill, and the fixture waits for the prior native notice to finish fading. No browser
deadline or attempt cap changes. The fresh attempt will run normal build and scoped diagnostic
before the unchanged complete develop-static owner, then the two phone canaries and Slice;
this avoids repeating static admission before discovering an instrument fault.

## Third clean U2 attempt: reveal the scrollable Training card

Signed7197d62 normal build passed. Nine scoped rows passed: all four portrait phone sizes at
default/A++ mono plus default landscape, including real Charts toggles, native Close/focus,
sticky/stack and lower-lane fault controls. The large-text landscape row stopped before native
Skip. Its retained Training card is221px high with301px scrollHeight/219px clientHeight and
scrollTop0; the diagnostic never scrolled that owner before requesting Skip. The original
failure lacked the button rectangle, so no exact failed hit geometry is invented.
[Immutable checkpoint](UI_U2_LOCAL_CHECKPOINT_7197d62_20260906/manifest.json) retains28PNGs and
all native/evaluation evidence. Remaining rows, develop static, phone canaries and Slice NOT RUN.
The bounded correction reveals Skip through the measured native Training scroller and retains
button size, clip ancestors and hit owner before any refused dispatch. Nine source-executing
tests and root validate pass. The first new unit fixture used overflow shorthand that jsdom
did not expand to computed longhands;8passed/1failed, then explicit overflowY corrected that
fixture and9/9passed. Both logs retained. Product bytes/producer and browser bounds unchanged.

## Fourth clean U2 attempt and Nick's spacing observation

Signed ded28c1 passed normal build and9full scoped rows, then large-text landscape reached
and passed native Training Skip/Charts. It stopped on a13.5px Planetside/context overlap in
the synthetic lower-lane fixture; remaining rows, static, canaries and Slice NOT RUN.
[Immutable checkpoint](UI_U2_LOCAL_CHECKPOINT_ded28c1_20260906/manifest.json) retains37carriers,
including29PNGs. Inspection found that fixture mixed Surface biosphere with the prior Sol
hint/context. The intersection is real for that mixture; it is not a demonstrated native
Surface outcome. The successor derives actual Surface text and AppChrome keyword formatting
from hashed source, snapshots/restores hint/context attributes and exact child identities, and
retains synthetic geometry labels. The failed stress receipt remains unchanged.

Nick also flagged the Settings/Training image's spacing. The real Settings defect is an ID
specificity conflict: common `:is(#setpanel,...)` beat the later `body.panel-open .panel`,
leaving top102/max-height155 instead of safe top6/max-height339 at844×390 with44/21safeinsets.
The landscape override now carries the same ID owner. The diagnostic checks the independent
safe-height workspace and injects/restores that exact bad-anchor behavior. Planetside also
gets a separate landscape floor: a painted toast in the disjoint right column does not consume
left-sheet height; overlapping columns still reserve it. Global/portrait reservation unchanged.

Focused source owners/negative controls18/18, three TypeScript configs, root validate and
normal evidence-mode producer build PASS. The first new observer identity fixture used a deep
DOM equality matcher and hit jsdom's opaque-origin storage getter (17pass/1fail); an identity
comparison corrected the fixture. Both logs retained. Producer pins move together to
627b067cc917dc3d553a36fd85ef5d39e2612e24cac0ae4e8386fee9e7fd9fc8. Draft81/SHA418b46f0…,
measurement4a93479… and all numeric ceilings unchanged. Fresh rendered normal/large-text
review is required before advancing; neither this correction nor a clickable Close alone is
visual acceptance. No Inkscape or hosted action.

## Fifth clean U2 attempt: rendered spacing passes, native Survey stops

Signed594ece6c79269ca36787fad2fa3f11608c9fef63 passed the normal20-row diagnostic with60PNGs,
including default and A++/mono across all ten viewports, real Charts toggles, sticky Close,
Settings/Training layers, safe-height landscape workspace and all injected/restored controls.
Root and a second reader inspected both landscape Settings/Training images: readable Settings,
aligned controls, unclipped Close; the partly covered Training card and dimmed blocked dock are
consistent with the active overlay. This is scoped visual inspection, not Nick's approval or
physical device UAT. Full develop static PASS317files/3399tests/1skip.

Small-phone Glass then retained one PRODUCT RED, zero instrument failures: Survey root
[12,132,308,176] is44px high while its44px Close starts at147 and ends191,15px beyond the root.
The same centre is hit-testable; full containment is still false. Large-phone and Slice NOT RUN.
[Immutable checkpoint](UI_U2_LOCAL_CHECKPOINT_594ece6_20260906/manifest.json) retains71carriers.
Exact terminal/browser-authority read verification returns no errors; this verifies RED evidence,
not a PASS. The transition follows a live Beyond Your Charter toast, system-navigation caption
changes and Survey hiding ctx. Glass currently has no named font/two-frame settlement before
Close measurement. That leaves stale lane measurement versus genuinely exhausted toast space
unresolved. The next bounded diagnostic retains before/after complete lane/header/Close geometry
around one named settlement, preserving the same live toast; it does not wait for toast expiry,
change deadlines, clamp the44px controls or guess a new minimum that could collide elsewhere.

The product's20-view rendered evidence stays attached to594ece6; tool-only follow-up need not
repeat that unchanged diagnostic. Its new exact-source canonical checkpoint still runs the
complete develop static owner, both phone canaries and Slice/named verification, stopping first
nonzero. A read-only attempt to snapshot the prior normal dist for future U3 was refused by its
hash guard because the static owner had already rebuilt evidence mode; no snapshot was created.
U3 before images will need an explicitly rebuilt normal predecessor, not the changed dist.

The tool-only successor now records the actual before/after floor, lower lane, toast, hint,
context, Survey, header and Close geometry around the canonical font/two-frame owner. It
requires the same live Charter toast and route across that interval; stale frames, missing
geometry, changed/expired toast and out-of-order timing are rejected. Existing Close containment
still decides product outcome after settlement. Two source-executing focused tests (ten negative
receipts), all three TypeScript programs and root validate PASS. No product or producer change.
The prior RED terminal/browser read receipt and this check log are retained in the preparation
manifest. Peer review found that the later Close evaluation could outlive the measured toast.
The corrected diagnostic captures after geometry and the canonical Close outcome in one browser
task and feeds that retained outcome to the unchanged product guard. Three source-executing tests,
thirteen mutated receipt controls, all three TypeScript programs and root validate PASS. A retained
44px card/15px protruding Close remains product RED even when a later post-expiry read would pass.
Both focused check logs are retained. A clean committed canonical run is next; no prior-source
retry is authorized.

## Sixth clean attempt: settled space pressure and Nick's compact-notice choice

Signed489d780b630d09dff2dd38a0e5825e886f2ac373 passed complete develop static318files/3402tests/1skip.
Small-phone then retained one PRODUCT RED, zero instrument findings: the same44px Survey with
Close protruding15px. Before/after the named fonts/two-frame settlement are identical: lowerTop367.5,
sheetFloor171.5, toast179.5–359.5 (180px), hint367.5–444, Survey132–176, header147–263.5 (116.5px),
Close147–191. Same live Charter toast/serial/route and atomic Close geometry prove actual capacity
pressure; this is not a stale-layout diagnosis. Large-phone/Slice NOT RUN. The nine-carrier
[checkpoint](UI_U2_LOCAL_CHECKPOINT_489d780_20260906/manifest.json) preserves the complete named receipt.

Nick chose “Use compact notifications when space is tight.” The bounded correction measures the
full toast against visible overlapping portrait sheets, keeping native header/insets plus44px usable
body. Compact headlines reserve their actual rendered size; full message DOM, accessibility policy,
notification history and3600ms expiry remain. Planetside capacity uses the existing surface-chrome
start plus8 and72px total, not its floor-dependent top. Survey's unchanged subtitle/badge move into
the scrolling body below the sticky title/Close. The private detail reader follows the message span
so Tame counterpart and Feed announcement policies remain exact. No gameplay or authority change.

Three source-executing message/Survey cases PASS, including escaped content, complete text/ARIA,
original expiry, altered-message rejection/restoration and retained native Close/subtitle placement.
The existing wiring/review17checks PASS. A new fixture first had a quoting parse error and then used
an unavailable TypeScript compiler API; the existing portable Rolldown transform corrected it.
Raw failed and passing check logs are retained. Combined capacity/release85checks, all three
TypeScript programs, root validate and evidence build PASS; the12 controller cases include the
moving-top Planetside rejection/restoration. The28 producer-budget controls PASS with new producer
7e6733e00a06051525020e48ff7065f3fe26e74f545e632a6c99173b2e5e8635. Measurement and numericceilings
are unchanged. The draft retains81bullets with orderedSHAf84853aa36934c78f3bbf48574425420fb69ea5b2bd5d9a0f9605a04f4a64624.
Independent bounded source review found no actionable concern. Fresh normal rendered review and
canonical phone/Slice checks are pending. No checkpoint PASS is claimed before those finish.

## Seventh clean attempt: Close fixed; landed Capture needs joint allocation

Signed56648b27b9063132ef01c08ffa8a683d9f45dc25 passed20 normal cases/60PNGs and full develop static
319files/3410tests/1skip. The live-toast receipt now measures52.5px notice,167px Survey and55px
header; native Close is fully contained and hit-testable. Before/after named settlement agree.
Later real landed Capture stopped PRODUCT RED/zero instrument: Planetside204–299 (95px), Survey
132–196 (64px), and settled44px Tame/Scavenge/Sample targets hit the sticky H2 at their centres.
This retains the current heartbeat/focus chronology rather than changing the instrument. Large-
phone/Slice NOT RUN. The [71-carrier checkpoint](UI_U2_LOCAL_CHECKPOINT_56648b2_20260906/manifest.json)
and named terminal/browser read receipt preserve both the early correction and later failure.

The next bounded correction allocates measured Survey header/insets plus44px usable body before
capping the separate72px biosphere strip. Native scroll padding accounts for the sticky header.
The final measured strip height plus final lane floor replace the old top-position receipt for
Survey capacity, since translation alone need not trigger ResizeObserver. Compact portrait
Surface+Survey stacks yield only scene-hint paint when room remains insufficient after compacting
the notice. Original hint text returns on room/Close; Nick was informed during work. Existing
compact-notice preference, full message/AT/history/expiry and accepted U1 controls remain.

101 focused checks PASS, including17 controller cases with exact retained geometry, stale-top
rejection, joint capacity, observer stability and restoration. All three TypeScript programs,
root validate and evidence build PASS. Producer budget controls28 PASS. Peer review caught that display:none would also hide the hint from assistive technology. The final clipped one-pixel presentation explicitly excludes its nonzero rectangle from lane measurement and keeps text/ARIA intact. All17 controller cases, typechecks, validate, build and producer controls28 PASS after that correction; earlier logs and intermediate producer receipt remain retained. Draft remains81 with
orderedSHA8b9e5246cfd4c9b365cfc487a468777b6f6db7c5f90c6e5865ddba594920b475; producer becomes
547ec4ba43dc58c0d03c873dab3a3e9937f5b0daacf23da95299f05bfd490581, with measurement/ceilings unchanged.

The next exact-source run uses full develop static, both native phone canaries and Slice/named
verification, first-nonzero stop. A scoped runner limits product edits from56648b2 to the lane
controller, sheet CSS and draft copy. The20 normal Settings/standalone-lane cases remain evidence
for56648b2; repeating those unchanged scenarios is not proof of the corrected joint landed state.
That state is owned by the canonical native Capture geometry/action/focus outcomes. No U2 PASS or
U3 application is claimed before native completion.

## Eighth clean attempt: intentional hint yield reaches the preference guard

Signed U2 source `781b79cf75538b711bb770a42c189c6dc70eeb16` passed complete develop static
(319 files, 3415 tests, 1 skipped), then stopped at one small-phone `PREFERENCE_SURFACE_INERT`
result: the scene hint's deliberately clipped 1px text failed the older visible-sample requirement.
The early live-toast Close remained PASS. Native Capture completion, large-phone and Slice were
NOT RUN; the unchanged product has not yet proved the landed-action correction. The bounded tool correction preserves generic visible preference checks and verifies accessible
hint yield, actual revealed preferences, exact restoration and guidance after native Survey Close.
Its capacity decision independently measures native chrome/header/toast geometry; stale product
variables cannot authorize clipping. Focused23 tests, all three TypeScript programs and root
validate PASS. Fresh exact-source native verification remains pending.
Prior RED reports and both older unknown causes stay open; U2 completion remains pending.

The [nine-carrier checkpoint](UI_U2_LOCAL_CHECKPOINT_781b79c_20260906/manifest.json) retains the
PRODUCT RED classification exactly, including zero instrument failures. Named terminal/browser
read verification returns no errors; that validates the failure evidence, not a PASS. The old
visible-sample contract predates U2's owned hint-yield state. The upcoming tool correction must
keep missing/hidden generic samples red and exercise the real preference owner after a bounded
synchronous reveal with exact restoration. Product source and producer authority stay unchanged.

Two late peer reference-only clarifications were saved under `/private/tmp/cf-u2-late-reference-20260906`
and restored to their committed bytes during the exact-source run, then reapplied after terminal.
The isolated candidate remained unchanged and clean. No prior report or source was retried.

The source-executing wrapper tests retain the generic hidden-sample RED, actual size/font/tone
failures, empty/inaccessible/unowned/wrong-route/wrong-viewport clipping, exact restoration failure,
and native Close wiring. Peer review found that trusting product capacity variables could create
a false PASS; independent native rectangles/edges now replace them, with stale-variable controls
in both directions. Intermediate19 and22 PASS logs remain retained beside the final23 PASS log.
Reveal or cleanup failures become instrument failures before product outcomes. No product source,
producer/draft pin, deadline, toast expiry, native activation or generic preference check changed.

## Ninth clean attempt: Capture passes; shared panel scroll projection finds Charters

Signed f61aeb39cf649eebbe2edb30fe4f7b22bd2fcd29 leaves product identical to781b79c. Full static
passed320 files/3435 tests/1skip. The native small-phone run passes guidance preferences and
post-Close restoration, then all three retained Capture outcomes, including geometry/focus,
trusted native close/reopen and unchanged capture/persistence state. This resolves the retained
781 preference stop and provides the first native PASS for the landed allocation correction.
Later Charters stopped PRODUCT RED/zero instrument: its starter action centre[138,185.56] hit
`#chpanel > h3.sheet-header:Charters — Current Expedition`. Large-phone and Slice NOT RUN.
The [nine-carrier checkpoint](UI_U2_LOCAL_CHECKPOINT_f61aeb3_20260906/manifest.json) and named
terminal/browser read retain this terminal result. No full small-phone or U2 PASS is claimed.

Source inspection finds the generic `scrollControlIntoView` ignores sticky-header content
occlusion, centres in the whole scrollport and returns as soon as ancestor rectangles contain
the target. That cannot distinguish a recoverable covered position from an unreachable control.
The old finding carries point/hit only; missing header/root bounds cannot be reconstructed as
retained evidence. Next correction independently measures shared header exclusions and reports
full bounds/clips/scroll attempts, preserving full-target and exact-restoration checks. Product
also extends Survey's measured native scroll clearance to all shared sheets. This is one shared
U2 correction, with no panel reskin, action, save, timer, font or approved U1 placement change.

Shared scroll clearance and independent header-aware projection are implemented. Nineteen
controller and16 scroll-contract cases cover wrapped/refilled headers, exact owned-style filtering,
nested scroll owners, native Close/header controls, full44px body capacity and richer hit diagnosis.
Exact scroll restoration preserves raw offsets, complete style bytes/presence and declared/computed
scroll-behavior/transform evidence; originally absent style uses the earned second-removal guard.
The first focused run passed159 tests across8 files, then stopped on TS2345 for the test extractor's
guaranteed first split entry. A type-only non-null assertion leaves runtime semantics unchanged;
all three TypeScript programs and root validate subsequently PASS. Both logs remain retained.

One evidence build PASS. Draft81 orderedSHA is
b791c37bcee759754e90e688d262ec4cb24a874df1d6e353da2b41a30bff857c; current producer is
0adbee29aed0004c63d4acc0cebf84a53dabc0bca33f6000d52223fb932f8711. All current producer pins moved
together; measurement authority and numeric ceilings/history are unchanged. No fresh Compendium
certificate is claimed. Next is a clean signed-source static/phone/Slice attempt, stopping first red.

## Tenth clean attempt: scroll style absence stops the instrument

Signed c84aecde00814460eec7027b067cd7f871b46441 passed full develop static (321 files,
3453 tests,1skip). The small-phone run again passes all3 Capture outcomes, then stops
INSTRUMENT RED with zero product findings: scroll-behavior/transform declarations and computed
values restore exactly, but the style carrier is empty instead of originally absent. The old
receipt does not name its actual scroll owner. Large-phone and Slice NOT RUN.
The [nine-carrier checkpoint](UI_U2_LOCAL_CHECKPOINT_c84aecd_20260906/manifest.json) and named
terminal/browser read []/[] preserve this result. No full small-phone or U2 PASS is claimed.

The bounded tool correction clears live CSSOM before removing an originally absent style
attribute, retains declaration/computed evidence and the second removal, and names the owner
in its restoration receipt/error. Present-empty attributes and product behavior remain unchanged.
Three added cases cover required clearing, blocked clearing and wrong restored declarations.
First focused run40PASS/2FAIL exposed a fixture assuming the native cssText descriptor lives
on the immediate prototype; traversing that chain fixes the fixture. Final42 focused checks,
all3 TypeScript programs and root validate PASS. Both logs remain retained. No product build,
producer/draft re-derivation, deadline or geometry threshold change is needed for this tool-only
correction. Next is one clean signed-source static/phone/Slice attempt with no product diff fromc84.

## Eleventh clean attempt: native DOM reset replaces ineffective CSSOM clearing

Signed2dba37946ef73d8d67e3aa9cdd8771ca5d541132 passed complete develop static (321files,
3456tests,1skip), then small-phone stopped INSTRUMENT RED/zero product findings at the now-named
`#inventorysheet > section.inventory-sheet-card`. CSSOM clearing still left style="" instead of
an originally absent carrier. Laterlarge-phone/Slice NOT RUN. The
[nine-carrier checkpoint](UI_U2_LOCAL_CHECKPOINT_2dba379_20260906/manifest.json) and named
terminal/browser read []/[] validate this RED metadata, not a full small-phone/U2 PASS.

A separate isolated native six-case synthetic diagnostic reproduced the same absence failure
with the exact current helper. Replacing only CSSOM clearing with `setAttribute('style','')`
before removal passed absent, present-empty and nonempty smooth!important cases, preserving
raw scroll77 and exact declared/computed/attribute state. The failed strategy's other two cases
passed; all six observations remain retained. This is local native strategy evidence, not a
retry of the game checkpoint or proof of all product flows. The tool now adopts that exact
strategy with unchanged strict comparisons, second removal, owner diagnosis and geometry.
The source-executing controls require actual DOM reset, reject blocked reset and wrong restored
styles, and preserve empty-carrier semantics. All42 focused checks, three TypeScript programs
and root validate PASS. Product/release/producer bytes remain unchanged fromc84aecd.

## Twelfth clean attempt: cleanup passes; Objective/Planetside collision remains unqualified

Signed437a3aa08f1e5cb3c1b0b134e02f8cbe92c56204 passed complete develop static (321files,
3456tests,1skip) and the real Inventory scroll cleanup. Small-phone stopped PRODUCT RED with
zero instrument failures at charters-opener-off: Objective[148,60,310,322.88] has centre[229,191.44]
covered by the Planetside heading. The [nine-carrier checkpoint](UI_U2_LOCAL_CHECKPOINT_437a3aa_20260906/manifest.json)
and named terminal/browser read []/[] retain that result. Large-phone and Slice NOT RUN.
Only the target/hit bounds are available there; original Planetside/toast/lane values are unknown.

The [13-carrier isolated diagnostic](UI_U2_OBJECTIVE_DIAGNOSTIC_20260906/manifest.json) used actual
layout controllers and CSS in synthetic DOM. It shows22.5px overlap after two frames with only
33.5px space for a72px strip, but its shorter216.5px Objective remains centre-hittable. Missing
HTML charset corrupted static emoji, and native-event callback ordering differs from the original
synchronous Close expression. This qualified mechanism evidence cannot close the actual cause;
all outputs, source and limitations remain immutable. No product correction or rerun followed.

The bounded exact-path tool change calls the unchanged Charters Close owner and retains before,
immediate, microtask and named font/two-frame state, followed by atomic opener geometry/audit.
Full copy/classes/lane geometry, published values, scroll and live-toast identity separate transient
measurement from lasting capacity pressure. A notice expiring/replacing across settlement is
instrument-red, not a rescued product PASS. Other panel paths, timers, deadlines, floors and
product/release/producer bytes remain unchanged. Focused45 checks (three new source-executing
cases with27 corrupt-receipt controls plus42 existing), all3 TypeScript programs and root validate
PASS. Exact-source native run pending. The new audit result is retained atomically and reused at
the original opener-off site, so a later notification expiry cannot replace it with a fresh PASS.

## Preserved blockers and next checkpoint

b457a7a small-phone retained one22.5px Planetside/hint overlap, zero instrument failures;
its later native Charts Settings phase, large-phone and Slice did not run. Keep
UI_U1_LOCAL_CHECKPOINT_b457a7a_20260906/manifest.json and all earlier failed manifests immutable.
The U1 technical gate remains OPEN until the applicable native/checkpoint checks pass.
08cd97d unsolicited Milky Way and c57aaaeb portrait Runtime.evaluate timeout remain unknown;
nonrecurrence is not causal repair. Physical iPhone/Safari/PWA UAT remains open.

Codex owns U2 and the remaining authorized U3/U4 checkpoints on openai/mac. Claude need not
be opened now; leave anthropic/mac and unmerged173c806 untouched. No PR needed now. Future
openai/mac→develop integration requires separately authorized exact hosted action. Main and
production remain unchanged. No more Inkscape launches this session after Nick's crash report;
the icon study uses text-authored SVG and the isolated browser, with the first rejected policy
review retained separately from the later authorized successful rendering.
