# Item 5 — compact Centipede scale refusal

Current resolution: Nick reports Claude applied the prepared fix and actual KNOWN-pin removal verbatim in one signed commit, `f73394018950bcb24341008dd5efc6f9634a6a5e` (G). Read-only verification confirms both changes and byte-identical local family-actions.ts. Its library-arena sweep8/8 and old-line negative control are Nick-reported, not a repeated local run. The local item5 commit now contains the same crawl change. Earlier pending/signing notes below remain historical checkpoints. I5 now waits for the final integrated anthropic/mac head after Claude merges all three signed local items.

Local fix: keep the rigid trunk level during crawl; remove only that action's extra vertical bob. Its idle still breathes, all 28 walking chains keep their metachronal stance/swing schedule, and the ultimate pair stays sensory. No binding, skin, solver, contract, threshold or stage edit.

The exact Claude library bout was read and reproduced without writing its worktree. The bundled harness records and rechecks every source SHA-256; its stage, fixtures, records and rendering owners are Claude's original bytes. Explicit local overlays contain only contact and motion modules. These runs are CPU stage/skin diagnostics, not a clean integrated-source certificate, GPU/native film or CPU tier measurement.

| Run | 0.85× | 1× | 1.15× |
| --- | ---: | ---: | ---: |
| centipede-claude-02 | 1 refusal | 0 | 0 |
| centipede-local-motion-01 | 1 refusal (2 folded triangles) | 0 | 0 |
| centipede-observed-01 (rejected support-mode alternative) | 0 | 0 | 1 refusal |
| centipede-level-crawl-01 (retained fix) | 0 | 0 | 0 |

Cause isolation: the rest-support first bad frame is approach:crawl at 100 ms, with stage displacement 0.04594662409033256 body lengths. Folds are beside the front far leg. The probe initially used observed supports and failed to reproduce; that instrument mismatch is retained as probe-01. Probe-02 uses the correct rest supports; halving head/antenna rotations clears that one frame but is not a demonstrated bout fix. Probe-03 varies vertical body motion: original factor1 folds two triangles; .75/.5/0 clear that frame. Removing the extra crawl bob then clears the full three-scale bout. The failed first bundling attempt (multiple chunks/output.file mismatch) is retained as claude-01. No failure was deleted.

Validation: compact-myriapod eight tests pass; protected-input receipt rehashes every original protected input and reports unchanged. Root validate is retained separately. Existing native films/review remain in ../MOTION_ANATOMY_20260923/review.html; no new native film/CPU claim is made.

## Same-commit test pin removal / paired next steps

The requested library-arena.test.ts exists only in the read-only Claude lane, not on openai/mac. Creating an unrelated replacement test or editing that lane would misrepresent integration. This signed local fix carries centipede-remove-known.patch plus its target SHA-256. Claude must adopt the fix AND apply that exact pin removal in the SAME integration commit, then run the actual full library scale sweep once on the integrated tree. The proposed removal replaces the KNOWN filter with expect(found).toEqual([]); it weakens nothing. This cross-lane adoption remains pending. Codex holds signed results; Nick can open Claude after the batch to consume them. No GitHub write is authorized or performed.
