# Item 7 — route 53 existing candidates

Commit scope: this item7 source and audit packet are signed separately after item5 `39e00aa02e50b139fd1dba23885f4a4a15f4dce1` (G). Nick resolved Claude’s actual pin removal in signedf7339401 and requested this ordered local commit. Historical pending-signing notes below describe the prior checkpoint; final signing/source handoff is in the five-regression packet. The previously passing12 tests/typechecks/root validation are reused because their tested source is unchanged.

Full findings: [ITEM7.md](../ITEM7.md). Twelve focused tests, all three typechecks and root validation pass. Before identities are retained in item7-before.json; item7-coverage.json retains53 pending fits and zero newly qualified species. Existing habitats and weapons are unchanged. No new painting, mask, film or sheet was made; this is routing only.

Signed commit is pending the same 1Password blocker. Codex holds the reviewed source; Claude consumes candidate routing together with needsObservedFit/coverage changes after signing, then updates its coverage study. Nick separately selects any paintings later. Hash inventory: ../SHA256SUMS.json.
