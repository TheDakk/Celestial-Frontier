# Item 4 — first-use battle2 cache

Signed commit: `1e9f5920`. External schema and exact next steps: [CLAUDE_BATTLE2_MANIFEST.md](../CLAUDE_BATTLE2_MANIFEST.md). Every shipped battle2 file is pinned by path, byte length and SHA-256; verified on first use before cache/serve. Shell install excludes these assets. Original 128 MiB aggregate budget remains.

45 PWA tests, 35 release tests, all three TypeScript projects and the local evidence build pass. First PWA run had a stale comment-literal assertion, retained and fixed; no safety assertion removed. The authority printer exits2 after its successful build because the committed budget still refers to older producer bytes. `pwa-authorities-parsed.json` is a working-source receipt, not a certificate or final producer after later motion/profile edits. No native film or screen is appropriate for this worker unit test; integrated controlled-worker picker smoke is still Claude's next check.

Claude: generate apps/game/battle2-assets.json with the documented shape from its real builder; include any public MANIFEST.json that remains as an ordinary pinned file, not as the runtime discovery source. Adopt signed worker change, build, and run the controlled-worker picker once on final integrated bytes. Codex holds; Nick may relay this contract now. No hosted work authorized. Hash inventory: ../SHA256SUMS.json.
