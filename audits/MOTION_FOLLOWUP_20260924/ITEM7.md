# Item 7 — 53 candidate-template routes

Commit scope: this item7 source and audit packet are signed separately after item5 `39e00aa02e50b139fd1dba23885f4a4a15f4dce1` (G). Nick resolved Claude’s actual pin removal in signedf7339401 and requested this ordered local commit. Historical pending-signing notes below describe the prior checkpoint; final signing/source handoff is in the five-regression packet. The previously passing12 tests/typechecks/root validation are reused because their tested source is unchanged.

All 53 formerly empty candidateTemplates now reference the existing specialized templates. Mappings follow the existing missing-body target register, including its Sea Squirt → sessile-filter and Fiddler Crab → brachyuran exceptions. Profiles for those exceptions are split explicitly. The other tunicates remain colonial-filter. No template, solver, limit, weapon, habitat, part count, raster or registry stand-in was added or changed.

`needsObservedFit: true` makes the distinction durable: wiring is not a painted admission. The missing-body/coverage producer includes these explicit pending rows after candidate routing, so all 53 remain visible to Claude's coverage planning. Mismatched counts (for example, different small-crustacean leg arrays), shapes and optional organs still require observed records and normal admission; selecting a candidate cannot infer them.

Validation: item7-tests-02 passes 12 tests across profile routing and specialized anatomy; all 631 current fauna names remain exactly covered; all 53 old-empty names preserve habitat and intended weapons. Wrong-family Clam/Fiddler and missing-limb Tardigrade controls still refuse. Generic synthetic template fixtures are explicitly not painter observations. Typecheck passes all three projects. `item7-coverage.json` measures 53 pending species, zero newly qualified species and zero pending species with complete binding artifacts in this scan. No new paintings, film, CPU measurement or visual approval was created.

Retained refusal: first test invocation ran before its test edits because that edit command used the wrong cwd and failed before writing. It reported three stale fixture/empty-candidate assertions (8 passed). After correcting the cwd and updating the fixtures to exercise the new structures without inventing admission, the second run passed 12/12. Both logs retained; no unchanged retry.

Paired next steps: Codex holds the implementation and exact audit, pending signed commit after Nick renews 1Password approval. Claude can consume the signed profile and pending-fit changes together when available, then regenerate its coverage plan. Paintings remain Nick's separate choice; this change never selects the ranked 138 painting plan or commissions art. Existing review/films: ../MOTION_ANATOMY_20260923/review.html (Python first). No hosted action or PR43-green claim.
