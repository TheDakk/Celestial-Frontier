# C18 — repair the previous handoff before C15 paintings

The evidence-chain contract now expects the measured 87-bullet inventory in all three assertions. The pure weekly ordering hash lives in the existing progression domain (which already owns its Rand dependency); persistence calls that function through its existing progression dependency. This preserves the ordering formula and removes the unnecessary new persistence dependency. Both package manifests/lock are restored byte-for-byte to the pre-C11 versions. No historical certificate, sample, measurement authority, ceiling or sentinel is rebound.

The first profile attempt caught two remaining stale Glass assertions and a newly added domain file outside the exact inventory. Those failures are retained in develop.log. The helper now lives in the existing progression source inventory. Final develop-02.log: 479 test files pass, 5,080 tests pass, two expected failures, two skipped tests; the sole failure is the pre-existing current-producer-authorities I5 assertion. Compendium budget and evidence-chain tests pass. Root validate-02.log passes, including the unchanged 50-probe fingerprint. The profile stops at that red; downstream owners were not claimed green. Lock SHA-256: 744f7c5079d0892fee85d6d2af20c343955ff0d47bda9cce8368d9babd6d1402. V1 budget SHA-256: c109b5845b12862bbd8fa069b6f8e394b48d9d8568d8537f20565b63c5d907fc.

Startup checked official tool metadata under the shared lock (toolchain-check.json). REAPER suffix resolved as current by brew outdated --cask reaper (no output). Node stays at the explicitly authorized anatomy-run version 26.9.0; 26.10.0_1 is available and deferred for this continuing run. No app update, dependency upgrade, private UI access or certificate attempt occurred.

Paired next steps: Claude consumes this signed repair from the shared object store; Codex begins C15 with Jellyfish, then the four-winged flier and Sturgeon. New weekly/economy work and the I5 epoch remain parked. No hosted green or certification claim.
