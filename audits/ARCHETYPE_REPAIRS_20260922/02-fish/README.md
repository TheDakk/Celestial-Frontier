# Salmon repair — PASS

Completed signed commit9c50c190f215fab034fe7023ac0d92e72cf01497; signature verified after Nick approved key use. Signing refusals below remain historical evidence.

Run **20260922-salmon-native-02** passes on the local Codex battle2 stage:0 left/0 right rig refusals,602 live frames over10016.3ms,606 encoded frames over10.098203s. Desktop per-rig CPU p95 upper bounds: left1.5ms/right1.100000023841858ms, both within3.5ms. Each charges complete shared sampleTurn time plus that rig's performance/contact/publication, accumulated across transition ticks. Whole stage/effects/render p95:2.600000023841858ms; containment admission cost is separate. Exact frame samples and scope retained in native-02/report.json.

Static:13 action rows plus presentation allPASS; exact source-pixel rest0 changed RGBA channels and final rest PASS. Current static.json is a single changed-source run because I5 removed unused declarations from two motion files; accepted binding and master bytes did not change. The original dry-arena refusal remains in ARCHETYPE_SPRINT_20260922/02-fish/native-01.

The prior harness's dry-world selection and mass-only sizing prevented aquatic admission. Twelve exact absent source files were imported under the authorized local stage repair from read-only Claude5154c64f6f5fe0b9a75bc3f7a51e7da56b6b2e7f; source-import.json retains imported hashes. The local derivative rejects morph/guardian requests, measures full published meshes through composed poses, chooses one medium-fitting scale with deliberate presentation margin, and uses that same scale for placement/render/half-widths/cadence. Fresh final rigs validate the rebuilt plans and every film frame. Unchanged habitat bands and oversized refusal remain. Canonical world/script exact bytes and hashes are retained; the water overlay is labelled procedural over the retained painted far plate.

Stage5controls and envelope17controls PASS; app TypeScript PASS. Initial imported adapter had a three-argument skeleton call unsupported locally (stage-types.log). The explicitly unmorphed adapter now rejects jointScale and uses the actual local two-argument owner; adapter-types.log passes. No solver, family contract, gate, limit, threshold, accepted binding or S2 input was changed. Legacy discarded landmark probes are replaced only in this harness by the separately measured full painted envelope; probe and final refusals remain failures.

Review: [sheet](review-sheet.png), [film](native-02/battle-10s.webm), native-02 stills. Nick owns art acceptance. Retained-inputs.json binds original1254² master, prompts/tool receipt, explicit presence, labelled masks and admitted fit; every output hash is in manifest.json. The new runtime reports all consumed source hashes including fit manifests and exact script/world bytes. No morphology or shipped integration claim follows from this diagnostic.

Historical signing block: both standard SSH and configured interactive signing helper refused (exit128 each); signing-refusals.json retains them. At that point HEAD was the signed I5 result4cf88823fc74. Nick then approved key use; the normal signed retry succeeded as9c50c190f215, and its signature verified. Codex continues Eagle without repeating Salmon checks. Claude consumes signed source/evidence under Nick's integration direction; Nick need not open Claude now. No hosted attempt, PR, merge, release or deployment. I5 local-only certification remains recorded separately; no PR43 green claim.
