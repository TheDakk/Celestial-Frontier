# Chimpanzee fit-02: individual red diagnoses

Executed `node audits/ARCHETYPE_REPAIRS_20260922/07-primate/contact-diagnosis-02.mjs` once. Replayed only the three retained first failures from `static-02.json`, with exact error-string equality. This was not a full static battery or native film. All 52 bundled source hashes remained unchanged; 51 match the static source receipt exactly and one belongs to this new entry. Record, binding, and original static-report bytes remained unchanged.

Real GSAP/performance owners reconstruct each attempted pose; the exact presentation schedule is asserted. Contact tracing consists only of three invertible observational insertions in a temporary bundle. The sole ARAP invocation is the retained dodge failure; its attempted resolved pose and every reported ARAP statistic match static-02 exactly. No production source, gate, threshold, accepted binding, or source input changed.

| Row and first failed time | Owner and cause |
| --- | --- |
| dodge, 37.333333333333336 ms | Contact completes all four support passes without compression. `solveArapSkin` then refuses eight inverted triangles, all owned by `near-leg-upper`. Their source-coordinate bounding box is x=195…273, y=587…646.5. Minimum signed area ratio is −0.03548368563274386 after 128 orientation passes. The output sentinel remains untouched: failed geometry was not published. |
| faint, 385.6666666666667 ms | The far leg drives 36.46389261464945 px of compression in pass 0, followed by 2.6233900518121915 px in the first painted-support correction. Total 39.08728266646164 px exceeds the unchanged 38.06856971308484 px cap. The near leg requests no compression in these passes. ARAP is never invoked. |
| presentation, 9883.333333333334 ms; dodge elapsed 80.9774124622345 ms | The near leg requests 38.42798277024843 px of compression immediately in pass 0, exceeding the same cap. Its hip-to-target distance is 647.6976551214974 px against 613.848805099595 px maximum reach. The far leg asks for only 8.394536655309407 px, so it is not the dominant constraint. ARAP is never invoked. |

The dodge's rejected triangles lie in the proximal near-leg transition. Their retained vertices show mixed near-knee/root/far-knee weights, with some pure-root vertices. The JSON includes each exact triangle, source/target/private rejected coordinates, weights, and pin flags. This localizes the failure; it does not establish that changing those weights or the solver would be permitted or correct.

As in the original diagnostic, a static contact refusal's stored `firstRefusal.pose` is stale because it updates only after contact succeeds. The actual attempted poses here are captured before contact resolution. For dodge's ARAP failure, the stored pose is the actual attempted resolved pose, and exact equality is asserted.

The changed-knee candidate therefore remains RED in all three listed rows. No further fit or execution is proposed by this diagnostic. Parent owns the corrected painting's separate review and item closure; every prior candidate and refusal remains retained.
