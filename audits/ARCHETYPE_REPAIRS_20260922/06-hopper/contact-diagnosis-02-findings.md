# Tree Frog fit-02: six exact first contact refusals

Executed `node audits/ARCHETYPE_REPAIRS_20260922/06-hopper/contact-diagnosis-02.mjs` once. This was a browser-free diagnostic of six already-failed timestamps, with no skin solve, static battery, native film, timing certificate, or acceptance claim. Every refusal's complete first line equals the retained `static-02.json` first line. All 40 bundled source hashes remained unchanged; 39 also match the static run's receipt. Record, binding, authoring, presence, and subject inputs remained unchanged.

The real GSAP player and performance owner reconstruct the exact attempted pose, then call the real contact owner. The temporary bundle adds three observational trace statements only; removing those statements restores the exact original source. The receipt retains their bytes. No production file, equation, contract, limit, or threshold changed.

`static-02.json`'s contact-failure `firstRefusal.pose` is the **last successful painted pose**, because that field updates inside `rig.applyPose` and contact refuses first. Use this diagnostic's `attemptedPose`, captured before `contact.resolve`, to inspect the actual rejected sample. The original report remains unchanged.

All distances below are source pixels (normalized square-source measurements multiplied by 1254).

| Action / timestamp ms | Exact first conflict |
| --- | --- |
| cast / 76.25 | Pass 0 far foreleg needs 35.539459143179904 px downward accommodation, exceeding the unchanged 33.682375213158586 px compression cap. |
| dodge / 25.666666666666668 | Pass 0 far foreleg needs 36.44778596855763 px accommodation, exceeding the same cap. |
| victory / 20 | Pass 0 far foreleg needs 41.96498234972293 px accommodation, exceeding the same cap. |
| hit / 161.25 | Pass 0 near hind root-to-target distance is 241.34866257602172 px, below its 241.61870444364357 px inner reach. The longer 441.3184791055094 px lower segment cannot fold into the shorter 199.69977466186583 px upper segment. |
| melee:bite / 118.22222222222221 | All four support passes use zero compression; the far forepaw's final counterrotation is 47.94402136281136 degrees, outside the unchanged −45 to +45 degree joint limit. |
| faint / 186.33333333333334 | All four support passes use zero compression; the far forepaw's final counterrotation is 46.96417433402339 degrees, outside that same limit. |

The far foreleg's upper/lower lengths are 128.24975633505113 / 123.98790263570079 px, with 252.2376589707519 px maximum reach. Its observed painted support is offset −3 / −20 px from the ankle and belongs entirely to the ankle owner. The compression refusals occur before the first endpoint solve, so neither support correction nor ARAP causes those three first refusals. The near-hind inner-reach refusal also precedes support correction. The bite and faint angle refusals occur after the exact established initial solve and three support corrections, before any optional rigid-support fallback.

The near-hind target is also inside its inner radius in dodge's initial trace, but the far-fore compression cap fires first. This is a secondary incompatibility, not a claim that a later solve was executed.

These results support a corrected source painting with a less extreme near-hind fold and visibly relaxed forearm bends, followed by fresh manual landmarks. They do not justify moving landmarks off the observed image or relaxing contact policy. The retained first master, presence, authoring, binding, static reds, and limits remain intact. Parent owns the one corrected repaint and subsequent admission; this diagnostic performs no further execution.
