# Override parser repair

The retained `develop-profile-admission.log` stopped at the relative import from
`painter-topology.ts` to `tools/creature-animation/repeated-anatomy.mjs`. Discovery
previously stopped at the art directory. The new graph follows static relative
imports and named re-exports through their actual JavaScript/TypeScript sources
inside v2, refuses missing/ambiguous targets and symlinks/boundary escapes, and
passes every unsealed dependency through the existing AST route/mutation audit.
The three reached modules are repeated-anatomy, skeleton-pose and kinematics.
The existing hashed verbatim inputs and exact bare dependencies remain sealed.

`skeleton-pose.mjs` uses fresh `Object.create(null)` dictionaries. The parser now
recognizes only that one-argument literal-null form; arbitrary prototypes,
descriptors and method aliases remain refused. No dependency source changed.

The first focused run reached a second stale instrument assumption: routing now
lives in private `paintOverrideCanvas`, behind the public topology replay wrapper.
The checker now audits that actual routing body and its stable binding, seals
the public wrapper and synchronous `paintWithTopology` helper, and unwraps only
the exact zero-argument callback supplied with the same ink and observer. The
existing selector, precedence, kingdom, painter, fit and returned-canvas checks
remain active. This is a parser migration, not a product or threshold change.

Retained attempts:

- `override-parser-targeted.log`: refused on one incorrect fixture cycle path
  and the pre-existing private-router mismatch. Both were diagnosed and changed.
- `override-parser-targeted-repaired.log`: 19 passed, 3 failed including the
  aggregate parent test; two mutant anchors each matched twice. No gate failure
  was hidden: both mutations stopped before execution. Full unique contexts fixed
  the anchors.
- `override-parser-targeted-final.log`: 22 passed, 0 failed, 0 skipped. Isolated
  copies exercise source closure, missing/ambiguous/malformed/escaped inputs,
  symlink refusal, transitive route mutation, null-prototype construction,
  duplicate owners, private reassignment, ordinary/capture return substitution,
  skipped/wrong painter callbacks, ink/genome identity and precedence. Restoring
  the exact source recovers identical real gate output.
- `overridecheck-repaired.log`: actual local source, exit 0; 42 art + 3 transitive
  modules, 1,014 live route keys, 0 dead, 1,010/1,010 Earth species covered.

No browser, full profile or production control battery was run by this repair.
The existing production controls were migrated to the same current routing
anchors; static inspection found all 48 literal router anchors unique. Control
CC now refuses the discovered duplicate owner rather than pretending an
inside-v2 source is outside discovery. Two read-only helper inspections used the
wrong working directory and failed path/module resolution; neither changed
source nor ran a gate. All measured source/log hashes are in
`override-parser-repair.json`.

Next: Codex runs the changed-source develop profile under the parent I5 owner,
then signs the preparation and performs the authorized clean-source certificate.
Claude consumes the signed packet under Nick's integration direction; opening
Claude is unnecessary for this local repair. No PR #43 certification is implied.
