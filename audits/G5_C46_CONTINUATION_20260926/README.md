# C46 continuation — 2026-09-26

Signed no-ff merge `375935b6` integrates Claude through`0c208c4c`, including the stage finish provider and four candidate derived-label packets. The CLAUDE SESSION HANDOFF block remains in ROADMAP.md. No conflict markers were present before signing. Startup checked official metadata under the shared lock; all tools current, REAPER suffix resolved through an empty outdated result. No installation/version/pin changes. Start free disk227GiB.

## Decision boundary

D26 is still **open (Nick)** in the absolute read-only decisions mailbox. This run asked for the explicit answer and has not received it. Alpha eligibility stays===255; no four-pixel erosion, source alpha or conservation change. The conditional five-crab rerun and1254 Cougar proof have not run, and the old five-crab proof is not superseded without that approved change. No Cougar finished original exists to show from this batch.

## Source-label review

`audits/G5_C46_LABEL_REVIEW_20260926` independently reproduces all four maps, refuses changed atlas/binding/dimensions/provenance/forged matching receipts, and binds supplementary rows for Civet/Eel/Rat/Salamander. The registry now has38 usable source-label entries. The source-library builder copied all38 exact masters/maps; no source pin or original fit evidence changed. All FOUR masters are fully opaque and differ from keyed card alpha; labels do not establish card eligibility or anatomical admission. Stage projection preserves the existing atlas alpha.

## Phone publication preparation

The old native crab receipts are proof identities, not the actual application identities: they use short creature IDs instead of Compendium visual keys and a raw authoring-manifest hash instead of the runtime model identity. They cannot be relabelled as runtime output. This batch prepares a new three-crab production run under the existing alpha255 policy, using actual canonical genome/visual key/seed, runtime finishSourceV1 and creatureFinishModelHashV1, exact cached model/source pins and the real worker adapter. This is separate from D26's conditional five-crab regression proof. No prior artifact identity is rewritten.

Preparation exposed two independent runtime mismatches: the engine's512-character individual ID bound rejected canonical~740-character visual keys despite already allowing2048-character visual keys; the adapter rejected the worker's Uint8ClampedArray output. The bounded fixes preserve exact identity/hash values and byte content, with oversized and malformed controls; no D26 or conservation change. Native execution and publication evidence follow on a clean signed checkpoint.

## Fit and G2 continuation

`audits/G1_TAIL_REPAIR_20260926` owns the automatic tail-contour candidate and controls; it is not adopted as a pre-admission default or hand fix. `audits/G2_STRIDE_PILOT_20260926` retains six fresh original paintings using planted strides and readable tail roots, exact prompt receipts, one review sheet and the unchanged G1 evaluation. Five author refusals and one Tapir static red remain; no new painting is admitted. D24 old gate stands and D25 shopping remains off.

Root validate passes with1010 renders, zero boot errors and the unchanged50-probe fingerprint (`root-validate.log`). Final integrated profile, native results, cleanup and own-branch Git handoff will be recorded before batch completion.
