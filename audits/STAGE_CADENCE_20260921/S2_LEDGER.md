# Solver-mode S2

Signed producer0072cbdb; one sweep PASS. All six subject receipts exactly equal the prior ledger; five crab rows also equal R2c′. Civet max0.165256373px; exact rest and presentation pass. Raw evidence in s2/. Native cadence is the next, separately measured scope.

## Native shared-path halt

**S2_STOP** after Freshwater Crab refused at154ms, stageDisplacement0.07165014577259474 body lengths. Crab and Coconut pass; remaining6 native subjects unrun, bear fit unstarted. Solver-mode bit identity and exact rest remain PASS. See [native rows](NATIVE_ROWS.md), `native-summary.json`, and `native/freshwater-crab/report.json`. No retry.
