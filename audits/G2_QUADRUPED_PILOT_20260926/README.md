# G2 quadruped pilot — 2026-09-26

Twenty original generated masters, exact compiler prompts and canonical species identities are retained in the numbered packets. No new hand authoring, landmarks, labels or rig admission was produced. master-measurements.json binds the original PNG bytes and the unchanged IC4 observations. review-sheet.png is the one review sheet.

The strict verifier reports 10 ADMIT / 10 REFUSE. These are unqualified diagnostic results, not anatomy acceptance: Coyote assigns a fore paw to an ear (1117,199). Correct counts alone are insufficient. Generated coats, framing margins and partial-alpha edges also need review; originals were not repaired. The preliminary measurement-progress.log incorrectly counted a tail appendage as a leg; the final measurement filters only fore/hind paw slots.

Twenty masters total 27,804,156 bytes. The 120 derived masks total 8,684,365 bytes. Each candidate manifest binds its source PNG, generator, seed and six patterns. White RGB, source-alpha ceiling, no outside-alpha paint and nonempty coverage are checked after PNG encoding. Plain needs no mask; iridescence belongs to the emissive path. These are cf.derived-marking-masks/v1 candidates: admission still requires a G1 record/recipe binding. No anatomical material exclusion is claimed. Existing intake and finish-conservation gates remain unchanged.

The compiler CLI is `node port/v2/tools/painted-creature/compile-master-prompt.mjs --library "Coyote" <new-directory>`. It uses the actual ArtKit compiler and frozen Discovery Atlas reference. Removed one obsolete nonexistent readArtKitBlocksV4 re-export that prevented that compiler from building. The library adapter creates no authoring file.

`node --test port/v2/tools/painted-creature/derive-marking-masks.test.mjs`: 3 PASS, including alpha, RGB, empty-mask and determinism controls. An initial seed test exposed indistinguishable small seed phases; integer mixing repaired it before the final outputs. Full integrated application profile belongs to the following gameplay recovery batch. Free space at pilot completion:233GiB.

Nick's latest request prioritizes completing the preserved gameplay next. G5 remains outstanding; S4 stays parked until G1 passes.
