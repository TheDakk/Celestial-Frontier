# Codex adopts the operating model — September 25, 2026

The user explicitly adopted Claude's model. Codex read the model, incoming mailbox and decisions queue by absolute read-only paths. Verified owner: OpenAI/Codex, macOS, `/Users/nick/Projects/celestial-frontier-openai-mac`, `openai/mac`. `verification.json` binds the source documents and effective Git configuration.

## Verified signing and transport

Repository config selects `/Users/nick/.local/bin/git-ssh-sign-cf` and `/Users/nick/.ssh/cf_agents_signing.pub`; origin is `https://github.com/TheDakk/Celestial-Frontier.git`. The requested signed throwaway commit-tree check printed **G**. The original unquoted `%G?` encountered zsh wildcard parsing; quoting it fixed the shell command, with no signing refusal. No branch or tracked tree changed in this probe. No key, credential, wrapper, user configuration or remote was edited. GitHub signing-key registration and gh login are Nick-reported; this batch verifies local signature status and configured transport, not a new hosted write.

## Mailbox disposition

- **C1 DONE with finding:** signed instrument cfd6e7fb and outcome b5ed59a7. First v2 calibration on product b4f191c3 stopped at painted thumbnail ownership observability; no successful sample, active budget or certificate. V1 and limits untouched; no retry. The mailbox asks Claude for truthful painted ownership/resource diagnostics before a changed-source epoch.
- **C2 IN PROGRESS — standing order now authorized:** the batch-end mailbox changed, and signed Claude decision2a0eacee records Nick's D1 yes. The ranked order and one sheet per ten are accepted; no new painting has been delivered in this model-adoption batch. No per-paint approval question is needed.
- **C3 BLOCKED:** measured pair cost is retained; no skin-only cost or phone acceptance is inferred. C3_PHONE_BUDGET.md records the decision and a bounded profiling request to Claude.
- **C4 DONE with decision:** current admission keeps master-byte hashing. A future pin-only overload requires independent build-owned authority and exact negative controls; an unverified hash string is rejected. C4_MASTER_ADMISSION.md gives the proposed trust boundary and asks Claude for its build pin contract.
- **C5 DONE:** mailboxes at run start and batch end; each lane writes only its own mailbox. Signed commits are visible immediately in the shared store, so no push/relay waiting. Signed --no-ff lane merges and own-branch pushes are authorized when the required local battery is green. PR/label/hosted/develop/main/release/deploy gates remain explicit. The later signed D5 decision records conditional once-daily label authority; it does not bypass the whole local gate list, exact budget record or any red evidence. No label is eligible now. C6 acknowledges D5 and D6; C7 confirms Codex ownership of A7/A8 art, A9 instrument/budget/skin, A10 economy implementation subject to N2, and the N2 policy proposal. These owner confirmations do not claim the deliverables complete.

No merge or push is claimed in this model-adoption batch: the actual integrated I5 gate remains red and no green combined local battery has been established. This is not a request to wait for Claude's push. The next cross-lane work is sent directly in `audits/MAILBOX/TO_CLAUDE.md`; Nick need not relay it. No unchanged browser or test battery was repeated for this documentation-only batch. Prior signed v2 controls/root validation remain linked in the I5 packet.

Current references AGENTS.md, CLAUDE.md and PARALLEL_GIT_PROTOCOL.md now state the adopted overrides; the old live handoff is archived verbatim. No v1 budget/sample, code, accepted binding, solver or S2 input changed. .DS_Store remains untouched. Actions mode UNFROZEN; no hosted authority or runner time used.
