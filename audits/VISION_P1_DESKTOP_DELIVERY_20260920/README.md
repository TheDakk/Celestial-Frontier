# P1 canonical painted-desktop delivery

Authority: Nick, read-only anthropic PROGRAM.md§6, September20. Full-film rig p95≤3.5ms
on desktop; boundary24/interior56 canonical. Phone/painter strict2ms unchanged. No mask,
landmark, binding, solver, contact limit, source pixel or IC-3 writer change.

Producer pending signature. native-cpu-policy tests cover both sides of3.5ms, unchanged2ms,
missing CPU, and geometry/rest/contact/continuity/capture mutants. Per-clip2ms flags remain
visible in raw reports and cannot substitute for the real full-film desktop measurement.

Coconut fit04: reassess retained complete root-guard-native-01 (split guard, exact rest),
no new capture. Crab fit01, Freshwater fit03, Mud fit01, Vent fit01: one new full-row capture
each under signed producer. Final sheets compare each master with accepted Civet and its
painter crab, with native pinch/faint frames and truthful CPU/geometry result labels.
Every run is retained, including a CPU leaf red if one occurs; no unchanged retry.

frozen-inputs.json hashes canonical fits, masters and the complete shared writer directory.
run-delivery.mjs checks those hashes before and after each run. IC-3 frozen on completion;
no new intake/painting/P2/roster until IC-4 passes. No fetch/push/PR/merge/release/deploy.
Node26.9.0 continuous-session receipt reused; browser commands hold the toolchain lock.
