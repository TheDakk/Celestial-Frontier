# Signed lane integration and source-graph reconciliation

Codex/macOS, openai/mac. Merge parent 0225d8e9621e95a1d5a1f66d72f31f55f4719087; Claude input 4aeb8f15f603dcc2f539de64f23bc36ce3954714 (G). The --no-ff merge incorporates Claude's painted ownership diagnostics, active-play Weekly Charters, source-mapped phone CPU profile and earlier signed game work, including main.ts changes authored in Claude's lane. No main.ts hunk authored here.

The incoming 3f0faea6 checker replaced recursive dependency discovery with a single allowed import escape. The retained source-graph test caught that regression. Reconciled by retaining Codex's checker and control implementation, with only the two SHA-256 seals advanced to Claude's actual generated HD-art bytes. The current product passes the original stronger audit. No gate/limit/threshold or accepted binding changed. The diff and before/after hashes are retained.

Validation: 22 source-graph tests pass, including 14 mutation subtests. The restored full override control has 134 PASS rows, exit 0. Root validate passes with zero boot errors and the identical 50-probe fingerprint. Painted ownership: 7 tests pass. Final develop profile: 479 test files pass, 5076 tests pass, 2 expected failures and 2 skipped; one current-producer-authorities test fails on the historical v1 producer mismatch. The profile stopped there; no later profile steps or browser attempt ran. This is not a green full battery, certification, or permission to push/label.

An intermediate profile launch was interrupted before accepting results because it overlapped the mutation control; that incomplete log is retained and is not evidence. A wrong-root targeted npx invocation was interrupted; the corrected v2 invocation supplies the 7-test result. No unchanged failed calibration was retried.

V1 budget and samples remain byte-identical. C8 observation work, C10 release inventory, C11 conquest and C12 cost optimization follow in separate completed-item commits. Claude consumes signed local history directly; Codex owns the remaining instrument changes. Nick need not relay this packet or open another app. No push, PR, hosted label, release or deployment occurred.
