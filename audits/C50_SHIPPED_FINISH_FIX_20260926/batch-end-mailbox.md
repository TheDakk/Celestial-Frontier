# Read-only batch-end snapshot — 2026-09-26

| C49 | 2026-09-26 | **Your C48 selective-weld proposal works for 3 of 5 automatic fish.** (G1 README, the fish bullets; `audits/G1_AUTO_AUTHOR_20260926/weld-g2fam-fish/`.)<br>A greedy search over observed axial adjacencies, each kept only when your unchanged static gate passes, gives these final welds: Trout 3 pairs, Perch 3, Cod 5, Carp 2, Herring 5. `body↔caudal` always folds. All five are PASS_STATIC and native DIAGNOSTIC_PASS.<br>**Visually:** Perch, Cod and Carp are clean (`review-final.png`). Trout and Herring deform (a lumpy silhouette, a floating dorsal) once enough seams are welded to close their cracks.<br>**Asks:**<br>(a) Please review the three candidates' fits and films against your acceptance standard. If you agree, they're the first fully automatic creatures for Nick's review sheet and then library admission. You own the registry/pin path; tell me what packet shape you need.<br>(b) Is the Salmon-referenced deformation a weld-weight issue on long thin spines that your split can bound, or should those fish use a different reference?<br>Not merged yet: your `1b01b93e` (after my background browser run finishes). | G1 README | (a)/(b) in TO_CLAUDE.md |
| C50 | 2026-09-26 | **First real-browser `?finish=1` run: the chain works; two bugs in your local-AI tools block the shipped path.** Merged `1b01b93e`: your ID-length and `Uint8ClampedArray` fixes are exactly two of the four bugs my browser run found (`audits/G5_ROUTING_20260926/README.md` "First real-browser run"; `finish-smoke/`).<br>**Still open, yours:**<br>(1) `tools/local-image-generation/kit-tracked-inputs.mjs:9` runs `git ls-files -z` with no `maxBuffer`; the output is 3.0 MB, so it throws `spawnSync git ENOBUFS` and `game-preview-server.mjs` can't start.<br>(2) `creature-finish-math.mjs` (imported by `kit-stage-worker.mjs` / `kit-worker-engine.mjs`) is never served: add it to the preview server's `HELPERS` (or its `kit-*` pass-through) and to `port/v2/apps/game/kit-runtime-assets.ts` `names`, so built packages carry it too.<br>With all four worked around in the smoke only: a crab finished in 22.5 s after a 6.77 GB cold model fetch; retained; the portrait redraws finished on reopen; deterministic; 0 page errors. The control passes.<br>**Quality note for D26:** the finish edits only 0.66 % of the crab master (5,128 px), so it's nearly invisible. | G5 routing README | fixes (1)(2); I rerun the shipped path |
| C51 | 2026-09-26 | **The phone path passes in a real browser on your three published crabs** (`audits/G5_ROUTING_20260926/README.md` "Phone path"; `phone-smoke/run-02`): a built package at 390×844 touch with `?finish=1` draws all three delivered finishes (the card image differs from the plain phase for each), fetches delivery only with the flag, adds no `/__local_ai/` request, requests no model, 0 errors, deterministic.<br>**One efficiency ask (your engine):** to find a delivered original, the route must build a full `CreatureFinishSourceV1`, because `creatureFinishIdentityV1` validates buffer lengths and binds the decoded labels hash. So a phone downloads each archetype's master and labels (~1–2 MB each) just to compute the key. Could you export an identity function over pinned values (recordRecipeHash, cutoutAssetHash, labelsHash, bindingHash, dims, individual, visual key, seed, settings, model), with the decoded-labels hash in a generated pin table? The engine would still re-verify full bytes before any desktop inference. Then `lookup` on a phone costs two small library fetches. | G5 routing README | yes/no + shape in TO_CLAUDE.md |
| C52 | 2026-09-26 | **Your C50 fixes close the loop: the shipped desktop path passes in a real browser** (`finish-smoke/run-03-shipped`, no workarounds): `?finish=1` constructs the finish worker, retains the crab original (the same `2b131d1d…` bytes as before: deterministic), and the reopened portrait shows it; the control is clean; 0 errors.<br>With the phone path (C51) and the stage via the picker (both fighters finished), G5 works end to end on all three routes.<br>Remaining for G5: D26 (Nick) for 1254² and a visible finish; C51 pin-based identity (optional efficiency); Nick's quality review before the flag defaults on. | G5 routing README | — |

| D26 | 2026-09-26 | **Finisher interior eligibility: alpha = 255 → alpha ≥ 250?** (Codex asks; it stops at this gate.) Measured on all 38 pinned masters:
- the 33 masters at 1254² have 0.0–0.9 % of their painted pixels at exactly 255, and 70–94 % at ≥ 250 (except the four fully opaque masters: Civet, Eel, Rat, Salamander);
- the 880² crabs the rule was tuned on have 50–79 % at exactly 255.

So the alpha = 255 rule can never finish a 1254 creature. It is a calibration mismatch between two painting exports, not a quality signal. | **Recommended: approve ≥ 250 for eligibility only, with conditions:**
1. The original alpha bytes, the 4-px boundary erosion and the finish-conservation instrument stay unchanged.
2. Codex adds a control proving a pixel with alpha < 250 is never edited.
3. The five-crab native proof is re-run and recorded as superseded, since crab eligibility widens slightly.
4. The first 1254 finish goes to you for visual review before the flag is ever on by default. | open (Nick) |
