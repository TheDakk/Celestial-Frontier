# C50 shipped finisher transport fix — 2026-09-26

Signed no-ff merge f85cac18 integrates Claude through bb770d53. Seven incoming commits verified G; no conflict markers; Claude handoff preserved verbatim. Startup official tools current; REAPER suffix resolved by empty scoped outdated result; no updates. Disk224GiB.

The actual repository reproduced ENOBUFS in the old tracked-input helper (`before.json`). Its git ls-files now has a bounded16MiB maxBuffer; exact required-path membership is unchanged, including omission controls. `after.json` records the complete actual listing and admission.

`game-preview-server.mjs` explicitly serves creature-finish-math.mjs in HELPERS; `kit-runtime-assets.ts` includes it in both development serving and built emission. No broad route wildcard or traversal permission changed. Existing localhost tests now require200, JavaScript MIME and exact helper bytes. The actual build hook emits identical bytes; omitted/changed-helper controls fail, then genuine restoration passes. Initial sandbox listen EPERM is retained separately; unchanged tests pass with localhost permission. No browser/model retry here: Claude owns the shipped-path smoke.

Focused controls: 14 Node checks and one build-plugin Vitest check pass. Integrated browser-free develop profile: **5,646 pass, I5 only fail**, two expected failures/two skips across 563 files. Root/app/worker TypeScript, art audit, override check, spec check and override controls all pass. Root validation: 1,010 renders, zero boot errors, unchanged 50-probe fingerprint. Logs and tested implementation hashes are retained. The compiler prompt edit was uncommitted but stable during this battery; no exact-head browser certificate is claimed. The actual built helper also matches its source byte-for-byte (`built-helper.json`).

Read-only end mailbox C51/C52 reports phone browser delivery and the shipped desktop path PASS, without the earlier smoke workarounds. The same crab finish bytes were retained and redrawn; Claude reports zero errors. This is cross-lane evidence, not a new Codex browser run. Nick's visual review and D26 remain outstanding; alpha255 is unchanged and no old five-crab proof is superseded.

C49 review, nine G2 originals and the rejected contact-projection experiment have their own packets. Pruned 26,871,161 bytes of ignored rejected-fit scratch only, after preserving file hashes, inputs and reports (`cleanup.json`). Positive review fits and every original remain. Three permanent worktrees, no stashes, two preview directories, approximately 222 GiB free. No PR/label/hosted/develop/main/release/deploy.
