# Retained execution and refusal ledger

All output directories are fresh; raw prior reports remain unchanged. Signed M0 preceded instrument/baseline work; signed M2 preceded product motion fixes. Browser-owning commands ran outside the sandbox using the existing exact browser launcher and toolchain lock. Node 26.9.0 remained unchanged. Startup metadata and scoped REAPER outdated results showed no eligible authoring update.

## Instrument corrections

The original audit adapter sampled pure timelines and used `record.habitat?.realm`, which is not the compiled runtime realm; it also omitted the static owner's `travel: stage` melee context. This produced 27 action-local contact failures in final-audit although painted static/native evidence passed. Corrected context-audit uses `card.realm` and the existing melee travel owner: only Bear gallop and tail retain contact failures. No gate or solver tolerance changed. Original baseline and both earlier after reports are retained, not retroactively made green.

The ground-only six-rig projection still establishes schedules, but Eagle/Bat's actual aerial realm bypasses those ground contacts. Corrected rows explicitly fail their walk/crawl contact proof. The full-support criterion no longer applies to Bat flight, where ground wrist contact is inapplicable; missing wrist support remains a crawl failure. Added envelope, end-rest and root-relative strike criteria exposed limits that a root-lunge-only check missed. Final context report has 1,977 rows, so its failure count cannot be directly compared as a success percentage to the 1,875-row baseline.

Raw idle curves can begin in a secondary-lag phase, and a universal head-height criterion also flags intentional flight/hop root movement. Those instrument readings remain visible and diagnosed, distinct from the unchanged painted exact-rest gate. World-path following refuses because a local action sampler has neither accumulated world translation nor preceding head history. No new certificate was attempted.

## Failed attempts and process mistakes

- Known Crab and override defects reproduced in retained pre-fix logs.
- `contact-defects-after-01.log`: edit match failed before the run; this was an accidental unchanged retry. Correct scoped replacement produced passing after-02 evidence.
- `after-wave-01`: Salmon tail phase failed due to double material lag; explicit phase ownership fixed it in after-wave-02.
- `builder-controls-01.log`: new builder referenced an unexported `P`; a local typed constructor corrected it. The first correction was issued from the wrong working directory; builder-controls-02 accidentally repeated unchanged source. Builder-controls-03 passes.
- `portable-controls-01.log`: a synchronous throw did not match the async assertion contract. The adapter now returns an async rejection. `portable-controls-02.log`: Windows absolute-path detection also treated a POSIX root as Windows; explicit drive/UNC detection fixed it. Controls-03 passed three groups; new quadruped reseal coverage in controls-04 passes four.
- `release-content-test.log`: legitimate added bulletin changed 85 to 86 entries. The first repair command used the wrong relative path from port/v2, so release-content-test-02 accidentally repeated the original failure. Test-03 updated the positive inventory but exposed the missing-entry negative control's old expected count; test-04 updates it from 84 to 85 and all 35 tests pass. Required motion copy is explicitly asserted; no check was removed.
- Several read-only searches used a nonexistent guessed filename or unmatched glob and were corrected to discovered paths. A wrong-directory typecheck redirection never launched the compiler. No source or evidence was lost.
- A staged whitespace check rejected a raw Vitest log's extra terminal blank line during M2 assembly. The raw bytes were preserved; this was not a signing refusal.
- Native refusal reports are exhaustively enumerated in review-data.json. Shorter ready/command pauses and Frog's existing observed-support option were changed inputs, not identical retries. The original Centipede fold remains unresolved for its original timing, even though the compact after program passes.

## Checks retained

Five instrument control groups, 18 focused contact tests, 29 source-closure tests plus override mutation controls, four portable-writer controls, three builder controls, 86 motion/contact regression tests, 35 guide/release tests, all three TypeScript projects, root validate, eight affected painted static reports and six unchanged S2 sentinels. Exact logs, source hashes and native reports remain in this packet. No unchanged full battery was intentionally rerun; the accidental retries above are process defects, explicitly retained.

## Review rendering limitation

The in-app browser rejected the file:// review URL under its URL security policy. No alternate surface, localhost proxy or raw-CDP workaround was attempted. The page was inspected structurally (18 animal sections, 33 embedded videos, 108 local references, zero missing links); layout rendering was not verified. Python and Centipede film stills had been visually inspected separately during the native evidence batch.

## Signing gate at final commit

The first signed commit attempt and the one authorized retry each exited128 with `Couldn't sign message (signer): agent refused operation?`, then `fatal: failed to write commit object`. Exact stderr and tool receipts8694d3/9756f8 are in signing-refusals.json. No configuration or unsigned fallback was attempted. Work remains staged, last signed HEAD95c97b0811358d43a5f6e08ccfb0f82eae04b1d0 G. Nick must renew 1Password SSH approval with “until quit” before one further retry. This supersedes the earlier successful signing checks; unlocked vault state alone does not establish that the agent approved this signing request.
