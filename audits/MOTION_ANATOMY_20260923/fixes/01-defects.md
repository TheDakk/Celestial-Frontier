# Three reported defects

## Crab

`creature-rig-contact.ts` now permits recovery from iterative refusal only for templates declaring explicit source-step travel. The broader painted residual candidate path remains available. This restores legacy crab/quadruped refusal boundaries while retaining Beetle's repair. No tolerance, family contract, gate, historical record or measured bound changed.

`crab-before.log` retains .072643 against .07165014577259474. `reach-after.json` records .070494 both directions and the next refused sample. `reach-probe.ts` is retained. `contact-defects-after-02.log` passes the 18 focused tests; the S2 identity receipt verifies the six original sentinels. The initial after-01 was an accidental unchanged retry after an edit failed to match; it is not new evidence.

## Override graph

`fixed-attachments.mjs` obtains the intrinsic plain-object prototype through the already-approved direct `Object.getPrototypeOf({})` call instead of escaping `Object.prototype`. The source-graph test copies both newly reached transitive modules and expects five. `overridecheck.mjs` itself and its sealed hdart values are untouched. `override-after-01.log`, `override-controls-01.log`, `override-mutations-01.log` retain the green gate and positive/negative controls.

Claude's committed `hdart.verbatim.js` and `hdportrait.worker.verbatim.js` hashes match Nick's two supplied values. The read-only verification at Claude HEAD `236d01860ff8b0fa017a3535f5336fe76b9d430d` is in `invariant-verification.json`. Claude must use those values when reconciling its gate against its actual TypeSafe re-lift. Local values must continue to certify local bytes.

## Portable writers

`portable-record-writer.mjs` normalizes new sources before the family/quadruped seal. It rejects paths outside the repository, foreign absolute drive paths and symlink escapes. Quadruped resealing strips old derived hash/bounds fields first. The ten current family/habitat preparation writers plus Platypus use this adapter; reusable `intake-authored.mjs` replaces reliance on the historical sprint's absolute-root writer for future intakes.

Historical accepted records retain their original source strings and recipe hashes, as required by their bindings. This fixes newly emitted records; it does not rewrite provenance of old records. `portable-controls-04.log` passes four controls including normalization, hash validity, old-record identity, Windows and symlink refusal. Earlier failed controls are retained and explained in EXECUTION.md.

Codex: hold signed repair and its receipts. Claude: integrate code with its own committed art seals, use the portable writer for new records, and preserve old record identities. No new hosted attempt is authorized.
