# Duty, lift and metachronal leg timing

`gait-phase.ts` defines family schedules: insect tripod, arachnid tetrapod, ordered myriapod pairs, alternating biped feet and primate hindfeet with prospective arm slots. Stance occupies .65 of a cycle; swing advances smoothly and uses sin² lift. The contact owner schedules these actual support targets. Quadruped and brachyuran schedules return to their existing owner, preserving S2 and the measured crab refusal boundary.

`after-gait-01` measures six **ground-only projections**: 65.0–65.4% stance, zero relative phase error, admitted lift. Centipede has fourteen pairs with .20-cycle adjacent-pair offsets and a half-cycle side offset. Six `*-gait-static-01.json` files pass exact rest and zero changed source channels. `builder-controls-03.log` and `motion-contact-regression-01.log` retain builder controls and 86 regression tests. Every corresponding selected native film has zero refusals.

The instrument's initial context inadvertently omitted the compiled realm. Corrected `context-audit` shows four land-context walking rows pass; Eagle/Bat compiled aerial mode frees contacts even for the named walking/crawling clip. Their projection is useful timing evidence but not proof the stage actually uses ground contacts. Chimp hindfoot support also cannot certify knuckle walking. Full-contact inventory explicitly fails where the necessary supports are absent.

Centipede's original pre-change program refused an ARAP triangle during turn 1. Its compact after film passes, but different pause timing prevents attributing the entire difference exclusively to the gait fix. The original timing transition remains a Claude investigation. The actual stage source was not changed.

The three proof layers have different scopes: pure action/contact projections, painted static publication and native composed stage. None substitutes for the others or for Nick's biomechanical review. Codex holds; Claude supplies ground-mode contact context and stage integration without changing protected limits. Nick reviews the leg ripple and stance duration.
