# Travelling waves

Family parameters in `family-actions.ts` use the shared 64-key `travellingWave` builder. The phase-offset rest term makes start/end exactly zero without synchronizing extrema. Fish amplitude grows toward the tail; serpent amplitude stays nearly constant. Explicit `phaseOwnedJoints` prevents the material secondary-lag owner from delaying an already-phased tail a second time. No species branch, clock or random generator was introduced.

Python: lag 0 → .1125 cycles; travel index .03830038854328961 → .6427876096865395. Salmon: 0 → .1625; .09595799843472284 → .8615872043170306. Wavelength estimates .9876543209876544 and 1.0256410256410255 are based on indexed links, not true arc-length or world velocity. `after-wave-02/report.json` and raw samples own those numbers.

The first Salmon wave candidate (`after-wave-01`) failed the caudal link because secondary lag compounded the explicit phase. That failure motivated phase ownership; it is retained. Both `*-wave-static-02.json` reports pass every row, exact rest and source reconstruction with zero changed channels. Selected native runs are `20260924-motion-after-compact-films-python` and `20260924-motion-after-wave-films-salmon`; each has zero refusals. CPU is in review-data.json. Python's first after-wave native attempt exceeded the existing 10-second program cap and remains a refusal.

Python strike/constrict are not claimed rebuilt. Its head is a root child, not downstream of the segment chain; the original root excursion metric can pass while the neck barely recruits (.017411361201458392 body-axis root-relative head excursion). Full head-following requires actual stage displacement/history; constrict requires target contact. The Centipede's accepted trunk has no axial chain. These requests are explicit in CLAUDE_REQUESTS.md; no invented body-wave or physical propulsion claim fills the gap.

Codex: preserve this source-bound local evidence. Claude: integrate phase ownership, pass real stage history and inspect strike/return transitions on the integrated stage. Nick: judge the displayed Python bend independently of the still-unqualified neck strike.
