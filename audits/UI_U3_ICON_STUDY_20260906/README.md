# U3 icon study — review only

[Open the comparison](index.html). Ten original SVGs sit beside the current symbols,
with a gold selection preview and a 20–32px glyph-size control. The small dock illustration
preserves the accepted five-plus-four order and Prime count. No product icon has changed.

The proposed “Orbit line” direction uses a 24-unit grid, 1.65-unit rounded strokes,
ice blue normal state and gold selected state. Current references are exact Unicode:
🔭 📖 ✦ 🛠 🌍 🏆 🔔 ? ⚙, plus Objective's ⬆ / 📜.
Guide's question mark and Prime's star/count are text symbols, not emoji.
Objective text/progress and Prime N/9 remain separate visible information.

Author/source: OpenAI/Codex, original path geometry authored for Celestial Frontier on
2026-09-06; no third-party icon pack, borrowed SVG paths, downloaded art or embedded fonts.
No new repository-wide license is assigned. Current Unicode glyphs use the host's fonts;
font software is neither copied nor redistributed. Source hashes and the current-symbol
mapping are in [provenance.json](provenance.json).

The [program authority](../../port/UI_PARITY_PROGRAM_U1_U4.md) keeps current symbols in
U1–U2 and requires Nick's choice before adopting custom SVGs. This directory is a study;
it does not claim product adoption, device UAT, U3 implementation completion or closure
of either historical U1 cause.

**Current export status: PASS.** The parent rechecked the newer explicit U2–U4 user authority
and submitted it to approval review. The first actual isolated Edge rendering passed;
[PNG comparison](study-browser.png), [browser receipt](browser-export-receipt.json). Ten cards,
19 SVG instances, exact current symbols and zero horizontal overflow/runtime errors were
verified. The parent visually inspected the1240×1631 PNG: no missing glyphs or clipping.
The prior rejected review below remains immutable; it launched no process.

**Earlier Inkscape stop:**
Inkscape's default-sandbox version check failed before export. The exact child exit value
was not retained by that first helper; its wrapper exited 1 with empty captured stderr.
Nick reported repeated Inkscape crashes and directed all attempts/investigation to stop.
Inkscape is unusable for this session; 1.4.4 is last-observed metadata, not fresh qualification.
The [retained stop](export-default-sandbox-stop.json) records this honestly.
Do not rerun either retained Inkscape helper.

A single isolated Edge export was requested as a fallback and rejected before process
launch by automatic approval review, which still saw the earlier no-U2–U4 instruction.
[The retained rejection](browser-export-rejection.json) contains the exact call and reason.
No process was launched by that rejected request. The parent then verified Nick’s later
“Please proceed with U2-U4” instruction, presented that evidence to automatic approval review,
and received approval for the same isolated browser export. No workaround was used.

Editable carriers: ten named SVG files, the fixed [SVG sheet](study.svg), standalone
HTML, and the original authoring recipe. The recipe refuses to overwrite existing outputs.
The retained renderer scripts are provenance, not instructions to execute them now.
