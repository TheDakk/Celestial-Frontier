# Development-session tool maintenance — 2026-09-06

Nick replaced the daily chat schedule with startup maintenance for each new game coding
session. The app confirmed deletion of `maintain-game-development-tools`; its local schedule
file is absent. No replacement timer or scheduled task was created.

AGENTS.md and PARALLEL_GIT_PROTOCOL.md require the UI_TOOLCHAIN.md startup runbook after
repository identity/sync checks and before editing, building or rendering. The agent checks
installed identities and official stable availability, applies eligible approved updates while
the tools are idle under the shared lock, verifies changed capabilities and records results
or deferrals. The check command itself is read-only; installation is the agent's next step.
Continued messages reuse the uninterrupted session's receipt. A fresh session checks again.
This is the agent's coding-session preflight, not a hook that runs when an idle app opens.

The personal `nick-game-toolchain` skill now carries this preference into future game tasks.
Windows still needs its own inventory. Active jobs are never killed or closed to update;
verified versions remain fixed through render/certificate chains. Runtime/test dependencies,
sealed inputs, inactive tools, purchase boundaries and GitHub authority remain unchanged.

Independent documentation review found no gaps. No tooling installation or update was run
solely to change this scheduling policy. The companion JSON records the deletion, current
reference hashes and personal-memory hash. Original setup evidence remains historical.

U1 remains at its existing visual approval boundary; this scheduling change does not approve
U2, the integrated audiovisual pilot, a hosted attempt, PR, merge or release.
