# Six-reference native generation: first exact result

Overall **FAIL**, retained unchanged. The ordinary Land action performed actual V2 WebGPU
inference using all six hash/identity-bound references, with nine measured worker-entry requests.
Worker target IDs were not captured in this receipt; do not claim nine enumerated native IDs.
Generation to verified retained Ready took **135.671 seconds** on this Mac. The original is
1024×576 PNG, SHA `44f03a6c6c2e47c38d93106ce4ba08a3dcd7bac17320fd582aa57b8b6aa4c400`.
The real original-store reread, explicit Notifications/View, reload reuse and small resize
checks completed; all44 measured source hashes stayed unchanged. Target/browser/server/locks
closed. This is not a whole-run PASS, phone result or quality approval.

The final exception guard failed on the injected Vite dev client's refused WebSocket connection
to port24678 and its unhandled `WebSocket closed without opened` promise, initially and after
reload. The preview had disabled server WS but Vite still injected the connecting client.
Network favicon404 and ORT provider warnings also remain in the raw receipt. No error is removed
or whitelisted and no unchanged model run is repeated to repair this transport-only failure.
The corrected preview needs its own scoped no-inference browser boot result.

Individual references visibly improve distinct fauna and plant motifs, but whole-scene canonical
anatomy and placements are still not accepted. Read the independent exact-PNG manual review;
V1-only `landfall-fidelity.ts` has not assessed this V2 artifact.
