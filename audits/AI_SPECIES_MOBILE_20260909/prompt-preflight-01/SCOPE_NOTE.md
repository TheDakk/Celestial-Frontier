# Initial prompt preflight scope

The exact pinned tokenizer computed452/512 tokens and the compiler returned the canonicalV2
recipe. Vite also attempted an unnecessary WebSocket listener despite hmr:false, which the
sandbox refused with EPERM at0.0.0.0:24678. The raw result only covers token computation, not
clean preparation or server readiness. The changed successor explicitly sets server.ws:false,
as supported by the installed Vite type/source, and retains a separate receipt. No browser/model
was started by this preflight. The original runner and result stay unchanged.
