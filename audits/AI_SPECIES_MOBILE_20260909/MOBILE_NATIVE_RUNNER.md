# Native optional model delivery diagnostic

## Current outcome — September9,2026

Both full-model aggregates remain **FAIL**, with positive subresults preserved separately.
Attempt01 installed all20 pinned files but lost service-worker control after the diagnostic's hard
reload and stopped before offline verification. Corrected02 maintained the exact activated
controller across all3 documents; all21 model interceptions belonged to the service-worker CDP
session. Explicit resume copied the same6,691,020,416-byte /6,392-chunk attempt with a real nonzero
Range in108.690seconds from the independently hashed loopback mirror, not the internet.

Attempt02 closed both local servers before normal offline reload. Normal Use verified browser copy
rehash completed in79.775seconds; the compiled production read-only owner also reverified/opened
all20 actual native Blobs with exact sizes and independently matching first/last512-byte hashes.
A shifted JSON Blob slice was rejected. The99 cache responses totaled56,490,698bytes, including
control markers and no model files. Its final dedicated-worker debugger observer timed out.
All27 measured sources were unchanged, and targets/browser/profile/model copy/servers/locks closed.
See [native01](native-mobile-delivery-01/result.json) and [native02](native-mobile-delivery-02/result.json).
These results prove bounded desktop storage/offline behavior, not aggregate runtime, phone, CDN,
model inference or species-quality acceptance.

Current diagnostic compilation uses `compile-delivery-diagnostic.mjs` and the exact installed TS7
CLI for only the production delivery/SHA files; no strip/transform-mode fallback remains. Original
compiler failures and preparation text below are historical evidence, not current instructions.

Three subsequent small static-module attempts remain FAIL. They isolate the actual optional
service-worker defect: imported modules retain `destination=worker` but have an empty
`resultingClientId`; package02 wrongly treats these as new worker creations. The source now permits
this exact import form only from a persisted `worker:true` owner of a retained active/prior build,
still checking the complete asset inventory. Window-only, missing, stale or malformed owners
refuse. Ordinary generated worker bytes are unchanged. `run-offline-runtime.mjs` uses the existing
invalid-profile reply before GPU admission, plus exact offline WASM hashing; it copies no model
and performs no inference. Corrected controls passed12 targeted package/reply cases,34 existing PWA
cases, all3 TypeScript programs and root validation. Package03 built and independently verified
with426 unchanged source hashes; [build03 receipt](mobile-delivery/build-03/result.json) records its
changed-input reason (worker ownership and required draft release text).

Current package: `/private/tmp/cf-mobile-pack-20260909-species-03`,56,529,354bytes /97files;
same-size retained pair113,058,708bytes. External manifest SHA:
`5dc7a6e41eebe0e71c399db7c526d87ca47a43d850bb1fc3111dcf8c94aa0d5c`.
PWA build ID:`42d79c05f4b5269766d35e82c03f7b828f70062dc2bdca54ee98ca5f643465b4`.
The runtime/reference closure is unchanged from the independently verified six-reference runtime.

[Separate small native04](offline-runtime-native-04/result.json) **PASS** on that unmodified package.
Its real stage-worker module graph returned the expected existing pre-GPU rejection both online
under control and after physically closing the origin, applying CDP offline mode and normally
reloading into a new controlled document. All95 cached asset hashes matched; lazy Asyncify WASM
readback was25,749,873bytes with SHA
`503d17cb7411b79781b9fad1cf0978f03cf06b050c7d399c730e914f473bf549`.
There was no WASM instantiation, model OPFS namespace, graph/GPU/model work or inference. All12
measured source hashes were unchanged, browser error events/loading failures were zero and cleanup
completed. The retained screenshot was inspected as the ordinary booted Sol game. Native04 closes
the bounded module-delivery defect; it does not relabel native01/02 or small attempts01–03, supply
physical-phone/CDN/performance approval, or require another6.23GiB copy merely to test module loading.

## Initial preparation record — historical

The new `tools/local-image-generation/run-mobile-model-delivery.mjs` is a separate, noncertifying
native observation. It does not execute until the parent freezes the application/pack and launches
one exact command outside macOS Seatbelt under the shared toolchain and workspace locks.

It uses the optional combined static package and normal in-game Notifications controls for an
explicit Install, Pause after committed1MiB chunks, reload without automatic resume, explicit Resume
of the same attempt and full offline Verify. The actual pinned20-file model is6,691,020,416bytes.
Existing ignored local model files are independently hashed by `native-model-mirror.mjs`; only exact
pinned Hugging Face GETs are rewritten by CDP to that disclosed loopback Range mirror. Its CORS policy
allows only the measured static package origin and omits credentials. It is a transport diagnostic,
not a developer model fallback inside the application or a real remote/CDN transfer measurement.
Page and discovered service-worker requests are observed; unexpected remote requests fail closed.

The runtime configuration remains installed-only, with no modelFiles fallback or automatic model
fetch. The observer checks actual OPFS attempt/active/ready markers and immutable chunk sizes. It
hashes every exact CacheStorage asset, rejects model/uninventoried cache entries and then closes both
origin servers before offline reload. CDP offline mode is also applied. Normal Verify must rehash
all model files without network requests. The optional stage worker is loaded with no message sent
while offline, proving its ESM import path without graph creation, model inference or tensor work.

For bounded native Blob access, the runner uses Node's installed TypeScript transform to load the
measured production delivery owner in a detached diagnostic module, then calls only verify/openFile
on the already installed OPFS copy. It does not inject storage or invoke a game probe to install.
Every Blob's full size and first/last512-byte digests must match the independently hashed local
source. The same acceptor rejects a genuinely shifted native JSON Blob slice and reaccepts the
original receipt. This supplements the owner's full-file verification; it does not replace it.

All model bytes remain separate from the app/PWA cache and gameplay saves. The browser uses a fresh
owned profile; the normal launcher closes the browser and removes that owned test profile, including
its6.23GiB diagnostic model copy. It never deletes the existing developer model cache or player data.
First failures, failed control geometry, browser errors, screenshots, source checks and cleanup are
retained. Mirror cleanup explicitly records source-stat integrity, request overflow and streamed
bytes. The full resume attempt has one15-minute bound; it does not retry an unchanged failure.
Physical iPhone/Safari/PWA behavior, CDN delivery/CORS, thermals, GPU/RAM and generation performance
are not established by this desktop native diagnostic.

## Required control sequence

Before the actual native run, execute the new changed eight-test assessor/diagnostic-import set and
syntax check. This tests admission plus wrong readiness, counts, manifest, order, size, head/tail and
actual transformed-owner constructor behavior without browser/model bytes. First attempt retained
7PASS/1FAIL because installed TypeScript7 has no legacy transpileModule API. Second changed attempt
retained7PASS/1FAIL because strip-only mode cannot handle the production constructor parameter
property. The final bounded correction uses transform mode and tests an actual transformed-owner
import with its expected parameter-property error code. Parent owns the final changed control and
native executions; this document does not predeclare their result.

```sh
node tools/with-toolchain-lock.mjs --label mobile-runner-controls-03 -- node --test tools/local-image-generation/run-mobile-model-delivery.test.mjs
node --check tools/local-image-generation/run-mobile-model-delivery.mjs
```

The prepared static package is `/private/tmp/cf-mobile-pack-20260909-species-02`, external manifest
SHA `15cad742de80e6d1488c1d5f2af429c09b913481ebb372a3950df918de6c6485`.
The first native command is:

```sh
node tools/with-toolchain-lock.mjs --label native-mobile-model-delivery-01 -- node tools/local-image-generation/run-mobile-model-delivery.mjs --pack=/private/tmp/cf-mobile-pack-20260909-species-02 --sha256=15cad742de80e6d1488c1d5f2af429c09b913481ebb372a3950df918de6c6485 --output=audits/AI_SPECIES_MOBILE_20260909/native-mobile-delivery-01
```

Prepared runner SHA: `919cb8f8665e057811c884c8f8a0eadc53cc462d605e307425243c3e93cbbb6e`.
The parent must retain an exact source receipt if it corrects anything before or after a first run;
this preparation record remains historical and cannot be relabelled as native PASS.


## Subsequent supported CLI correction — current diagnostic path

Root combined controls subsequently retained13PASS/1FAIL: Node26 removed transform mode, so the
planned native import still could not run. Exact measured runner/test copies and the finding are
preserved in native-tool-controls-03. No browser or model byte copy ran through that failed path.

The current shared `compile-delivery-diagnostic.mjs` uses the existing pinned TypeScript7.0.2 CLI,
a capability confirmed from its installed package/launcher/native resolver and `tsc --help --all`.
One generated temporary config lists only the production delivery and SHA files with ES2022 output,
app-matching DOM libs/types:[], strict/noEmitOnError and no maps/declarations/incremental output.
It checks exactly two emitted JavaScript files, source integrity and one exact SHA import, records
installed native compiler/source/output hashes, and removes only its owned temporary directory.
The native runner performs compilation before opening a browser or reading/copying model bytes.
Its native audit retains compiled outputs and the compiler receipt under diagnostic-owner.

`delivery-cli-controls-01`:8/8 controls PASS, including actual CLI compilation and importing the
compiled production owner, rejecting its real invalid-manifest-total constructor error code, and
observing untouched unknown/not-ready status. Runner/helper syntax checks PASS. All measured source
hashes were unchanged; no dependency install, product/pack change, browser or model access occurred.
Previous failed diagnostic approaches remain immutable. No further broad compiler trial is needed.


## Actual first native install and controller-observer correction

native-mobile-delivery-01 retained FAIL after an actual108.716-second explicit full resume installed
all20 pinned files as6,392 OPFS chunks /6,691,020,416bytes in the same paused attempt. The initial
paused document was controlled; after the runner's Page.reload with ignoreCache:true the new
resumed document was uncontrolled. The late cache-control assertion correctly refused that state.
No browser exceptions were reported, and browser/profile/server/mirror/locks all closed. This
proves the actual desktop OPFS installation path, but does not establish controlled offline delivery.
The first result is immutable; its exact measured runner and source/result digest correction note
are newly preserved in that same audit directory before changing the diagnostic.

The corrected runner requests normal reload with ignoreCache:false at both boundaries. Before
initial Install, explicit Resume and offline Verify, it now requires the actual activated controller's
scriptURL to equal the measured package origin's /service-worker.js. Reload receipts also require
a new actual documentToken. These checks occur before the full resumed copy and offline rehash,
so a controller failure cannot wait until after the large operation. Model interception records
include actual CDP session, target type/ID/URL, networkId and frameId; owner counts distinguish
page requests from service-worker requests without inferring that every controlled-page request
used the service worker. No product, package or model bytes were changed.

mobile-controller-controls-01:10/10 PASS plus runner syntax, with exact sources unchanged. The
same acceptor rejects uncontrolled, unactivated, substituted script/query, wrong origin, missing
or stale-document observations. Existing Blob and actual pinned CLI import controls still pass.
Current runner SHA:c4fd9a625cd770a741f0decf69cc440a36ee0e129e09218398e57bd2d33e9eea.
Parent owns native-mobile-delivery-02 on the same species-02 package; this correction record does
not predeclare that result. Physical phone, remote/CDN transfer and inference remain unqualified.
