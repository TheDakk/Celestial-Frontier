# Corrected controlled full-model delivery: exact second result

Overall **FAIL** at the final dedicated-worker debugger observer. Keep the raw receipt unchanged.
The corrected normal reload retained exact activated service-worker control across all three
documents: before Install, before Resume and before offline Verify. All21 model fetch interceptions
were attributed to the actual service-worker session (zero page/unknown owners); the loopback
mirror independently verified all20 source digests and handled one real nonzero Range resume.

Actual full install/resume took108.690seconds and retained6,691,020,416bytes in6,392 chunks in one
attempt. No automatic reload download occurred. All exact cached app/runtime payloads were hashed;
99 cache responses totaled56,490,698bytes including control markers and no model files. Both local
servers were physically closed before normal offline reload, with CDP offline mode applied.
Native Use verified browser copy completed a full offline rehash in79.775seconds. The separately
compiled production read-only owner reverified and opened all20 native Blobs with exact sizes and
matching independently captured first/last512-byte hashes. The same acceptor rejected an actual
shifted JSON Blob slice and reaccepted the original. This is actual OPFS data, not injected readiness.

The final module-only worker observer timed out; its pending dedicated-worker Network.enable
command also timed out. No worker runtime error was recorded, and this instrument does not prove
whether imports completed. It must not be reclassified as an offline-runtime PASS or a model
failure. A separate small static-package/worker-message test can resolve this boundary without
recopying the model. The source worker already has an invalid-profile guard before GPU admission;
that existing response can prove module execution without debugger attachment or inference.

All27 measured sources stayed unchanged; source model stats/ledger remained valid. Targets,
browser/profile (including the test-installed model), mirror/static server and locks closed.
No internet download, graph initialization, inference, phone or shipping qualification occurred.
The combined package’s original metadata remains unmodified. Physical phone/CDN delivery and
resource/performance acceptance remain separate gates.
