# Ordinary Land on the accepted kit — September 12, 2026

Status: implementation and browser-free controls pass; native UI proof is next.
The accepted painting remains cd6b609f / 0eed6a21, 24.173s warm, tier-2 baseline.
Kit v4.1 and acceptance are signed in dbeb2448; frozen style/4E and original PNG unchanged.

## Product path

Ordinary boot starts the adapter without a query flag. On a supported canonical Earth
landing the precomposed painter loads immediately, independently of model availability.
It is the byte-verified composite made from the accepted offline-fitted inputs; there is
no in-browser reference resampling. The one interpreter compiles the kit from actual
request/roster data. One masked0.35 finisher runs in the page-owned warm worker; each of
four sessions and in-memory transformer expansion survives successful landings. The
original store commits exact PNG bytes before readiness. A ready current-world painting
crossfades over the composite in400ms (immediate with reduced motion). View never changes
routes; Inspect reads an original without landing. Other worlds retain their current
painter until their kit library is authored; the Earth library is not applied to them.

The old local-ai-runtime and both local-model-variant owners and obsolete tests are
removed. The shipped worker accepts only kit jobs. Historical diagnostic tools remain
in source history/research tools, not in the ordinary game path. No pack was built.
A Vite asset closure exposes the existing pinned ORT/tokenizer/kit modules; model files
are either verified parent-cache URLs or verified installed parent Blobs. Expansion
ranges slice Blobs in-worker and write no OPFS derivative. No new delivery/storage tier.

## Part K closure evidence

| Item | Repair | Negative control |
| --- | --- | --- |
|1|View returns false on another snapshot; no route/landing ownership|Execute actual View function with a mismatched snapshot; travel/landing spies remain unused|
|2|Preparation AbortError resolves as paused|Abort a pending preparation; no error notice/rejection|
|3|Opening rejection clears cache; versionchange permits reopening|Fail first open, then retry; close for versionchange, then reopen; explicit close still refuses|
|4|One redraw per animation frame; exact AI job focus restored|6700 events become one render; direct rebuild loses focus under the same assertion|
|5|Retention is commit point; post-commit abort publishes ready|Hold committed retention across abort; early-abort control still cancels without retaining|
|6|Synthetic AI input allowed only in evidence smoke tier|Actual handler invoked with evidence false refuses the same untrusted click|
|7|Synchronous AI-target branch before Survey Close/Leave|Actual close prefix runs immediately; old awaited guard fails synchronous timing|
|8|Retry replaces failed/canceled row|Fail generation, retry; only one ready row remains|
|9|Persistent pagehide handler cancels active jobs|Two hide cycles; both canceled and neither retained|
|10|Stale Inspect read surfaces retry notice|Change view during read; viewer stays closed with notice; current-view control opens|
|11|Invalid install exposes Restart; passes restart:true|Invalid state uses restart action; install cancellation remains non-error; delivery corruption controls retained|
|17|Ship fitted RGBA and verify hash/shape|Alter fitted shape or URL; admission rejects; all shipped hashes equal accepted input hashes|
|33|Remove exclusive lease acquisition from four unit suites|Injected acquisition rejected by source inventory; actual suites operate on private copies|
|34|Publish complete owner via exclusive hard link|Live and empty existing owners cannot be replaced; no empty-owner publication window|
|35|Checkpoint files already committed; preview verifies tracked closure|Omit each load-bearing path from tracked inventory; boot admission rejects|

K34 deliberately refuses stale/unreadable owners instead of automatically unlinking an
unowned inode: competing stale reclaimers could delete a fresh successor otherwise.
Explicit diagnosis/cleanup is required for an abandoned lease. Unit publication controls
use temporary test-owned paths, never the actual checkout lock.

59 adapter/storage/compiler/delivery tests,4 presentation tests and10 worker/integration
controls pass. Full TypeScript and root validation pass (50-probe fingerprint unchanged).
Initial type/test harness errors are retained separately; no native inference has occurred
in this batch. Normal-browser proof and second-landing measurement remain pending.

## Next order

One no-inference ordinary Land/retention/Inspect browser proof using the accepted PNG;
then one native run measuring two unchanged baseline landings on the same warm worker;
then one creative edge/runners experiment, actual iPhone probe, Civet animation proof.
No GitHub step. No pack, integrated chain, old variant native run, sweep or deployment.

## Signing stop and exact resume

The source commit attempt failed with `1Password: agent returned an error`. Nick has
been asked to unlock signing. The implementation, receipts and upcoming proof tools are
staged; no unsigned fallback, browser proof or new native inference has run.

Additional checks:21 frozen-preview/species-reference tests pass concurrently without
checkout locks. The first sandboxed attempt failed after loopback refusal; its cleanup
masked the cause with ERR_SERVER_NOT_RUNNING and left a test child alive. The identified
child was stopped; cleanup now always closes Vite, and the permitted local-listener run
passes.5 final integration controls include shipped-worker refusal of legacy jobs.

After signing, execute exactly:
1. `node tools/local-image-generation/run-kit-normal-land.mjs audits/KIT_NORMAL_LAND_20260912/browser-01`
   outside the macOS sandbox. Uses existing accepted PNG; no inference.
2. Only after that proof passes, `node tools/local-image-generation/run-kit-engine-proof.mjs audits/KIT_NORMAL_LAND_20260912/warm-prepared audits/KIT_NORMAL_LAND_20260912/warm-native-01 --landings=2`
   outside sandbox. One browser run, two unchanged accepted recipes, same warm worker;
   no creative parameter change. Inputs/prompt copied byte-identically, current app warm
   client compiled and source hash recorded. All four session creation counts must stay1.
3. Only after that, implement the authorized6–8px interior erosion/two Cranberry runner
   experiment, run once and show beside the accepted original. Then target-iPhone probe,
   then Civet proof. The target iPhone/iOS/connection question is pending.

The full Part K closure remains pending native outcome proof; green unit controls do
not claim ordinary-game visual acceptance. No pack or integrated chain ran.

## Browser-01 diagnosis

Implementation signed0d825fe5. Ordinary Land mounted the composite in182.74ms and
reload restored the exact accepted original; no runtime exceptions. The proof then
called surveyOn from surface mode, but that evidence helper explicitly accepts only
system mode. The correction clicks the actual #docksurvey control. Preserve browser-01
as failed harness evidence; corrected browser-02 precedes the warm native run.
