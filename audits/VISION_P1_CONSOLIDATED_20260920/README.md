# Consolidated P1 — measurement producer staged; signing blocked

Signing attempt returned `1Password: agent returned an error`; no commit object was written.
Producer and evidence are staged, not signed. Last signed HEAD remains4f575f84638f1f0a478e8e756471938ca667a654.
Native measurements and signed-producer shared controls have NOT run. Resume by signing
and verifying this producer, then run shared-controls.ts once before native-series.mjs.

Nick September20 consolidated direction; sibling review/program read-only, never synced.
All five generation01 paintings stand. Roster holds. No hosted actions.

## Intake and controls

Current fits and retained failures: ../VISION_P1_FOUR_CRABS_20260920/intake-02/README.md.
contact-measurement.json records all ten subjects (five painter/five painted), every row
and presentation. Maximum absolute Knee63.4567920936° / Foot93.9460409234° determine
family contact75°/105° by approved max+10°, rounded up5°. Raw clips/limits unchanged.
contact-negative.json retains insensitive Coconut72px and measured Freshwater46px limit
rejection;40px admits. runtime-controls-02.log:18 tests pass. Initial failure retained.
shared-controls.ts is the signed-producer five-crab R2c′ identity + Civet sentinel sweep.

## CPU measurement plan (no gate selected)

series.json records hashes, vertices, fixed part inventories and exact binding aliases.
Candidate boundary24/interior56,80,128,256; boundary32/48 at interior56.128 aliases80;
boundary32 aliases24. Native-series runs every distinct binding once, retaining prior
Coconut baseline. Native reports/logs are losslessly gzip-compressed; films/stills retained.
No CPU threshold or part inventory change. Nick decides from the completed five-subject
native-results table. Final delivery films wait for that decision and split continuity.

The compiler nearest-ink search now covers the candidate cell diagonal. The original
32px search failure is retained in build-series.log; completed builds in build-series-02.log.
Existing first-found ink is unchanged. Root continuity ledger/guard and P2 remain pending.

## Resumed and completed measurements — September20

The signing refusal above is retained verbatim as history. Exact staged producer **63b13bff**
was subsequently signed and verified; see producer-signature.txt. Shared controls ran ONCE:
five painter crabs bit-identical to R2c′, Civet max drift0.165256373px, exact rest; all PASS.
See shared-controls/static.json, individual rows and retained compressed support samples.

**[Complete five-subject CPU table](CPU_TABLE.md)**:30 requested entries,17 distinct bindings
(one retained Coconut baseline and16 new runs); aliases not re-measured. Boundary48 yields
Coconut1.30ms / Crab1.70ms / Freshwater1.20ms / Mud1.30ms full-film p95. Vent boundary48
REFUSES on painted-support iteration (five rows plus presentation, up to1.02521px residual);
no full-film timing exists. Exact rest remains0/0 in every run. Maximum observed seams remain
below0.00009px. Freshwater boundary48 planted drift0.227328px is close to0.25px.
No threshold, binding, mask or solver change was made to rescue that failed candidate.
The geometry refusal is within the authorized boundary-series experiment, not a regression
of the accepted Vent binding; it is retained as an inadmissible CPU candidate, not an S2 halt.
All CPU flags and old continuity reds remain in immutable native reports. Failed Vent’s
partial root PASS is not a complete-path acceptance. Nick’s CPU decision remains pending.

**[Root ledger and guard](ROOT_CONTINUITY.md)**: all six normal paths pass the measured
family non-gait bound; all six doubled faint-recovery key controls fail. Gait stride,
other-template behavior, raw clips and solver unchanged.6 target tests pass.

## Next deliverable supersedes P2/roster: intake compiler

Nick’s latest direction and read-only PROGRAM.md§5 put IC before further painting,
P2, archetypes or roster. Claude owns registration/hidden inference and geodesic labels;
Codex keeps the shared record, mask, atlas, observed-split and binding writers stable.
See [writer handoff](IC3_HANDOFF.md). These five manually authored accepted fits are the
regression truth, NOT evidence that automatic intake already works. A required hand step
is a compiler defect, never an ongoing per-creature procedure. No gate is auto-admitted.
