# Local commit signing restored — September 8, 2026

Nick confirmed that 1Password was unlocked and explicitly requested a retry. One normal local
SSH-signed commit succeeded. checkpoint-signature.json records the full commit/tree and local
verification against the existing configured public signing key. No persistent signing settings
changed, no unsigned fallback or private-key export occurred, and no hosted action was taken.

The committed index matched all 1,177 files and the raw binary SHA in the verified Earth recovery.
All 257 sealed review carriers plus two supplemental recovery carriers matched before committing.
The older failure records/manifests and binary backups remain historical evidence; their signing
blocker text describes the pre-retry state and is superseded by this receipt and live ROADMAP.
The extra EOF blank-line warning remains recorded. No game source or tests changed in this retry.

The prior scoped checks remain evidence for their exact source. A signature adds provenance; it
is not a full admission certificate, art acceptance, publish authorization or production release.
Codex owns openai/mac. Claude/macOS/anthropic/mac need not open or sync now; future integration is
an independently authorized openai/mac → develop action. No PR or GitHub action is needed now.
