# Five regressions after fb1922a0 — 2026-09-24

## Result

All five reported failures are repaired on the working openai/mac source. The protected Freshwater reach test passes with its original bound. No family contract, binding, S2 input, solver iteration budget, numerical limit or historical Compendium authority was changed.

- Contact: the prior source-step-only condition accidentally disabled the insect's independently tested exact rigid-support recovery whenever its stance is all-six-planted and has no travel declaration. Restore that existing insect path; keep other families on their existing explicit travel authority. Original iterative error plus final analytic refusal cause are retained. Mixed support is not silently treated as rigid.
- Release evidence: current rendered draft contains86 bullets, not85. Independently measured ordered SHA256 is `ab2cc349195fee0fd22fcc6de75e246c67ce3a61e6cdfc3d17d005afacb8ceaa`. Slice and Glass exact inventories, removal control85, and corresponding test literals now agree. Ordered identity, duplicate/reorder/empty/missing rows, semantic overclaim controls and immutable legacy history remain enforced. This is current authored content identity, not a rebind of historical memory samples.

No release-content.ts text changed in this repair. Its bytes are identical between local HEAD and read-only Claude4c2bd1ec6d052ef3081bd6aa0848e33b15db010f, so the measured bulletin identity also describes that shared source file.

## Retained checks and refusals

| Run | Measured outcome |
| --- | --- |
| before.log | Exactly5 reported failures;35 tests pass |
| after-01.log | Original five fixed, but broad rigid recovery wrongly re-expanded Freshwater reach to0.072643 against0.07165014577259474; one related stale evidence assertion also fails.55 pass/2 fail. Rejected. |
| after-02.log | Recovery narrowed to insect; protected reach and all five reported tests pass.59 pass/1 fail: a second stale expectedBulletCount literal in the related evidence suite. |
| evidence-chain-after-03.log | Only that changed suite rerun:9/9 pass. Across final scoped runs, all60 unique tests pass. |
| typecheck.log | All3 TypeScript projects pass |
| validate.log | Root validation passes;1010 species render, zero boot errors,50 deterministic probes match |
| protected-inputs.json | All90 previously pinned inputs unchanged; no unchanged S2 battery rerun |
| centipede-claude-current-01 | Rechecked on newer read-only Claude stage with explicit local motion/contact overlays:0 refusals at0.85×/1×/1.15× |

The initial test-launch command used an incorrect directory for log creation and did not execute tests; its empty created directory was removed. No source mutation resulted. Every actual red run is retained, including full assertion output. No new native film or performance number is claimed. The Centipede probe is a CPU stage/skin diagnostic with a per-source hash receipt, not integrated-source certification. Existing review remains ../MOTION_ANATOMY_20260923/review.html (Python first).

## Claude item4 handoff verified read-only

Claude HEAD at read: signed4c2bd1ec6d052ef3081bd6aa0848e33b15db010f, clean. Its committed external manifest is cf-battle2-assets/v1,133 files,50,426,871 bytes (48.09081172943115MiB). Its picker-smoke-06-sw report records Edge153.0.4234.48/CDP1.3 with worker control, successful painted pairs and cached arena reload while arena service is refused, with no extra server requests. A cold pair falls back rather than fabricating cached paint. This is Claude's evidence, not a repeated local browser run. Exact read source hashes are in claude-handoff.json; Claude's worktree was never written.

## Signing, same-commit pin removal, I5

Nick has not yet renewed approval following the prior two SSH-agent communication failures. No additional signing attempt occurred. Items5 and7 remain staged, signed HEAD remains1e9f5920 G. The five-regression repair is prepared as the next separate item; it does not silently join those commits.

The actual library-arena.test.ts is absent in openai/mac. The exact pin-removal patch and combined `item5-fix-and-pin-removal.patch` both pass git apply --check against current Claude source. The combined patch changes the motion and removes the actual test exception together, without editing that worktree. To remove that actual pin in the same commit as the motion fix, one of two scope decisions is needed: Claude applies both in its single signed integration commit, or Nick authorizes local integration of exact Claude4c2bd1ec6d052ef3081bd6aa0848e33b15db010f. That signed commit descends from localHEAD, but brings1544 changed files; it is not a narrow test-file copy. Standing no-sync/no-merge/read-only restrictions remain until resolved. No unsafe standalone copy of a test with absent dependencies was made.

After signing items5 and7 and the repair, I5 needs the final clean committed source. Nick's earlier local-only source choice remains recorded; no local certificate may certify different PR43 bytes. No fresh I5 attempt, producer rewrite, budget/test rebind or develop profile was consumed during this dirty-source repair. Once source/signing gates are resolved, derive final authority once, reconcile current certification fields together while retaining historical calibration, commit, run the exact-Edge certificate once, named-verify, and run the required authority/budget tests and develop profile under the appropriate unchanged-source chain.

## Paired next steps

Codex holds reviewed/staged work. Nick renews 1Password approval until quit and resolves the actual pin-removal integration route; then Codex signs in item order and finishes final-source I5. The existing wrapper/config is unchanged. No unsigned commit, fetch, sync, push, PR, label, hosted run, merge, release or deploy.

Claude can inspect this packet and its existing item4 integrated proof. It must consume the contact and release-evidence repair together with their tests once signed, plus the staged item5/7 results after signing; do not claim PR43 green before its own exact source is verified. Nick can open Claude to relay the reviewed scope decision. Existing PR43 is anthropic/mac→develop; no new PR/title is requested by this local repair.

Environment: Codex/macOS, /Users/nick/Projects/celestial-frontier-openai-mac, openai/mac tracking origin/openai/mac. Origin git@github.com:TheDakk/Celestial-Frontier.git. Cached tracking comparison0 behind/9 ahead; no fresh remote comparison or hosted authority. Budget mode UNFROZEN does not grant an attempt. Same uninterrupted toolchain receipt reused; observed Node26.9.0. .DS_Store untouched. Hash inventory in SHA256SUMS.json.
