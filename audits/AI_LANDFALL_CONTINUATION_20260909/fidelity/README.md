# Canonical landfall fidelity contract — bounded continuation

OpenAI/Codex on macOS, `/Users/nick/Projects/celestial-frontier-openai-mac`, branch `openai/mac`; base `309809f4df12c711d75822eff1e21bc9ad8a1463`. Startup receipt is reused from the uninterrupted session. This packet changes only the new pure fidelity module/test and this new evidence directory. No model generation, prompt sweep, browser, installed weights, gameplay authority, compiler, existing native outputs or shared documentation was changed by this owner.

## What is implemented

[`buildLandfallFidelityV1`](../../../port/v2/apps/game/src/landfall-fidelity.ts) builds an immutable detached review contract from the exact retained render-recipe string, original PNG declaration and exact ordered reference bindings. It reuses the existing conditioning compiler and full Earth request/19-genome admission. All six full29-field displayed identities, diagnostic roles and accepted normalized anchors remain present. Current and retained earlier prompt bytes are separately hash-bound; only that prompt text may differ from the existing compiler. This historical review allowance does not widen runtime admission or authenticate a producer.

`assessLandfallFidelityV1` requires six complete visual observation records, exact image/recipe/reference/contract digests, unique identity and observed-body IDs, every named diagnostic, explicit counts, placement outcomes and evidence. Missing, duplicate, mixed, stale or internally inconsistent observations refuse. Complete reported failures produce `species-rejected`; complete reported passes produce `review-complete`. Every result still has `semanticsVerifiedByCode:false`, `reviewerAuthenticated:false` and `fullQualityAccepted:false`. The module is not imported into the game and does not grant appearance publication or a durable reward.

The original digest binds the **exact encoded PNG bytes**, not a separately measured decoded-RGBA digest. The reference digest binds the exact source PNG plus declared480×320 preparation geometry and full subject identity. The pure builder performs no I/O: its caller must verify image/reference bytes against these declarations using the original/readback owner. The tests independently hash both real originals and the real reference; they do not infer image semantics from hashes. Serialized data does not become a live `CanonicalWorldRoster`, and a copied contract must be explicitly rebuilt before assessment.

## Actual negative evidence and favorable feedback

[`known-negative-observations.json`](known-negative-observations.json) transcribes the already completed **agent direct visual inspections**, with original and review file hashes. No classifier or new pixel inspection is represented by those records.

- Native01 has the bill-bearing spotted hybrid plus Frog, no separate Civet, and incomplete named botany. It remains species-rejected.
- Native02 has four mammal bodies, no Frog, incorrect Civet/Platypus anatomy and anchors, and incomplete named botany. It also remains species-rejected.
- Nick's latest “fantastic” feedback is recorded favorably for the latest preview's finish. His message did not identify a PNG hash. That feedback is independent from the unchanged canonical species/count/anatomy/placement requirements and does not rewrite either original review.

Only the positive control is synthetic, with a separate synthetic pixel digest and `observer.kind:synthetic-control`. No accepted canonical generated positive has been demonstrated. A malicious or mistaken reviewer could still report false observations; this owner checks consistency and evidence bindings, not semantic truth or reviewer authentication. A production automated fidelity solution remains unimplemented.

## Actual conditioning capabilities

The pinned [ONNX model revision](https://huggingface.co/cgb/flux2-klein-4b-onnx-webgpu/tree/3bffc0efef1d9f84727036cdbc44df3b6ab51131) has exactly five transformer inputs: float16 `hidden_states[batch,image_sequence,128]`, float16 `encoder_hidden_states[batch,text_sequence,7680]`, float16 `timestep[batch]`, int64 `img_ids[batch,image_sequence,4]` and int64 `txt_ids[batch,text_sequence,4]`. Output is float16 `noise_pred[1,image_sequence,128]`. The local4991601-byte graph SHA is `1c56c0b6ce5ce474a5ae5cc126ec3201516540e7f743de308dc07c9e72fb6102`; [raw signatures](raw-model-signatures.json) and [exact source hashes](source-bindings.json) are retained.

The actual stage worker, lines98–123, appends whole reference latents to output noise each step, supplies one global text embedding, assigns output `t=0` and ordered references `t=10,20,...`, and updates output tokens only. The native-game runtime currently passes one reference. No input or current operator wiring supplies resident boxes, regional masks, per-subject attention routing or an enforced object count. Numeric image positions distinguish token groups; they do not demonstrate that each species will occupy its requested scene anchor.

The [official FLUX.2 Klein4B model card](https://huggingface.co/black-forest-labs/FLUX.2-klein-4B) describes image editing and multiple-reference capability and acknowledges prompt-following limitations. It does not establish hard regional/count enforcement. The earlier pinned [Diffusers pipeline source](https://github.com/huggingface/diffusers/blob/040c7cde626504d14caf63b13b8b25b6a9f62120/src/diffusers/pipelines/flux2/pipeline_flux2_klein.py) inspection remains recorded in `tools/local-image-generation/source-contract.md` with SHA `8a57a8a7f1c22fc0c5bc5d3fd1bd9bc76c17c1f4d6644cc73370651dd955358e` and position/preparation/concatenation line ranges. A fresh small raw-source request failed DNS; [that unavailable-read receipt](official-source-receipt.json) is retained rather than claiming a new source download.

A smallest plausible later adapter is an explicitly versioned ordered reference array, binding each reviewed PNG/dimensions/image index to its complete species visual key and keeping the token-group order stable. The low-level worker already accepts several reference groups. This would provide individual **reference bindings**, not qualified spatial control or a guarantee of correct count, anatomy, botany or placement. It requires a new bounded actual output comparison and this same visual outcome contract. No such adapter or new generation is included here. Hard region enforcement would need separately proved attention/mask/export support; merely changing image IDs or adding prose cannot be labeled that capability.

## Verification and next ownership

The first owner run passed **18/18** on Node26.8.1 / Vitest4.1.10,451ms reported Vitest duration, with source bytes unchanged under both shared locks. [Raw log](focused-first.log) and [command/version/source-bound receipt](focused-first-result.json) retain the exact result. Tests include independent SHA checks for both actual failed paintings/reviews/reference, full identities and anchors, both-direction missing/extra-body controls, incomplete/duplicate diagnostics, mixed identities/rows, stale image/recipe/reference bindings, wrong world/genome/model/reference declarations, frozen detached data and hostile input hooks. No broad profile or browser admission is implied; parent owns subsequent TypeScript/root checks.

The parent's subsequent first TypeScript chain stopped on this module: TypeScript did not use the inferred arrow-bound `never` helper to narrow values after refusal branches. The exact failed module/test bytes are preserved in `typescript-first-failed-*.txt`, and the original validation log remains under `../validation-01/`. The only correction declares that same helper as an explicitly `never`-returning function; no runtime guards, casts, acceptance criteria or tests changed. The changed source then passed the same **18/18** focused outcomes once (470ms Vitest duration), with [new raw log](focused-type-correction.log), [new result](focused-type-correction-result.json) and [old/new source hashes](typescript-correction.json). This supersedes the module hash in the original source-bindings receipt without overwriting it. Parent's subsequent validation02 and final [validation03](../validation-03/result.json) passed all three TypeScript programs and root validation; the original first failure remains preserved separately from those corrected results.

OpenAI/Codex: keep this pure contract and its evidence with the current local continuation; root owns final docs, local checkpoint and any later integration. Anthropic/Claude Code: review the exact source and retained negative outcomes through the explicit next handoff; no Claude review was invoked here and Nick does not need to open the other app for this owner to finish. PR42 remains parked; no new PR or hosted action is part of this batch.
