# Full-painting inspection, browser runtime packaging and fidelity review

September 9, 2026. Continued from signed `309809f4df12c711d75822eff1e21bc9ad8a1463`.
Nick asked to continue and said **“The latest image looks fantastic.”** This is favorable finish
feedback for the latest preview. The message did not identify a PNG hash or waive canonical
species/count/botanical rules. Preserve the warm response alongside the earlier visual findings;
do not convert it into full species, mobile or distribution acceptance.

Verified side: OpenAI/Codex · macOS · `/Users/nick/Projects/celestial-frontier-openai-mac` ·
`openai/mac` · upstream `origin/openai/mac`. Prior SSH origin/read established; no fresh remote
comparison needed for local continuation. Starting head is ten ahead of origin/openai/mac and
121 ahead of origin/develop at the last verified refs. The uninterrupted September 8 Civet startup
receipt applies. `.DS_Store` stays untouched. No personal UI, new model/package install, model
render, GitHub write, scheduled prompt, automation, release or deployment is part of this batch.

## Implemented scope

- **Inspection:** an explicit modal displays the verified retained original, with Fit painting,
  Actual size/panning, keyboard access, Close/focus restoration and lifetime background isolation.
  It uses the original Blob without re-encoding and releases its URL/DOM on close or pagehide.
  Late decodes cannot revive a closed dialog or replace its successor. Ready rows expose Inspect;
  the normal Survey also exposes a restored original after reload when page job rows are absent.
  Navigation, species, saves and the accepted base control placement remain unchanged.
- **Static browser runtime:** [packaging evidence](runtime-pack/README.md) records the exact
  WebGPU/Asyncify/tokenizer/helper/reference closure, source pins, notices, deterministic static
  paths, external manifest-SHA verification, strict refusal controls and local output. It is
  28,393,596 bytes (27.08 MiB), including inventory, without model weights. The game admits its
  installed-only manifest and requires explicit verified OPFS files; no developer fallback or
  automatic download is possible through this route. Failed verification keeps generation closed.
- **Fidelity:** [contract and graph inspection](fidelity/README.md) bind complete six-resident
  identities/diagnostics/anchors and exact encoded PNG/recipe/reference digests to an explicit
  visual review. Incomplete, duplicate, mixed or stale reports refuse. This records observations,
  not image recognition or accepted generated pixels. The graph has no explicit mask/box input;
  whole-reference groups and global text do not enforce per-instance placement or animal count.

## Verification and current boundary

The first viewer/controller/main-owner set had **35 PASS / one instrument FAIL**. That fixture
compared complete HTML serialization after attribute removal/restoration, including incidental
attribute ordering. The corrected ruler preserves every attribute name/presence/value and passed
all seven viewer cases on unchanged product bytes. A subsequent pagehide case and unchanged
main-owner cases passed **22/22**. Controller installed-only/inspection cases passed **15/15**.
Raw first logs and source copies remain under viewer-controls-01/02 and validation-01. The
concurrent inspect/close read race was subsequently corrected and all16 controller cases passed.

Runtime pack controls passed **11/11**, followed by its first actual build and independently
supplied manifest-SHA verification. The first fidelity owner checks passed **18/18**, but the
combined TypeScript run failed because its inferred `never` helper did not narrow checked unknown
inputs. The exact old source/log is retained. Changing only that helper to an explicit
never-returning function declaration passed the same18 cases; no guards or tests were weakened.
Validation02 passed all three TypeScript programs and root validation.

Native viewer01 stopped before Inspect on control visibility. The initial observer could see an
already-mounted original before asynchronous View completed and omitted failed-control geometry;
the precise first visibility cause remains unproven. The corrected observer waits for the closed
Notifications state before reopening and records failed geometry/screenshot/UI. Native02 then
passed image/fit/detail checks but failed focus return on Close: resize had closed Notifications.
The final product captures the action/job intent before storage reads and restores its live
replacement, or the visible existing Notifications opener if that panel has closed. The ruler now
checks that observed panel state. Both first results and measured-source copies remain immutable.

[Final validation03](validation-03/result.json): **26/26** viewer/controller cases, all three
TypeScript programs and root validation PASS (1,010 renders, zero boot errors,50 unchanged original
fingerprints). [Native03](native-viewer-03/result.json) PASS on Edge152.0.4191.66/CDP1.3:14 trusted
clicks, seven observations, exact original Blob SHA, desktop1024×576 fit,278×156.375 small fit,
actual-pixel panning, rejected/restored40px-image negative control, keyboard trap/Escape, live
focus return and Survey inspection of the original after reload. Source36/36 unchanged; isolated
target/browser/server/locks closed. No new inference or model-file requests. The [independent screenshot review](native-viewer-03/visual-review.md) found no further viewer
correction; Actual size is needed for anatomy detail on narrow screens. Three favicon404s
are retained; no zero-browser-log-error claim. This is desktop small-viewport verification, not
physical iPhone/Safari/PWA, touch/pinch or full admission. See the screenshots and the
[paired handoff](HANDOFF.md) for the exact scope and next steps.

## Limits carried forward

The latest retained native model run is still the prior 74.307-second experiment. This batch
must not claim a new inference time, phone inference or species-success result. The full model
remains 6.23 GiB, separate from the runtime, saves, original images and live RAM/GPU allocations.
The standalone runtime pack being below 128 MiB does not prove the combined game pack or its
256 MiB retained-update budget, deployed headers/offline behavior, distribution clearance or
physical phone suitability. The native-component/source-notice correspondence remains qualified
in the packaging review, despite retained available official notices.

Exact original lookup includes the model recipe: changing that recipe across reload can prevent
automatic discovery of an older retained image. The bytes remain; this owner adds no deletion.
Cross-recipe discovery, outside-origin backup and exact-image sharing remain future work.
All original-generation failures, tiny base-scene image evidence and prior roadmap/HUMAN blockers
remain intact. An enlarged inspection surface does not move or enlarge the base scene band.
Claude's intended app review is pending. PR #42 remains parked; no hosted authority is consumed.
