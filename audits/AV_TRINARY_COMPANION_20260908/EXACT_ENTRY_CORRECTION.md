# Exact native entry correction — 2026-09-08

The first diagnostic imported the correct seeded trinary2166531614, then exited natively and
clicked its galaxy-map coordinate. The overlapping target at seed2868415165, normalized
(-145.1,22.14), received the click instead of2166531614 at(-145.99,22.47). Native Enter then
correctly entered that other six-planet system. The exact-seed wait stopped after20s; no runtime
or cleanup faults. Keep original native-first runner/report/screenshot; no third-star PASS.

The successor captures the existing read-only encodeHere() of the verified imported trinary,
exits natively, enters that exact CF1 through real Search and presses native Enter. This invokes
the existing source-proof/reach-check/durable Follow path. It must commit, clear Search and
publish the exact seed, normalized coordinates and rendered route with settled authority.
No fake target, injected star, bypass or production source change. The proof now claims native
Search→Follow entry, not successful canvas Survey→Enter. The nearby-target ambiguity remains a
recorded interaction finding for Claude, outside this graphics correction. The successful build,
browser-free profile and root validation are reused unchanged in a fresh native-exact-address run.
