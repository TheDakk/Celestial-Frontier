# Retained verification corrections

The first full browser-free profile passed330 files/3742 tests plus1 skip and failed5 tests
across4 existing verification files. All20 new atlas/motion/lifetime tests passed. The failures
were stale current-producer metadata and an84th release row against the fixed83-row inventory.
The motion sentence now belongs to the existing Planetside/horizon bullet; ordered digest and
current producer binding were refreshed without changing any ruler, ceiling or historical report.
The focused104 tests passed. This does not relabel the stopped profile as a complete PASS.

The continuation then found a custom Mesh generic typed as Pixi's default TextureShader, and
a newly imported Pixi test entering the root strict program exposed the known TypeScript7/
WebGPU declaration collision. Specify Mesh<MeshGeometry,Shader>; keep its test in the app's
existing scoped TypeScript program. Root package checking remains strict, with no new exclusions
or skipLibCheck. All3 TypeScript programs passed after that correction.

The next owner correctly refused the new planetgen/makeNoise imports in the protected shared
art package. This Earth-only adapter now belongs beside its game presentation view/worker,
with direct relative imports and no shared-art export/dependency change. Final52 focused tests,
3 TypeScript programs, art-unused/art-audit/override/spec checks and root validation passed.
Producer stayed019a63e3 after the ownership move; measurement/ruler/ceilings are unchanged.

The first native run stopped before Survey: immediately after Training Skip the observer read
all planet holders at the initial origin and clicked canvas center(195,422). The retained capture
shows Earth elsewhere; no Survey opened and no renderer ran. Correct the observer's readiness:
require actual canonical Earth/Mars rendered positions to be distinct by more than8 CSS pixels
before reading/clicking Earth. This uses real rendered facts, no forced camera/navigation/saves
or arbitrary waiting extension. The original runner/report/screenshot remain in native-first.
The corrected observer reuses the unchanged92-file evidence build.

The corrected observer reached the exact Earth Survey and committed Land. The real renderer
then threw while looking up its missing mesh render pipe. The view had loaded MeshPipe after
app initialization, but Pixi snapshots its render pipes during init. Register the optional pipe
beside CullerPlugin before app.init; keep the view lazy. Its existing GlMeshAdaptor is already
available. A pre-worker absence check now retains the standard globe if no pipe is present.
The actual exception and first unpainted ready state remain in native-corrected. The corrected
source passed focused producer/budget tests, all3 TypeScript programs and root validation before
fresh native work. The original92-file build remains immutable; the new build has its own receipt.

The registered-pipe run painted the phone globe, moved actual mesh pixels through its natural
18-second arc and settled exactly. It then stopped at a mixed-owner uniform cache count:4 versus
3. Pixi extraction enables a render group on the extracted target and binds pooled global
uniforms; the observer had left that group enabled and treated a renderer-global UID as if it
were another scene-owned turn UID. The corrected extractor restores the original group flag and
normal scene render in finally. Cache verification now classifies every actual dirty-group UID
against the shared turn owner, mesh-pipe local owner and retained renderer-global pool. A synthetic
orphan UID must fail the same check; program count/key and shared turn UID remain exact across
re-entry. No runtime source or evidence build changed for this observer correction.
