# First static run — retained FAIL

44 articulated rig tests PASS, then study TypeScript FAIL: MediaRecorder MIME remained `string | undefined` when constructing a Blob under exactOptionalPropertyTypes; two water-owner assignments passed a readonly filter array to Pixi's mutable-array setter. Runner syntax, all-three-V2-typechecks and root validation were not reached. No native stage ran.

The correction asserts the already-admitted MIME at the Blob site and passes shallow mutable copies of original filter arrays while preserving null/undefined and each filter identity. No artwork, rig, timing, contact or acceptance threshold changes. Static-first/logs retain the original source hashes and failures. The unchanged rig test PASS is reused in the next static continuation.
