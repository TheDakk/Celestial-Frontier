# Parked gameplay completion — 2026-09-26

Nick requested completing the remaining work saved from the removed gameplay branch, while continuing the Generated Creature Pipeline in parallel. The original signed WIP and its191KiB recovery bundle remain preserved. No old branch was recreated.

## Disposition

- Starter bioscan, descent/wave-offs, fifty Paragons, exact-instance progression and mature Atlas: already recovered by Batch4, retained.
- Weekly Charter lifecycle: current active-play implementation supersedes the old wall-week owner. No clock regression imported.
- Forge Training: restored and adapted to today's crafting contract. One canonical Iron Plate in an isolated cloned sandbox; zero live economy, RNG, receipt or save changes. Sixteenth lesson; current release/Guide copy and causal sequence updated.
- Living portrait: restored selected-detail controller, one existing image/lease and ticker; gentle whole-image motion, not rig animation. Hidden/reduced-motion/Back/Close/replacement lifecycles retained. No new paintings or manually authored rigs.
- Bulk copy/import doors/whole-file replacements: superseded by current fresh-start policy and system references; not outstanding feature work.

Initial restored Forge tests refused because the stale WIP expected a weeklyCharter result field that current Arc3 no longer returns. Its equivalent unchanged weekly progress/acceptance/completion state is now verified directly, retaining protected-carrier equality. Focused84 tests passed before native layout repair.

Native attempts retained by source hashes: attempt1 lacked evidence APIs in normal dev mode (instrument setup); attempt2 proved the native craft and unchanged save, then found the panel covering Training actions; attempt3's first CSS rule lost on specificity; attempt4 passed phone, then exposed desktop's different top anchor. These are retained failures, never certificates. Final native result and full battery follow below.

The Generated Creature Pipeline continues separately: G2 committed20 masters and120 mask candidates, G1 remains red, G5 engine proceeds in parallel. No hosted attempt, release or deployment is authorized here.

Native attempt5 PASS at390×844 and1440×900. Real native Forge activation reaches the next lesson with identical live save fields and every localStorage entry; sandbox released. Portrait owns one image/ticker, actually changes its transform, reduced-motion stops the ticker, Back releases both. Injecting the original oversized panel reproduces overlap. Final source-specific reports and inspected screenshots retained. This is scoped behavior/geometry evidence, not full Slice/Glass or physical-device qualification.

The first full integrated profile had14 reds: standingI5 plus13 extracted-Main fixture failures. The new dependencies were missing from three existing test environments. Harnesses now inject the real restored classes/adapter and execute the real Training interception with Training off; all46 focused integration tests pass, including unchanged negative controls. No product assertions were removed.

Small-phone320×568 geometry passed, but the browser log exposed a ResizeObserver notification loop. That result is not accepted as clean. Lesson measurement now defers observer-triggered publication to the next animation frame and skips unchanged values; teardown cancels the frame. The harness now also records window errors/rejections. The changed320×568 run passes with no runtime errors. Final390/desktop recheck follows on that same implementation.

G2 compiler negative controls refuse unknown species, a non-quadruped and an existing packet directory. All20 retained prompt hashes match, with no authoring files (`g2-compiler-controls.json`).

Final native proof passes320×568,390×844 and1440×900, with empty runtime-error lists. Added a real Close→reopen before crafting: closing releases the sandbox, reopening creates a fresh generation, and the craft still changes no live save/storage. The original parked allow-list stranded the closed lesson and exposed the whole Fabricator subtree; the recovered allow-list now permits its opener, section summary and one practice button only. Main also refuses recipe pinning during Training; a real-controller outcome test proves no save mutation or persistence call even under programmatic activation.33 focused lifecycle tests pass. The resize teardown uses the window frame owner only when a frame exists, preserving nonvisual environments.

No behavior from the removed branch remains forgotten: all named gameplay features are integrated or explicitly superseded in the disposition above. The original commits remain recoverable for provenance. Art quality, G1 admission, G4 selection and G5 routing are separate active pipeline work, not leftovers on a removed branch.

Final integrated battery:507/507 tool checks;5,615 unit passes,2 expected failures,2 skips; standingI5 is the ONLY red. All7 manual owners pass (three TypeScript programs, art audit, overridecheck, speccheck and mutation controls); see battery.json and source logs. Root validate passes with50 unchanged v1 fingerprints and1,010 Earth renders. Free229GiB. No sealed gate, ceiling or reference baseline was weakened.
