# Portable drawing diagnosis and local variant derivation

Work continues from signed `f6eed9b4c65a1aafc0ddd19f0be73851b3c87d13` on `openai/mac`.
This checkpoint committed the preceding staged 84-file batch. The configured 1Password
`op-ssh-sign` helper succeeded; the earlier `ssh-keygen` signing overrides and their failures
remain in the [previous packet](../AI_OFFLINE_INTEGRATION_20260909/README.md).
No persistent Git/security setting changed. Independent signature verification passed.
See [startup](startup.json) and the unchanged [signing receipt](signing-success.json).

## Active scope

The actual six-reference portable render timed out after session creation and before the first
completed step. The original [native02 FAIL](../AI_OFFLINE_INTEGRATION_20260909/native-02/result.json)
is immutable. A new explicit first-step diagnostic preserves its full recipe, six references,
1024×576 geometry and four-step sigma schedule. It persists execution/readback/device-fault
boundaries, stops after one step, and cannot publish a completed painting. Product preparation
and native module workers remain real; only diagnostic messages are intercepted by the observer.

A separate browser derivation core uses the already verified parent model and a pinned 142,918-byte
patch/range plan to reconstruct the existing block32 graph and scale/zero data. It streams exact
operand bytes and verifies both target hashes. No new weight download, inference selection,
UI change, smaller image, fewer references or altered denoise schedule is included.
The additional output is 352,323,881 bytes, separate from the 6.23 GiB parent and art cache.
The core is detached pending real browser storage integration and native qualification.

## Evidence so far

- [Plan controls](variant-plan-controls-01/result.json): three controls and exact graph
  reconstruction passed; seven source hashes unchanged. This does not measure parameter derivation.
- [First-step controls01](first-step-controls-01/result.json): thirteen passed. Independent review
  then found chronology gaps in the replay assessor; the first pass remains scoped.
- [Corrected controls02](first-step-controls-02/result.json): fourteen passed, including terminal,
  step and cleanup reordering refusal, on eight unchanged source hashes. Actual worker tests also
  cover unchanged ordinary four-step behavior, device loss/error and release failure.
- [Combined controls01](combined-controls-01/result.json): 37 focused tests and 42 lifecycle
  controls passed. Twelve runtime-pack cases refused the wrapper's redundant checkout lock before
  their bodies ran. The aggregate is FAIL; TypeScript and root validation were not reached.
  Corrected orchestration will give that suite its own lock and retain this first failure.
- Independent derivation review found no operand-expansion or hash defect, but requested actual
  transaction outcome controls and explicit success release after final marker readback. Those
  corrections are in progress; no transaction/native-storage success is claimed yet.

Native first-step execution, final combined verification, source review and signed handoff are
pending in this in-progress record. The target iPhone/iOS question remains unanswered. Desktop
controls cannot establish real phone memory, thermals, latency or durability. The existing exact
V2 image remains species-rejected and full quality acceptance remains closed.

No hosted actions, new model download, Claude invocation, automation or schedule. PR42 stays
parked Draft/unlabeled. `develop`, `main` and the live site are unchanged.
