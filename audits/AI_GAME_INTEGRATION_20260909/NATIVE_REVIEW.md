# Native integration outcome and limits

The second changed run passes the bounded real-game interaction/storage observation. It used
four actual browser workers, the existing freshly verified model cache and real native WebGPU
inference. No inference/download substitute, remote generation or scripted Land call was used.
The diagnostic setup selected Earth's existing Survey card; Skip training, Land, Notifications
and View used four measured, trusted native clicks. Source hashes matched across the run and
owned target/browser/server/workspace locks closed.

The existing landing save transaction had committed and published the canonical Earth scene
before drawing began. The product coordinator was idle at the captured observations. The real
Notifications panel answered during inference, showed actual ordered drawing/ETA, stayed open
at completion, and only explicit View mounted the painting. That one roughly74.307-second run
is not a latency guarantee or a full gameplayFPS benchmark. It does not prove sustained play
across suspension/background-tab throttling or physical mobile devices.

The original is1024×576 PNG,1,520,711B, SHA256
`aebec1c3b9cf9bb3761ff0178d77b7d525aa7683d5ee5f448408a5f0949fd7e5`.
The real IndexedDB owner committed it, read it back and checked fullinput/hash before Ready.
The exported audit PNG is unchanged. A new document remounted exactly those bytes with no
new model or inference-worker request. Resizing to320×568 also retained the same original and
settled on the same world/environment/epoch. The viewport result proves containment/restoration,
**not usable small-screen art size**: root inspection sees a roughly76×43 illustration in the
narrow available band. Accepted UI placement was preserved, but a legible art-inspection surface
or separately approved layout treatment remains necessary before mobile acceptance.

The first actual run took74.047seconds. Its overall result stays FAIL because its resize
observer sampled during pending art refresh. It had already demonstrated Land, Notifications,
Ready, explicit View, stored original and reload; its final observation was mounted again.
The corrected observer waits for exact identity, idle art work and stable scene/ticker frames.
Original failed result/runner and measured pre-change compiler/controller sources are retained.
This was a changed prompt/UI/observer run, not an unchanged retry or a relabeled first PASS.

Both images fail the visual requirement. First has a Civet/Platypus feature fusion and no separate
Civet; second has separate-looking Civet and billed mammal but two extra mammals and no Frog.
Botany/scale/anchors and approved subtle finish remain inaccurate. Prompt specificity changed
the failure, not the acceptance state. Shared wet contact/light/environment improved over old
cutout staging, but that does not establish species fidelity. No accepted asset is replaced;
qualityAccepted remains false. Read both independent visual reviews and original full-size PNGs.
Do not spend more runs on an unchanged prose-only approach and call it a fidelity solution.

The native receipts retain ORT CPU-placement messages and favicon404 notices; these are not
reported as zero browser-log errors. Neither receipt observed an uncaught game exception or
browser crash. First/current full statuses and source hashes remain separate. This is not the
Compendium/Slice/Glass certificate, physical Safari/PWA qualification or an Actions result.
