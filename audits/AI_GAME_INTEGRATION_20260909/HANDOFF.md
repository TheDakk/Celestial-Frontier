# Handoff for Nick and Claude

Nick's intended review in his Claude app is pending. Do not treat the saved earlier CLI response
as that review or approval of this batch. No new Claude invocation occurred here.

## Direction and accumulated scope

Celestial Frontier remains a browser proof of concept for a later engine game. Landfalls should
be cohesive, rich natural-history paintings generated locally on demand, with multiple correct
canonical flora/fauna and the approved common light, materials and atmosphere. Compendium keeps
their full seeded identities and gameplay properties. Later articulated 2D battle rigs cover land,
flying and aquatic families; universal objects share the same painted language. Named Earth rules,
biomes, lineage, deterministic world identity, saves and accepted UI locations remain fixed.
Current clocks are unchanged; real seasons/orbits and exact-pixel sharing are future systems.

For all accumulated changes before this batch, read the previous
[direction/change inventory](../CLAUDE_DIRECTION_REVIEW_20260909/HANDOFF.md), its retained review
disposition and ROADMAP_ARCHIVE. They cover responsive UI, audiovisual/art studies, generation
feasibility and earlier optimization/controller work. This new batch starts at signed
`98cb65c8c79d8e70bd6c6becb7d64b8a81b552a8` and adds:

1. Canonical conditioning compiler: complete 19-genome Earth snapshot, six named diagnostics,
   exact identity/anchors and a SHA-bound Platypus reference. Prompt 472/512 tokens, no truncation.
2. Explicit resumable model delivery: OPFS chunks, Web Locks, actual HTTP Range, incremental SHA,
   committed readback, atomic ready markers, quota refusal and no automatic original deletion.
3. Durable exact originals and page-owned jobs: independent IndexedDB transactions, complete
   recipe/hash identity, one active/three waiting, cancellation, verified retention before Ready.
4. Actual game integration: ordinary Land settles first; Survey/Notifications show progress/ETA,
   explicit View and cancel while the rest of the game remains usable. Originals reload without
   inference. Stale View/bitmap responses refuse; old visible art survives failed replacement.
5. A local preview server and source-bound real-game runner, preserving the isolated pinned runtime
   and model cache. No weights enter the repository or game pack. This is still development tooling.
6. A final model-selection Retry guard refuses a stale recipe before enqueue and tells the player
   to land again. It preserves original identity and does not fall back to the prior model.

## Evidence to assess

Read [NATIVE_REVIEW.md](NATIVE_REVIEW.md), both independent visual reviews and original PNGs.
The second actual game run passes its interaction/storage/resize observation at 74.307 seconds:
four trusted clicks, twenty observations, real model workers, durable original, explicit View,
same exact pixels after reload, no second inference and settled 320×568 containment. All 34 recorded
source hashes remain unchanged; targets/browser/server/locks closed. The first actual run remains
FAIL because its resize observer sampled before asynchronous remount completed; it is preserved.

**Do not approve visual or mobile readiness.** Both model outputs fail species/count/botany.
The stronger prompt separates the Civet from a billed mammal but creates two extra mammals and
omits Frog. The original 320×568 display contains the painting as an approximately 76×43 thumbnail,
which is not sufficiently legible. The native delivery audit uses 2 MiB synthetic files on desktop;
there is no full 6.23 GiB phone installation/inference certificate. A shipped browser runtime pack,
mobile memory/thermals/latency and physical Safari/PWA behavior remain unfinished.

Compiler 16, delivery/hash 32, original/job 15, runtime 20 distinct cases, initial main/layout 66 and
controller/expanded main ownership 23 scoped outcomes are detailed in README. First instrument
failures and corrected cases are retained rather than summed into a misleading new full battery.
Current 472-token compiler 16 / all three TypeScript programs / root validation pass; root gives 1,010 renders, zero boot errors and
50 unchanged original fingerprints. The old 4,121-test profile at 9bf is not admission for this source.
ORT CPU-placement notices/favicon404s are retained; do not claim zero browser-log errors.
After native02, the final Retry guard passed eleven focused controller cases and all three
TypeScript programs/root validation. Its separate retry-guard-01 and validation-retry-guard receipts
own those changed bytes. No later native run or full admission is claimed.

Next fidelity work needs spatial/per-subject conditioning and an outcome gate for separate
body count, named anatomy/botany, anchors and approved finish. Two prose-only attempts establish
that more specific text is insufficient here. No additional prompt sweep is queued. Qualify
the real target phone once Nick identifies it; do not infer phone readiness from desktop emulation.
Consider an accessible full-painting inspection surface for short screens while preserving the
accepted base controls. All predecessor verification/HUMAN blockers remain in ROADMAP.

## Paired Git handoff

Current side: OpenAI/Codex · macOS · `/Users/nick/Projects/celestial-frontier-openai-mac` ·
`openai/mac` · upstream `origin/openai/mac`. The local source/evidence checkpoint is the commit
containing this handoff; Git retains its full ID and SSH signature. Parent 98cb is nine commits ahead
of remote openai/mac at 9bf and 120 ahead of origin/develop at c1791e21 at the last verified fetch.

GitHub step now: **none**. [PR42](https://github.com/TheDakk/Celestial-Frontier/pull/42) stays
Draft/unlabeled on its old remote head. No new PR is needed. Before any future Ready/owner-label,
refresh its accumulated exact head/title/body and run the selected admission lane. Prepared fields:

- Base: `develop`
- Source: `openai/mac`
- Title: `Expand the browser slice with responsive UI, audiovisuals and local AI landfalls`
- Description:

```text
Expand the browser development slice with responsive presentation and audiovisual refinements,
cohesive painted-world studies, and a local AI landfall workflow. Ordinary Land completes its
durable gameplay transaction before the separate painting queue begins; Survey and Notifications
show actual progress, cancel and explicit View. Exact originals are retained independently and
can be restored after reload without generation. Model installation is explicit, resumable and
hash-verified, with bounded source-backed canonical species conditioning.

The local AI route is an Earth-only development preview. Both current paintings fail species,
botany and approved-quality acceptance. The 6.69 GB model, shipping runtime pack, physical phone
delivery/inference, small-screen art legibility and full admission gates remain unqualified.
No model weights are added to the game pack. Existing saves, deterministic identities and approved
base UI placement remain protected. All failed candidates and first verification failures remain
in the committed audit packets.

Validation: source-bound focused owner tests, all three TypeScript programs and root validation;
native small-file OPFS cancel/resume/hash/readback; actual-game WebGPU Land/progress/Notifications/
View/original-retention/reload/settled resize. These scoped results do not replace exact-head
Compendium/Slice/Glass or physical-device acceptance. See ROADMAP and the AI integration packet
for exact receipts, prior accumulated changes and blockers.

This draft does not synchronize Claude's workspace until a separately admitted develop merge.
No release, deployment, version bump or hosted test attempt is included.
```

Other side: Claude has not received these local changes. Nick need not open Claude while this
coding batch is being finalized; the committed packet is ready for the intended review when he
wants it. Do not copy files manually. Only after a verified develop merge may clean `anthropic/mac`
fetch/merge origin/develop; finish or commit dirty Claude work first.

Release status: develop/main/live site unchanged. Actions budget UNFROZEN/PUBLIC, private fallback
3,000, zero new exact owner-label attempts. No push, Ready, label, Actions, merge, release/deploy,
automation or scheduled prompt occurred in this batch.
