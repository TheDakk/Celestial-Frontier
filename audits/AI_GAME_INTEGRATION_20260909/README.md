# Species fidelity, browser delivery and normal-game local AI

September 9, 2026. Nick requested these three priorities; his intended Claude-app review remains
pending. This packet records implemented source and measured results separately from acceptance.
All local candidates retain `qualityAccepted:false`; no phone or shipped model is qualified.

The compiler preserves all 19 canonical Earth genomes and maps six named residents to source-backed
anatomy/botany and composition anchors. Its initial prompt used 376/512 tokens; the current explicit three-body clarification uses 472/512
with the exact pinned tokenizer/chat wrapper. Both actual outputs still fail species quality. [Reference review](PLATYPUS_REFERENCE_REVIEW.md) and [binding](platypus-reference-binding.json)
preserve the original Platypus PNG and all 29 identity fields. The built-in authoring generator made
this reference; the actual local model must independently demonstrate scene/species quality.

The ordinary game Land path now creates a page-owned job after durable landing publication.
Survey and Notifications report actual ordered progress/ETA, cancel and explicit View. The native
PNG commits atomically in a separate original store and is reread/rehashed before Ready. No game
save/schema/reward/genome change. Navigation cannot cancel the job or auto-return at completion;
a stale View request and stale decoded bitmap refuse publication. Visible prior art remains owned
until a successor has mounted. Original pixels stay full 1024×576 and are contained in the existing
DOM scene band.

Browser model delivery now has real OPFS/WebLocks/Range resume/hash/readback/atomic readiness.
[Separate native delivery packet](../LOCAL_MODEL_DELIVERY_20260909/README.md) passes with 2 MiB of
explicit synthetic files,five trusted clicks and eight observations. This is not full 6.23 GiB / phone qualification.
The real-game runner uses the existing fully verified local developer model cache, not a download,
synthetic workers or an inference server. Production runtime/model distribution is still open.

## Focused evidence and retained first results

- Compiler: 16 PASS and exact tokenizer 376/512 PASS; initial guessed private Rolldown module path
  failed before compilation/testing, corrected using public exports. Both records retained.
- Delivery/hash: 32 PASS; first 31 PASS / one fixture FAIL retained. Corrected test explicitly establishes
  the saved prefix/Range request before its rejection control. Delivery source did not change.
- Original-store/job owners: 15 PASS after initial 11 PASS / two staging fixture FAIL; changed tests await
  actual transaction staging. 13 then 15 PASS; immutable hashes, abort, late cancel, readback controls.
- Runtime: 20 distinct cases: 19 PASS / one progress fixture FAIL, then the corrected sole case PASS.
  Failed expectation omitted legitimate loaded-stage progress; all runtime bytes unchanged. Its
  doubles cover reference hash/binding, shape/transfers, ordered progress, cancellation and cleanup.
- Main wiring/layout: 66 PASS after the retained 56 PASS / three FAIL / one import FAIL / three unhandled fixture errors.
  Fixtures required the new variant global and changed layout signature. New native aspect case
  verifies 1024×576 containment; original negative/ownership controls remain.
- Controller/expanded real-main ownership: 23 PASS: nine controller + fourteen mount cases. Includes preserved
  prior art on early/late failure and failed lease, stale View, storage/inference exclusion, and a
  premature-retirement mutant rejected by the same outcome assertion.
- Preview server: eight HTTP fixture PASS, not Vite/native/model proof.
- Final TypeScript first stopped on a test-only overly broad mock constructor type. Exact awaited
  fake return type corrected it. All three TypeScript programs and root validation subsequently PASS: 1,010 renders / zero errors /
  50 unchanged v1 fingerprints. Direct logs/commands in validation-01/02.json. No full profile rerun.
- After native02, the model-selection Retry guard passed eleven focused controller cases on its
  first execution: failed/canceled recipes refuse after switching models, while ordinary Retry
  retains successfully. See [guard receipt](retry-guard-01/result.json) and
  [final TypeScript/root validation](validation-retry-guard.json). This is a later source boundary,
  not an extension of native02 or a new full admission result.

[Native02](native-game-02/result.json) passes the actual game flow at 74.307 s, with original retain/
View/reload/settled resize and no reinference. The [native review](NATIVE_REVIEW.md) explicitly
rejects both paintings and flags tiny small-screen art despite containment. Native01 remains
aggregate FAIL at its premature resize sample; no result is overwritten. [Comparison](NATIVE_COMPARISON.json).
The real game native receipts and unmodified originals own inference and visual outcomes. Earlier 4,121 tests or prototype passes do not certify this later head. Preserve all
predecessor blockers in ROADMAP/ARCHIVE and the earlier immutable audit packets.

No GitHub writes, Ready/labels/Actions/merge/release/deploy, automations or schedules. Local signing
is required for the checkpoint containing this packet; the paired handoff records the verified
ownership and remaining integration steps. See [current owner reference](../../LOCAL_AI_GENERATION.md).
