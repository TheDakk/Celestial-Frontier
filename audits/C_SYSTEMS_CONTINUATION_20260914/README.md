# Independent C-system continuation — September 14

This is the earlier batch receipt. Current status and one consolidated review prompt are in
[the cross-package audit](../CROSS_PACKAGE_PROGRESS_20260914/README.md); later C2 seam passes and
sound/arena intake supersede the pending technical claims below. Earlier evidence is retained.

Nick requested continued local coding while Claude review is deferred. Starting signed
checkpoint: f36cb38d. This follows C_LANE_BATCH_REVIEW_20260913 and C_LANE_REPAIRS_20260914;
old failed receipts are historical evidence, not overwritten results.

## Completed technical work

| Area | Change | Evidence and negative control |
| --- | --- | --- |
| C3 preparation | Offline voice-set WAV → Opus export, new output only, fixed ordering/metadata/serial, recipe and output hashes | Real FFmpeg/libopus tests: repeat bytes, exact decoded frame count, original bytes intact; sample-safe intersample clipping and changed manifest refuse |
| C3 measurement | Source and lossy output both measured, <= -1 dBTP; channel/rate/duration/byte policy | Missing metrics, encoded peak overshoot, wrong channels/duration and existing output refuse |
| C4 preparation | Ordered complete batch of one to twelve hash-bound masters | Existing first-batch twelve originals inspected unchanged; missing/reordered/duplicate/changed/oversized inventory controls |
| P1 hashing | Native asynchronous SHA-256 for ordinary painting/composite and retained original verification; yielding fallback; one identity key per store operation | Independent SHA-256 parity, altered bytes, native failure, source mutation during yield, oversized input and lying Blob controls |
| Acceptance test | Wait for ordinary-game ready/failed outcome rather than assuming two event-loop turns complete native hashing | Exact accepted E bytes retained without inference; changed PNG refuses retention and inference |
| Producer binding | Refresh current producer for new index/service-worker bundle | authority-refresh.json proves fixed measurement/ruler/ceilings/samples/browser policy preserved; independent live binding test |

C3 tests generate private technical tones and delete them. They are not quadruped recordings,
kit sounds, or assets for listening. Original sources/rights, short-term mix loudness,
headphone/phone listening, derived voices and arena playback remain unfulfilled. Integrated
LUFS is never substituted for short-term LUFS. The bed byte profile does not complete a
rain-bed source or loop gate; only the voice-set manifest adapter exists. Codec determinism
is scoped to this installed FFmpeg 9.0.1/libopus toolchain, not across codec versions. Codec
integration tests explicitly skip absent authoring tools, but fail a present broken tool;
this Mac ran every codec test without skipping.

C4's plan is a readback inventory in the earlier audit order, not a new compiler system card,
generator taxonomy or library painting order. `qualityAccepted: false` describes technical
intake's inability to grant acceptance; existing human acceptances stand.

PNG hashing preserves schema, IDs, limits and retention policy. It reads a bounded buffer
(up to the existing 16 MiB cap); fallback takes an additional copy before yielding. Native
crypto implementation memory is not measured. This replaces synchronous JS hashing at the
cost of extra temporary memory; no browser/phone frame-time or peak-memory claim is made.

## Verification and remaining gates

`checks.json` binds the raw compressed check logs. Full v2 suite and typecheck, original
master readback and root validation are recorded separately. No native certificate or
performance run is inferred. SceneMemory remains production-only/quarantined with its
existing stale authority; its binding was not refreshed to hide that state.

C2 remains stopped at head--ear-far: the retained-frame cut diagnostic has three fully
transparent pixels at each stride extreme. Zero-gap/rest/band/cap constraints remain.
No new capture, recording, painting, finisher run, kit edit or Claude-owned module edit.
No main.ts hunk. E stays active; the second weather pick and smaller-phone-finisher decision
remain open. C3/C4 package acceptance and C5 promotion prerequisites are not complete.

GitHub step: none. PR42 parked. No new branch or history rewrite. Codex may continue local
independent code; Claude has no action now. When Nick resumes review, use REVIEW_PROMPT.md
for one combined response. No need to open the other app or sync branches yet.
