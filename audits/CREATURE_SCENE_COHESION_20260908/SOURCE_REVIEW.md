# Source and instrument review

Read-only independent review of cohesion-owner.ts, study-harness.ts and study-runner.mjs preceded the first native run. Root corrected three concrete gaps before native evidence: broad body shadow could satisfy missing paw checks; a missing declared paw could evade the census; and native checkbox state could change without painted output. The final rulers compare Civet contacts against a body-only reference, require the exact four ordered paw IDs, and capture the already-painted native canvas before/off/restored without calling render/extract in the screenshot helper. Trusted checkbox events are recorded. An identical-pixel negative control fails that same toggle acceptor.

Native-first then exposed the legacy zero-alpha instrument error; FIRST_NATIVE_FAILURE.md records the diagnosis and changed input. Source copies in both native output folders remain immutable. The corrected ruler observes translucent ground below the existing solid-ink threshold for legacy residents; it still requires fully exposed ground for Civet. Each declared contact must darken actual nonempty pixels, and the original missing/shifted shadow, overstrong light and full-background grade controls must fail.

The compositor admits the full immutable canonical Earth plan, complete genomes and exact source placement. It borrows the existing background/resident/Civet objects and decoded textures, adds one owned filter and 16 Graphics (15 contacts + one ambient body), and creates no texture or animation loop. Its one-shot CPU reads use bounded scratch canvases reset to 1×1. Scene lighting changes RGB only. Disposal restores original filter identities, layer order and living borrowed resources before base study retirement. Native proof verifies those objects and unchanged baseline pixels, then separately retires all study meshes, geometry, textures, sources and bitmaps. Filter pool/driver heap is not measured by these ownership facts.

Static-first, static-reviewed and static-final PASS belong to their exact intermediate reviewed inputs; static-ground-ruler PASS binds the source used by the final native run. The original rig's 113-test PASS and all-three-V2-typecheck PASS are retained unchanged in the first packet. No full battery is implied by isolated study checks.

Final read-only review rehashed all nine bound source inputs, 17 built files and 178 report-linked encoded images. Fourteen isolated offline negative controls reused the exact native cohesion acceptor (seven per mode): omitted paw declaration, measured excessive lighting with valid contact receipts, empty lighting/background/contact rulers, changed alpha row and lost borrowed source. Positive native receipts passed before and after. This separately validates the lighting threshold because the full native strong-light mutant correctly stopped earlier at its legacy-contact guard. See final-results.json; no native rerun or source change was used for this review.
