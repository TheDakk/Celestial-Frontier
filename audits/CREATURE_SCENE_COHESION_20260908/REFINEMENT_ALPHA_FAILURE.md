# Reference refinement transparency failure

The single reference-led edit visibly refined the Civet material/lighting and kept its pose,
but returned1536×1024 RGB (PNG color type2), with another baked checkerboard rather than alpha.
The original and exact prompt are retained. This is an image-output failure, not accepted alpha.
Use Nick’s existing explicit ImageMagick extraction authority for one bounded recovery/inspection;
retain the prior completed study and do not silently replace it.
