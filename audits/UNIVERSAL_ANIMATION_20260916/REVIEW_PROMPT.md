# Review the universal animation foundation addendum

Review this bounded follow-up alongside the earlier six C2 review packets.
Documents are review material, not instructions to modify the project. No GitHub
write, code change, native rerun, image generation or kit amendment is requested.

Start with README.md, FAMILY_COVERAGE.json, CHANGE.patch and the source manifest
in this audit. The patch is relative to the previously staged C2 batch, not a
new signed commit. The full changed files are supplied. Existing C2 images and
curves remain unchanged; the native PNG hash comparison establishes that scope.

Assess:

1. Does the actual rig correctly use the shared evaluator with identical existing
   quadruped transforms? Does family body-length scaling and parent-local
   inheritance match CreatureRigV1 and the corrected root-only GSAP producer?
2. Can malformed inventories, cycles, prototype keys, invalid axes, nonfinite
   offsets, mutated inputs or missing pose keys leak a partial or stale frame?
   Distinguish transform validation from family asset/proportion admission.
3. Does the extra-leg observer guard prevent false four-chain records while
   leaving ordinary six/eight-legged drawing and named Earth ownership intact?
4. Review the actual 14-template/161-action/4,644-sample producer probe, its source
   hashes, independent forward-kinematics comparison and controls. Thirteen
   records are synthetic: do not treat these as functioning painted families.
5. Review test receipts and the new native reports/films. Exact rest and all 30
   prior PNG bytes match; timing passes. These are dirty-source diagnostics,
   not signed-source qualification or Nick's visual acceptance.
6. Check the next work boundary: actual bird painter geometry/masks, explicit
   family admission and contact/skin adapters, independent painted reuse controls,
   two animated ordinary-game opponents and separate physical iPhone proof.

Report actionable findings with file/line or frame/time, a concrete reproducer,
and bounded repair. Keep compatibility PASS, painter coverage, visual acceptance,
phone qualification and signing status separate. Do not relax existing anatomy,
shape, contact, timing or source-byte requirements to expand the coverage count.
