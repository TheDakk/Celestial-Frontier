# P1 animation: bounded CPU measurement and root-continuity split

Authority: Nick's September20 direction and the read-only sibling
`audits/VISION_PROGRAM_20260920/P1_ANIMATION_REVIEW.md`. Accepted `hidden-01`,
masterIntakeAccepted, original1254-square RGBA, all masks, landmarks, hidden contract,
raw clips and raw limits stand unchanged. No intake/rigs for the four new paintings.

## CPU measurement in progress

The accepted56px interior /24px boundary binding is the retained native02 baseline.
New candidates use80,128,256px interior; boundaryStep remains24. Only the interior
mesh build setting changes, followed by the current shared observed surface split.
The exact accepted parts directory is copied into isolated measurement inputs; the
accepted fit04 remains untouched. Node26.9.0 and the uninterrupted September19
single-run toolchain receipt are reused. All build/native work uses the shared lock.

| interiorStep | Field vertices | Native evidence |
|---|---:|---|
|56|3838|Retained hidden-01/native-02; baseline.json|
|80|3791|Signed producer890b3f5f; native measurement pending|
|128|3791|Byte-identical binding to80; will share that measured run|
|256|3787|Signed producer890b3f5f; native measurement pending|

The adaptive compiler subdivides any cell containing alpha boundaries or part
ownership cuts at boundaryStep. Interior-only coarsening therefore removes at most
51vertices (1.33%) in this bounded set. No new CPU threshold, selected production
binding or painted-tier admission follows. Nick decides from the completed table.
`candidate-identities.json` proves the80/128 alias; no unchanged native retry.

Build syntax and source diff checks pass. Root `validate.log` passes, including the
unchanged50-probe v1.0 fingerprint. Candidate record/mask/atlas/part byte integrity
is retained in `input-integrity.json`.

Initial signed-commit attempt refused by the1Password agent; subsequently resolved
by signed/verified producer890b3f5f. Four paintings are retained. Latest Nick direction
prioritizes their no-hidden intake before CPU decision; current partial intake and
three unresolved chain locations are indexed in
`../VISION_P1_FOUR_CRABS_20260920/intake-01/README.md`. Native timing and the root
ledger/guard remain pending; no runtime guard or CPU-gate change.

No fetch, push, PR, merge, release or deploy. PR42 and roster hold. Codex completes
this measurement packet locally; Claude reads it without copying/syncing.
