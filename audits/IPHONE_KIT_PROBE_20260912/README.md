# Physical iPhone probe — September12,2026

Target supplied by Nick: iPhone17Pro, iOS26.6.2, USB-C, Web Inspector enabled.
Safari first refused the locked device, then required Remote Automation. Nick unlocked
and enabled it. The third session succeeded: physical iPhone (simulator=false),
iOS26.6.2 build23G90, Safari26.6.1. Device identifiers redacted from committed receipts;
active session connection is in a private temporary file. No model attempt yet.

The next source-bound probe uses existing accepted recipe/inputs and shipped kit worker,
a temporary HTTPS origin on the observed Mac LAN address, and the verified parent cache.
It queries maxBufferSize,shader-f16,storage quota, then one unchanged warm finisher if
required features exist. Session loads already precede warm timing. Transformer-stage
memory is retained only if exposed; JS heap is not native/GPU memory. No installer,
OPFS derivative, new delivery tier, parameter change or creative painting.

Physical-device admission has wrong-Mac/iPad/version/simulator/missing-ID negative
controls. Actual network/secure-origin/model capability remain to be measured.
Node syntax, admission controls and root validation pass. No unit checkout lock.
The Safari session is already open at local driver port49763. Run the one prepared
probe after signed source, using private session/TLS files under /private/tmp, observed
host192.168.1.62, output native-01. Do not retry a model attempt automatically.
Report phone results before the Mac installed-model run or any delivery work.
No GitHub activity; PR42 parked. Weather/mat and Civet remain ordered after the Mac run.
