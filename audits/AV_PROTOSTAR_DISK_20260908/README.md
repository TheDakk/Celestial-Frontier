# Protostar dust-disk restoration — September 8, 2026

The live V2 system renderer previously sent PROTO through the ordinary stellar corona and
close-surface path. This batch restores the original game's distinct protostellar dust disk
and warm core. `system-protostar.ts` bakes the canonical .4-radian tilt, .34 vertical disk
scale, radial14→70 dust gradient and radius30 warm core into one fresh420×240 transparent
canvas (100,800 pixels /403,200 raw RGBA bytes), displayed at140×80 world units. The explicit
PROTO branch mounts it with the existing sceneTexture owner; sysStar stays null so zooming
does not add an ordinary stellar surface. No new ticker or retained cache family exists.
Reduced Motion and Effects Off retain this static class identity. No seed, game, save,
protected portrait, audio asset, version or hosted action changed.

The fixed three backing pixels per world unit are a bounded presentation choice, not an
infinite-resolution promise. Phone/desktop close-up captures and independent canonical pixel
comparison determine the scoped result; physical device/human art acceptance remains open.

## Verification

`preparation.json` is PASS: the existing83-bullet draft digest refresh, one evidence producer
build, producer-metadata refresh, complete browser-free develop profile and root validation
all passed in sequence. Develop:329 files /3646 tests /one skip, all3 TypeScript programs,
art/override/spec checks. Root:1010 rendered species and50 unchanged deterministic fingerprints.
No redundant new unit assertion merely mirrors the gradient commands; the native diagnostic
compares the actual canvas against independently extracted original renderer bytes and an
omitted-disk negative control, plus actual scene pixels and resource retirement.
Native outcome: PASS on the first run, Edge152.0.4191.66/CDP1.3. Four actual390×844@2 /
1440×1000@1 screenshots inspected. Source-bound Survey→Enter, native wheel zoom, Reduced/
Effects Off and NS/Sol absence controls pass. Actual canvas RGBA exactly matches the independent
legacy recipe (SHAaa07ee58d0cd76ff9a65a7af5e6007eaee2dab7d56b0c6e83f08ab618ad8a0cf);
omitting the disk changes160254 channels. Hiding only the actual scene sprite changes130804
channels; restoring it returns every pixel exactly. Deep desktop zoom exceeds the old ordinary
star-surface threshold while zero star-surface leases exist. Native exit destroys Sprite, Texture
and TextureSource, clears source.resource, retires the old scope and releases all3 scene leases/
textures with exact registry balance. The diagnostic-held CPU canvas need not shrink; resource
detachment and disposal are checked independently. Zero runtime/cleanup faults; browser and
temporary server closed. These are scoped diagnostics, not human/device acceptance or certification.

Current producer ff1d6051b34b2f2b7027b7f99e45a9d5ee692f5b0568f7133b7321d7a01c3b62;
83-bullet draft SHA0115e324858dc0b88acc25d6eaffba532fb06d13bd638e776d2280a636c9b618.
Measurement4a93479b62b032155a4825bde6425ebd430ccb286979dc69e90064bb3c7f5e12,
ruler and numeric ceilings remain unchanged. No current Compendium/Slice/Glass certificate;
SceneMemory remains production-quarantined. Every earlier failure and certificate is retained.

The fixture is the source-proven home-galaxy PROTO at seed412604657, raw coordinates
(-715.1403196742758,-157.66362113971263), CF1-normalized (-715.14,-157.66). It uses the
existing isolated veteran fixture plus an owned Long-Range Array; chapter2 alone does not
grant home-galaxy travel. No personal save or natural progression claim is involved.
Native input owns Survey→Enter, wheel close-up and exit; the existing evidence import only
prepares isolated source-derived fixtures. Sol and NS are absence controls.

## Review and checkpoint

Review the explicit class branch, bounded canvas ownership, original-recipe comparison,
static accessibility behavior and scoped native captures. This is a small canonical visual
restoration, not new whole-universe material production or accepted anatomical animation.
The next useful audio candidate is recorded separately in `NEXT_AUDIO.md`; it is unimplemented.
The older MAG capture's singular `1 worlds` caption is a pre-existing copy issue, recorded
for a future UI text correction; it does not change this graphics result.

Current parent is signed837db4aaa0ef5d3d8bffc79c70f62dcc2503032d on openai/mac,38 ahead/0behind.
The previously staged299-file MAG checkpoint and its exact recovery snapshot remain intact.
Current changed source/references supersede their old hashes; the old manifest is never rebound.
Signing still awaits1Password restoration; no unchanged retry or unsigned fallback was used.
The live ordinary local preview remains the immutable earlier landing/battle package and lacks
both MAG/PROTO increments. Current evidence builds are local and not publishable packages.
No dev site, develop/main branch, release, PR, push or hosted workflow changed.
