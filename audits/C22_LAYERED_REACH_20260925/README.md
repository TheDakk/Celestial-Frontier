# C22 — cheaper layered reach

All22 measured diagnostic admissions match the old helper. The shared grid search
restricts its candidate on a failing pose instead of repeating full scans at
every bisection. It retains121×32composite poses, zero-displacement admission,
the8-bit displacement lattice, cap/reserve and unchanged contact solver. Earlier
poses are checked again at the final candidate. A restriction during that check
invalidates the entire prior pass, including its suffix. Termination follows
from a strictly decreasing positive integer candidate; no monotonicity assumption
permits an unchecked pose. This remains a sampled envelope, not a continuum proof.

comparison-01 retains the first candidate and baseline. Candidate01 regressed
full-cap cases by doing a redundant full scan; rejected. comparison-02 retains
the changed prefix-only verification candidate, with baseline timings explicitly
reused from01. Centipede2878.32→1175.41ms, Crab1623.72→698.95, Civet886.11→482.20,
observedImpala2940.87→1065.58. Single desktop startup diagnostics, not a repeated
benchmark or phone/frame/60fps certificate. VentCrab467.54→472.70ms; no universal
speedup claim. Complete source hashes accompany both runs. Final runtime extracts
the same candidate02 search into a directly testable helper.

Six targeted tests pass. Positive non-monotone control passes; removing prefix
revalidation or retaining a stale suffix each fails with unsafe4/2 instead of1.
App TypeScript and root validate pass. Full develop:5120pass,1failed(I5 current
producer),2expectedfail,2skipped.484filespass,1failed,1skipped. Profile stops before
later owners; no overall-green claim. All93inputs of the last measured S2 run
remain byte-identical, so no unchanged S2 battery repeated. No accepted binding,
family/solver limit, historical sample or budget changed.

The first edit command used the wrong working directory and changed nothing;
targeted-01 consequently covered only the existing3tests. Corrected edit plus
new controls is targeted-02(6tests). No missing-test success is claimed.

Claude: consume the signed reach helper with existing cache/cap/reserve. Codex:
continue C23 loader, C24 root types, C25 actual-source films, then §20 balance.
Nick need not relay. C8 parked; no hosted attempt, label, release or deploy.
