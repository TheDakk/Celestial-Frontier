# Facing and two-combatant study — September 16, 2026

Nick's review: the accepted Civet painting looks backward and the Platypus is static. Combatants must face one another, with mobile heads and articulated action/reaction.

## Changes actually exercised

- New hash-bound Platypus record uses its actual canonical Earth identity and accepted master. Seventeen painted parts in one pinned-packer atlas (816×2047), continuous surface, three visible webbed feet. The occluded hind leg has a declared kinematic chain but no invented paint; internal ear landmarks produce no external ear parts. All packed source channels match. The original master is untouched.
- The shared turn sampler/contact owner now supports the right participant. Its own idle continues across both roles and hitstops; support releases during its own attack, not the other actor's attack. Both participants use the unchanged read-only producer and Motion Kit curves. This is diagnostic tool wiring, not ordinary-game promotion.
- Civet profile/front head copies come from the existing turnaround, bound to its SHA-256 and the original record. The battle profile reflects that real side view to face right, tracks the opponent in the available plane and joins the chest through source-painted neck geometry. It does not synthesize hidden anatomy. Front-view intake exists but is **not** used in these films.
- The head replacement is a single adaptive surface. Separate ear/jaw articulation and transitions between head views are **not yet implemented**. Its neck/shoulder silhouette is visibly rough and remains a visual fitting issue. Do not call this completed free head turning or promote it as final C2 acceptance.

## Evidence and controls

`native-01/report.json` refuses the first Platypus declaration: insufficient rest slack in nearly straight chains. `platypus-02` bends those joint landmarks within painted legs; all four root/knee/ankle chains exceed 3% body-length rest slack. The 8% contact compression bound is unchanged. `platypus-01` is retained as a failing control.

`native-02` is the first two-rig film. Visual inspection found a rigid replacement-head/neck gap. `native-03` extends the source neck and deforms its lower attachment with the chest. `native-04` increases the profile fit and suppresses the old neck part so both necks are not drawn. **Its shoulder transition still needs visual fitting.** Earlier films remain as diagnosed history, not accepted versions.

`native-04/report.json` passes 601 poses per pairing (1,803 total), rejecting body-surface failures and flipped head triangles. It records nonzero opponent head, bill/jaw, tail and leg motion. This is not a pixel seam oracle or anatomical beauty judgment. Three ten-second clips run at about 60 fps on this Mac. Whole-frame CPU p95 is 2.6/2.4/1.8 ms for Civet/fox/procedural pairings. The legacy per-creature timing field covers the selected left rig; **do not relabel it as the cost of both rigs or claim independent <2 ms qualification for the new head and Platypus**.

`tool-tests.txt`: shared left/right role ownership, exact existing left-side pose exports, continuous support, actual named-master/record/atlas admission, corrupt record/hash controls, old straight-leg control, binding mutation, chest-attached head geometry and reflected-triangle flip/collapse rejection. `focused-tests.txt`: existing aim/contact/performance tests. `game-typecheck.txt` and `root-validate.txt`: game TypeScript and root validation; deterministic baseline remains unchanged.

All captures are dirty-source `DIAGNOSTIC_REVIEW` with source hashes. No signed-source qualification, original-art acceptance, phone qualification, kit edits, model run, Claude-owned edit or main.ts hunk. Existing signing/ownership questions remain pending. HEAD remains `1a6dd61d`; work is uncommitted.

## Review

Latest films: `native-04/civet-10s.webm`, `fox-10s.webm`, `procedural-10s.webm`. Local MP4 viewing copies and the comparison player are at `/private/tmp/cf-animation-preview-20260916/facing/`; URL `http://127.0.0.1:49816/facing/`. Those temporary copies are not new performance measurements. Previous preview is retained at the parent URL.

Next bounded work: fit the Civet source neck to the actual shoulder silhouette, add source-backed ear/jaw subdivisions and view-transition controls, qualify actual rendered contacts/joins for the Platypus, then obtain clean-source evidence after Git signing is available. Do not infer all-family turning coverage from this authored head. The same view contract needs painter-emitted source coverage for procedural creatures.
