# C11 — Weekly Conquest settlement, verification and pause handoff

Codex/macOS, openai/mac, following signed C10 45e0bd10d21633ba51a971f593268a7e970e24bc. The weekly pool now offers wk-conq (one post-acceptance conquest; 30 Stardust), and Arc 6 settles it inside the existing F4 receipt/CAS using that transaction's activePlayMs. Both old blanket refusals are removed. No extra writer, random draw, retry, wall clock, version bump or main.ts hunk was added.

The pure weekly lifecycle moved from the app into packages/persistence/src/weekly-charters.ts; the app keeps its rendering and re-exports the shared API. Persistence declares its already-installed workspace domain-rand dependency in package.json and lock, without downloading/upgrading a dependency. The conquest verifier binds saved chWeek, chacc, chProg, Stardust and honored-Charter counters to the registered transaction and measured reward delta. Conquest refuses malformed completed/accepted progress, duplicate acceptance, missing trade prerequisites and capacity overflow. Expired acceptance yields no Charter reward. Starter completion and Chapter progression remain under their existing owners.

Validation: 63 tests across persistence combat settlement, Starter/Weekly Charters and main-wiring contracts PASS. These exercise the actual app→save→reload result, one-payment/re-acceptance refusal, duplicate receipt, expired acceptance, mutated reload projections and counter saturation. Three TypeScript owners PASS. Root validate PASS: no boot/render errors, 50-probe fingerprint identical to baseline. Initial failures retained: stale structural refusal expectation, a wrong test result property, and a duplicate-acceptance fixture that the codec correctly deduplicated before the transaction. The duplicate negative control now exercises the pure stager directly; completed-progress/capacity controls still pass through persistence. No native film or certificate was needed or run for this transaction change.

Current v1 budget SHA-256 remains c109b5845b12862bbd8fa069b6f8e394b48d9d8568d8537f20565b63c5d907fc, historical samples untouched. The earlier integration battery remains red on its sole historical I5 producer-authority assertion; this packet does not claim a full develop PASS or PR43 certification. No push, label, hosted run, release or deployment.

Nick explicitly requested pause after this item to preserve usage. Codex is PAUSED after signing this packet. Claude can consume the signed 0e3225b7 sentinel reconciliation, 45e0bd10 release note, and this conquest commit from the shared object store; the playtest behavior changes, so Claude may republish under its existing D9 direction after merging/verification. Nick need not relay anything or open another app.

Resume: read both protocol and Claude mailbox. Finish C8's v2 painted/broker observation and negative controls before consuming any new epoch. Current painted ownership diagnostics lack per-kind encoded byte totals required to preserve the portrait counter; residentArchetypes.bytes currently counts master+labels, while the mask cache needs explicit accounting. Resolve those facts before claiming complete resource observation. The b4f191c3 epoch remains stopped, never resumed. Then calibrate three times and certify once on the exact current signed Claude head, no rebinding and no retries. C12 hot path is known (orientation active kernel); no optimization or phone budget change was made. C13 pin proposal was read but its admission review/implementation remains pending. D1 painting order remains pending; no new artwork in this batch.
