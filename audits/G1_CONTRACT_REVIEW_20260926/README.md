# G1 contract follow-up — 2026-09-26

Read-only review of signed Claude head `77623d3fdd263f5e852ce162356ee43a06976a73` (signature **G**), including adopted-v5 commit `fd37bfd6`. Source and retained reports were read through `git show`; no corpus, test, build, browser or mutation job ran. The line references below are relative to that exact head, not a moving worktree. This revisits `audits/G_PIPELINE_CODEX_20260926/G1_REVIEW.md`; it does not approve D24, change an acceptance denominator, or authorize an automatic packet for play.

Claude reports **12/40** corpus ADMIT + PASS_STATIC and **10/20** G2 pilot ADMIT + PASS_STATIC. These are retained reported results, not independent measurements by this review. G1 remains below its existing >=30 requirement. Native, finish and human acceptance remain separate.

| Prior finding | Disposition and evidence |
|---|---|
| Independent presence inventory | **Improved, not fully closed.** `auto-author.mjs:325–329` runs a paint-only counter before part nudges; `:353–358` refuses when that inventory is absent. But an existing counter result is not proof that every named limb is independently resolved: `limb-counter.mjs:120–121` disables `missing`, `merged` and `coveredExtra`, while `:157–159` uses the lowest same-family ground-contact count. The emitted presence still has all three exclusion lists empty. `run-auto.mjs:107` calls every ADMIT “all-visible, backed by the measured visible inventory.” That claim remains too strong for unresolved overlaps. Retained `mutants/summary-v4.json` reports erased 31/34 and duplicated 26/27 refusals; its erased Eagle, Reef Shark and Herring, plus duplicated Heron, still ADMIT. This review does not ask to enable ineffective options blindly or weaken other thresholds: retain the measured geometric inventory and refuse unresolved semantic presence before calling it an all-visible attestation. |
| Corpus ID / target authoring boundary | **ID selection fixed.** `run-auto.mjs:91` passes `id: s.id`; `:81` excludes the target id and species from references. Independent G2 targets explicitly have `authoring: null` (`:58–64`). The corpus evaluator loads target authoring for later scoring (`:55–56`, `:110`), but does not pass it into automatic authoring. **Identity consistency remains incomplete:** `:85–89` checks family equality, profile existence, an integer seed and a visualKey string of length >5. It never recomputes the visualKey or verifies the genome/name/seed against canonical species data. Thus provenance `identity.checks: "PASS"` (`:102`; retained Wolf provenance lines 9–14) means shape checks, not the canonical agreement requested in the first review. The unchanged intake likewise only checks seed type and visualKey length (`intake-authored.mjs:42–45`). |
| Species materials | **Original family-default problem fixed for the reviewed examples.** `run-auto.mjs:30–42` selects from the pinned profile group: lizard→scales; salamander/eel/frog→smooth skin; unknown group refuses (`:90`). The envelope labels this biological metadata, explicitly not painted observation or finisher permission (`:105`). This is a hand-maintained group mapping, not independent observation of the pixels. The older README paragraph at line 114 still says “family default table”; refresh that reference sentence. |
| Honest automatic provenance | **Fixed for adopted topK=1.** `run-auto.mjs:94–107` records automatic transfer, reference master/authoring hashes, target and subject-source hashes, habitat profile hash, material source and visible inventory; it explicitly disavows intake's unconditional legacy manual/no-reuse flags. Retained `auto-v5/wolf/provenance.json:5–19` contains this envelope. `auto-author.mjs:357` now accurately describes the single registered reference. Intake remains unchanged. The identity and presence wording qualifications above still apply. |
| 38 vs 40 / habitat semantics | **Explained, not a changed gate.** README lines 86–92 reconcile 38 shipped archetypes with 40 hand-authored subjects: intersection 32, eight extras named, six shipped-only entries described as five crabs + Civet. It reports 10 admitted/static passes on the shipped-32 subset. Keep the six unevaluable shipped entries explicit; 10/32 must not silently replace a requested 38 denominator or the program gate. For a fully reproducible subset, retain an exact per-ID 38-row manifest/status list (including the five crab identities), rather than only the grouped prose. Habitat wording is fixed: `run-auto.mjs:43–48` distinguishes biological capability from pose and keeps adult flight separate; `:104` binds profile id/media/hash. |

## Honeybee classifier side finding

Confirmed by source inspection, without running or changing the classifier. The retained hand packet `audits/ART_BATTLE_FOCUS_20260925/28-honeybee/authoring.json:92` says `chitinous shell with short thoracic fur and transparent veined wings`. `port/v2/apps/game/src/motion/secondary.ts:48` returns `furred` on `/fur/` before its chitin test at line 51. Thus that mixed-material hand string selects the furred class. This is separate from the new automatic group mapping, which emits `chitin`. Repair ownership stays with the main lane; changing precedence globally needs an inventory of other mixed strings and focused outcome checks.

No source, tests, thresholds, existing evidence, Claude mailbox or Claude worktree were edited. This note is the only new file from the bounded review.
