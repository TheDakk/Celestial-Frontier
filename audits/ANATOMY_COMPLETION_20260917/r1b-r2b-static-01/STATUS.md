# R1b/R2b prepared correction — pending N1 bound decision

September 17, 2026. HEAD remains signed evidence commit `57dfe112cf5a584193afa17ae91d779da694f0e7`; implementation changes below are local and unstaged. No new source commit or native capture has run. R3–R8 remain closed. PR42 remains parked; no GitHub operation, sync, history/LFS operation or other-lane edit.

## Authority and current decision

Nick pointed Codex to Claude's R1/R2 direction block and explicitly approved Q4. The review was read by absolute path, read-only, without copying it into this worktree: `/Users/nick/Projects/celestial-frontier-anthropic-mac/audits/ANATOMY_REVIEW_20260917/CLAUDE_R1R2_REVIEW.md`. Its hash is in `direction-receipt.json`.

Only N1–N4, one N6 Civet sentinel and the approved Q4 paragraph are in scope. The exact proposed Q4 paragraph has been applied to Motion Kit §8; other kit wording is unchanged. Neither this edit nor Q4 opens R8.

**N1 ambiguity requiring Nick:** the direction both preserves the body-axis bound and raises its floor so the real Crab's 0.041573 root→carapace pivot distance refuses. A new motion scale alone does not repair admission under that literal requirement. Changing genuine pivots merely to pass a floor would invent anatomy. Recommended interpretation: preserve observed pivots and existing body-axis bounds, and add an independently named `motion-scale` span bound with minimum **0.08**, maximum **0.9**, measured between template-declared `leg0FarRoot` and `leg0NearRoot`. Current Crab's span is **0.093745**; an erroneous 0.041573 scale would refuse. Other current crab spans are 0.1015–0.1435. The alternative is to raise the old body-axis floor and explicitly refuse incompatible records. The question is pending; neither bound interpretation has been silently applied.

This is a substantive contract interpretation, not a generic request to continue. Once Nick chooses, finish that exact bound and its passing/failing controls, run affected checks, sign and verify source, run the eight subjects plus Civet sentinel against that unchanged signed producer, then stop for his review. Do not infer approval from elapsed time.

## Work already prepared

- **N1 partial:** shared `motion-scale.mjs` declares brachyuran leg-root span and plant longest observed root-to-tip chain. Other templates retain their declared body axis. The BodyCard exposes `scaleLength` and `scaleReference`; amplitude uses the new scale while existing pose-translation units and proportion bounds remain unchanged. Uniform/mirrored geometry behavior and missing-reference refusals are covered. No accepted source landmark or master was changed. The bound decision above is the remaining N1 edit.
- **N2:** deterministic stance/swing is driven by explicit elapsed time. Shared stride is 4% of the shortest declared two-bone reach, body travel equals one stride per cycle, peak lift is 15% of each lower-leg length, with an inward swing arc of 10% of lower-leg length. The first 10%-reach stride exceeded Vent Crab's real stance reach; the retained first/second targeted logs show the failures. Four-percent stride passes all five actual sources and mirrors with existing limits unchanged. Brachyuran faint/hit peak vertical loading is 8%/3% of declared scale length. Readability tests enforce those world-space displacement floors, exclude frozen-motion and old microscopic-lift controls, and check cycle continuity and 30/60/120-Hz determinism. These floors await native and human review; no claim of visible success yet.
- **N3:** surface splitting takes explicitly declared contact endpoints. It locks the nonzero barycentric supports of the nearest endpoint-part painted vertex to `[[endpoint,1]]` after diffusion and pins them in the existing solver. No coordinates/triangles/source pixels change. A conflicting fixed owner refuses. The receipt records part, vertex, source distance and field support IDs for every foot. The unpinned fixture retains mixed weights; the pinned fixture is exact and immutable. `crab-fits-03` contains all five rebuilt candidates: 26 parts/44 joints each, zero source/rest/atlas changed channels. The 0.25-pixel native gate is unchanged. A pinned point may still expose an offset/rotation issue in actual moving paint; that must remain a failure rather than a threshold waiver.
- **N4:** native action rows run before presentation/framing. Per-action failures retain id and ms; presentation failures now include action and ms, and framing exceptions include both. Framing is skipped after earlier action/presentation failures, preserving the detailed rows. Timing labels now honestly describe pre-presentation clip sampling. Native Persimmon proof is pending the signed-source stage.
- **N6:** `civet-sentinel-input-01` is a path adapter copying the existing candidate-10 Civet binding, original record, keyed source and matching atlas byte-for-byte; `input-manifest.json` hashes and names all originals. It is not a new fit or a new intake pass. No sentinel browser has run yet.

N5 phone performance remains deferred R8. N7–N10 are outside the authorized correction and are not declared closed: overwritten raw leg keys, edge fringes, plant profile kit constants and other-family contact exclusions retain Claude's findings. No new painting, open-gape adoption, per-species clip/gain/solver branch, reserved module, main.ts, seed/save/version, gameplay math or unrelated repair.

## Static evidence

- `regression-final.log`: **100 tests / 16 files pass**, including the motion directory, current five crab contacts/mirrors, readability floors, existing family/contact/frame/performance/aim/attack regressions.
- `tools-final.log`: **210 tool tests pass**, including the pin ownership/conflict/mutation outcomes.
- `typecheck-final.log`: all three TypeScript projects pass.
- `root-validate.log`: zero boot/render errors, 1,010 species rendered, all 50 baseline determinism probes match.
- `crab-intake-01.log`: all five rebuilt pin candidates preserve exact original/rest/atlas pixels.
- `targeted-01.log` and `targeted-02.log`: retained reach failures before the bounded correction to stride/arc. `targeted-03.log`, `readability-01.log`, and `contact-pins-01.log`: focused successes. No native retry or changed timing threshold.

The September 17 uninterrupted-session toolchain receipt is reused under the nick-game-toolchain skill; no maintenance/download or new dependency was needed. Blender remains unused. The old shared 169-action table itself is unchanged in this correction, so its recorded amendment hash is not reissued. Scale/contact compilation behavior changes and will be bound by the new source commit and native input hashes.

## Handoff

Codex waits only on the precise N1 floor interpretation before finishing and signing this producer. Claude remains read-only/idle; Nick need not switch apps or synchronize branches. The prior eight-run evidence remains immutable and is still the latest native result: four numeric passes/four failures, none newly visually accepted. No additional species qualification or physical-phone proof is claimed.

Local refs: `openai/mac`, HEAD `57dfe112`, 131 ahead of cached `origin/openai/mac`, 242 ahead of cached `origin/develop`. The pre-existing `.DS_Store` is untouched. No remote state inferred.
