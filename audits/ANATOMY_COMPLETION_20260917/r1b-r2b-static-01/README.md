# R1b/R2b final static checkpoint — September 17, 2026

Status: complete static-checked correction, ready to sign before native capture. This supersedes the earlier `STATUS.md` and pending decision in `direction-receipt.json`; those remain dated preparation history. Nick resolved N1 explicitly and added N11/N12. The only authorized next measurement is the same eight subjects plus one Civet sentinel on unchanged signed source, then a review stop.

## Completed correction

- **N1:** `motion-scale.mjs` is the shared template reference table: brachyuran leg0 Far↔Near root span; woody/herb longest observed root-to-tip chain; other families retain their body axis. BodyCard carries `scaleLength`/`scaleReference`; amplitude reads it. Brachyuran scale admission independently requires **0.08–0.9**, both in offline/runtime family admission and motion compilation. Existing body-axis bounds and observed pivots are untouched. Crab's old axis remains0.041573; its scale is0.093745. Small/large reference mutants refuse, exact boundary values pass, mirror measurement agrees, and current records admit.
- **N2/N11:** shared stride is4% of minimum observed leg reach; root advances one stride per cycle; swing lift is15% of lower-leg length with a10% inward arc. Source joints/reach/limits remain strict. World travel and planted target positions do not depend on blend weight. Controls vary weight0/.2/.7/1 at multiple cycle phases, test repeated-cycle continuity and30/60/120Hz replay, all five actual crab sources and mirrors, and explicit displacement floors. Initial larger-stride reach failures remain recorded; no joint-limit widening or native retry.
- **N12:** faint/hit loading is authored in `specialized-actions.ts` (shared brachyuran template, no species branches) at8%/3% of motion scale. The single timeline constructor converts the action's explicit `rootUnit:motion-scale` to existing pose body units. Ordinary/editor consumers therefore receive the same loading. The contact solver no longer generates hit/faint movement; a test proves zero input stays zero and authored root loading is preserved through contact solving. Other families retain their own clips.
- **N3:** explicit endpoint pins are applied after weight diffusion at surface split time. The nearest painted endpoint vertex's nonzero field supports are locked to the endpoint and solver-pinned, retaining every source coordinate and pixel. Receipts name every endpoint/part/vertex/support. Unpinned mixed-weight and conflicting-owner controls fail their expected contracts. New `crab-fits-03` has all five26-part/44-joint candidates with exact rest/atlas channels; the native0.25px contact gate is unchanged. These pins still need native proof under the stronger motion.
- **N4:** per-action rows precede presentation/framing. Failures retain action/time; presentation adds action identity; framing exceptions name action/time and framing is skipped after earlier shape failures. A failing Persimmon action can no longer be hidden by a preliminary601-sample readiness scan. Per-clip timing labels explicitly describe pre-presentation sampling.
- **N6:** existing candidate-10 Civet assets are copied byte-for-byte into `civet-sentinel-input-01`, with source hashes. This is a path adapter, not a new fit. Native sentinel pending signed source.

**Q4**: the exact previously proposed paragraph is now applied to Motion Kit §8 under Nick's approval:64 declared joints,40 parts,2048 atlas admission, provisional2,000 field vertices for phone screening, and physical4ms/30fps/two-combatant qualification. Other kit wording is untouched. **D1** further defines delivery: retained finished PNGs on phones, painter cut-out fallback, no phone inference; R8 measures that scope.

No source repaint, per-species clip/gain, main.ts, reserved effects/battle2/soundkit/worldlife, seed/save/version, gameplay math, admission-pin waiver or accepted-master edit. No R3/R4/R9 implementation. The existing169-action base table is unchanged; new brachyuran action root-unit/loading is an explicit N12 specialized-library change, separate from the previously recorded plant amendment.

## Final checks

- `regression-02.log`: **102 tests /16 files PASS**.
- `tools-02.log`: **210 tool tests PASS**.
- `typecheck-02.log`: all three TypeScript projects PASS.
- `root-validate-02.log`:1,010 species render, zero boot/render errors, all50 baseline determinism probes match.
- `final-direction-01.log`: N1/N11/N12 focused controls PASS.
- Earlier preparation logs, initial reach failures and `crab-intake-01.log` remain immutable.

No full app suite, certification, phone study or unchanged native retry. Prior stale Compendium certificate remains unresolved. No visual qualification promoted.

## Program decisions and future queue (record only)

Nick's latest message controls over the master program's older PROPOSED heading and any abbreviated step list. D1–D4 are approved: phone delivery uses retained finished PNGs with painter-only fallback and no on-device inference; first guardian later is one Earth-temperate apex at1536 through the existing pipeline; arenas roll out by biome family starting Earth temperate under Nick's eye; Claude owns E1 design now, code after R3 reaches develop through separately authorized integration.

Current order: **finish this correction → static → sign → eight subjects + Civet → STOP**. After review: R3, then R4, then R9, then the remaining plan's integrity/coverage/phone stages. R9 awaits Claude's bounded addendum after recapture: five crab painter masters through the accepted masked0.35 finisher, strict silhouette/alpha conservation so existing stage masks transfer, rebind finished PNG textures, seed-bound retention, desktop only per D1. No R9 rendering has begun.

Record these two R3 additions for the lane interface; do not implement early:
1. `ContactPhase.travel?:'solver'|'stage'`: stage mode contributes exactly zero solver root dx while the arena owns displacement and stance targets remain unchanged; explicit positive/negative tests.
2. `compileAnatomyAttack` returns `contactJoint` (pinch dactyl tip) with `contactMs`; brachyuran contact phase is explicitly strike.

These queued contracts do not authorize a push or merge to develop. No push, PR, hosted attempt, sync, release or LFS/history operation. PR42 parked. Claude can read this lane's eventual evidence without copying or syncing; no app switch required now. The uninterrupted September17 toolchain receipt is reused; Blender unused.
