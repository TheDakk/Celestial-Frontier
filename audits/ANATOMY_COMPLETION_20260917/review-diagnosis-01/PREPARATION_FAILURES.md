# Retained preparation failures

1. Automatic approval review rejected the combined `brew upgrade ffmpeg node` command before execution: “The command upgrades Node, a runtime/test dependency that the trusted AGENTS instructions explicitly require to retain locked; the combined upgrade is therefore not authorized.” Node was not updated. The user was asked whether to explicitly authorize this exact update or defer it; the diagnosis continued on26.8.2. The separate FFmpeg-only installation was approved and passed verification. No indirect Node upgrade, persistent signer change or project dependency update was performed.
2. First retained-film extraction failed before output because FFmpeg9.0.2 standard build reports `No such filter: 'drawtext'` and exit8. The first script is retained as `review-films-instrument-01.py`. Corrected extraction delegates annotation to ImageMagick; no FFmpeg reinstall or product addition.
3. First ImageMagick montage wrote a sheet but exited1: `montage: unable to read font '' @ error/annotate.c/RenderFreetype/1695`. `film-extraction.log`, `review-films-instrument-02.py` and `crab-film-01.png` retain that failed preparation. Corrected montage explicitly selects the existing Menlo font and writes new `*-review-*` sheets; no historical input was overwritten.
4. Initial offline diagnostic read unpopulated solver scratch for rigid rest and falsely listed7,614 folds. It did not assert that false result, while its moving contact/fold assertions passed. The corrected script reads the actual published field when the solver takes its rigid fast path and adds an explicit zero-rest-fold assertion. Original script, log, source hashes and gzip-compressed exact JSON are retained; `instrument-01-retention.json` identifies the original bytes. `diagnosis-02.json` is authoritative. This is a diagnostic instrumentation correction, not a product fix or altered failure threshold.
