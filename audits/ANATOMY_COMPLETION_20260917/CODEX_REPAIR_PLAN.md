# Codex repair plan — consolidated anatomy review, September 17, 2026

**Status: proposed; awaiting Nick's reading and approval. This file is local and unstaged. No repair is authorized by its existence.**

## Scope, source and current state

Reviewed read-only: [Claude's response](/Users/nick/Projects/celestial-frontier-anthropic-mac/audits/ANATOMY_REVIEW_20260917/CLAUDE_REVIEW_RESPONSE.md). It was not copied into this lane. Review SHA-256: ea007af751ee3788a19e4e33de23c4df0d4c59da3e7a7179bfa4a32c1ccbacbc.

Claude reviewed `0426ef4db5dc55205925725b9579ca61d960cd42` plus the September 17 working implementation. That implementation and review packet are now signed in `0b8857511c133fef36299505c9e6927e4e2d47f4`; the signed documentation successor is current HEAD `a184f73cc6c93d442fe22d77085e675d3cdb5f1c`. The successor changes only review/handoff documentation and the signing receipt, not implementation. Both signatures were verified in the preceding turn. No signing was repeated for this plan.

Identity: OpenAI/Codex, macOS, `/Users/nick/Projects/celestial-frontier-openai-mac`, branch `openai/mac`. Cached comparisons: **128 ahead of origin/openai/mac; 239 ahead of origin/develop**. No fetch, pull, merge, sync or remote-state inference. Initial working state contained only the pre-existing untracked `.DS_Store`.

This planning pass read source and existing JSON reports, without running tests, captures, native diagnostics or certification. Visual findings below accept Claude's inspected-frame evidence; I did not create another rendering or claim fresh visual acceptance. Pass 2 governs where it revises Pass 1. The old signing blocker is resolved; the implementation defects are not.

Path shorthand in the register expands exactly as follows, all inside the Codex repository:

- **G** = `port/v2/apps/game/src/`
- **M** = `port/v2/apps/game/src/motion/`
- **A** = `port/v2/packages/art/`
- **T** = `port/v2/tools/`
- **D** = `audits/ANATOMY_COMPLETION_20260917/`

## 1. Disposition of every finding

“Agree” accepts the finding, not necessarily every suggested implementation. “Needs Nick” accepts the defect or gap but requires the stated design/authority decision. No finding is dismissed because its evidence was labelled diagnostic.

### Pass 1

| ID | Disposition | Reason / planned disposition |
|---|---|---|
| A1 | Agree | `M/specialized-actions.ts:12,21–23` freezes leg rotations while dodge still translates root; fixed joints are not fixed world-space contacts. R2/R5. |
| A2 | Needs Nick | `M/family-actions.ts:287–294` changes the shared woody and herb action library; retain anchored roots only through an explicit library decision, with growth intent restored above the root. Q5/R5. |
| A3 | Agree | `T/creature-animation/split-observed-surfaces.mjs:47` reports zero excluded overlaps when they were welded; report observed, welded and left-independent counts separately. R5. |
| A4 | Agree | Earth missing-material refusal is intentional, but raster census does not establish body-card compatibility; add compiler outcomes and run the current full unit battery after repairs. R5/verification. |
| A5 | Agree | Pincer and flora tests read historical audit records without asserting their recipe identity; make current, hash-verified fixtures independent of audit pruning. R5. |
| A6 | Agree | `A/src/speciesoverrides.ts:1683–1694` checks geometry, dimensions and primary alpha but discards the capture ink needed for ordinary-versus-capture RGBA equality. R5. |
| A7 | Agree | `A/src/painter-prefix-replay.ts:11` captures only initial transform; explicitly restrict/refuse unsupported initial state rather than implying arbitrary-context replay. R5. |
| B1 | Agree | The 53 unbound/46 unobserved gaps stand; Fiddler needs its own source capture, and its six visible curved legs prevent blindly reusing the eight-leg record. R6 and bounded continuation ledger. |
| B2 | Needs Nick | Near-closed source pincers cannot yield a readable opening by solver changes alone; choose subtle motion or authorize a separate painted open-gape candidate. Q1. |
| B3 | Agree | `DrawnFeature.kind` currently conflates surface marks and structure; add explicit mark semantics and consumer controls. R5. |
| B4 | Agree | Hidden limbs/views and leaf/fruit articulation remain missing source information; never synthesize them to pass a fit. Deferred art/view gate. |
| C1 | Agree | The canopy chunk shear/smear is a real visual failure; address record geometry and template amplitude together, without claiming scaling alone proves a cure. R1. |
| C2 | Agree | Same-wave scuttle has no planted stance/swing model; amplitude attenuation alone cannot produce grounded locomotion. R2. |
| C3 | Agree | Body-only stationary actions lack readable recoil/collapse and intent; contact-aware template actions need distinct outcomes, not merely different IDs. R2/R5. |
| D1 | Agree | The reported idle canopy tear makes the default presentation fail, independently of attack extremes. R1 must include full sway. |
| E1 | Agree | Data reachability and production integration are separate blockers; R3 opens selection, while real battle/damage/audio/cancellation belongs to the reserved owner after authorized integration. |
| E2 | Agree | Realm metadata alone proves no arena placement; add crab and rooted-flora habitat outcome studies after shared repairs. R3/verification. |
| F1 | Agree | Pass 2 is correct: profile display name and recording attribution differ intentionally, but `creator` is misleading metadata; preserve correct attribution bytes and clarify the field. R7. |
| F2 | Agree | Retain the reported license inventory, source-only status, quarantines and unreviewed listening status; no new listening or integration approval follows. |
| G1 | Agree | Per-action tails are material failures: Persimmon disturb 127.3 ms cannot be represented by aggregate 9.7 ms; report every clip and real-time aggregate separately. R1/R8. |
| G2 | Agree | The crab desktop and accelerated per-clip results are different measurements and neither establishes two-combatant phone headroom. R8. |
| H1 | Agree | There is no physical iPhone evidence for these fits; R8 must use the actual target device, not viewport emulation. |
| I1 | Agree | Root drift lacks a deliberate moving-root failure; add both a real root-key failure and valid upper-body motion that leaves rendered root paint fixed. R5. |
| I2 | Agree | Fix misleading titles; the requested provenance check is satisfied in inspected reports: each includes the actual `native-entry.mjs` hash under `sources` (details below). R5 retains that binding. |
| I3 | Agree | Run-together wording and duplicated current-state paragraphs obscure scope; consolidate and repair current references without rewriting historical logs. R7. |
| I4 | Agree | Current parity/mask and coincident-landmark evidence stands; it does not remove A5's missing fixture identity check or A6's missing cross-render guard. |
| I5 | Agree | The stale Compendium producer certificate remains an admission blocker; only a fresh measured exact-source certificate can resolve it, not a pin edit. Separate later admission gate. |

### Pass 2

| ID | Disposition | Reason / planned disposition |
|---|---|---|
| S1 | Needs Nick | Atomic rig refusal exists but the performance owner has no recoverable-frame policy; recommend bounded hold-last-valid presentation with visible diagnostics and zero refused frames for acceptance. Q3/R4. |
| S2 | Needs Nick | Shared record-relative amplitude is missing; recommend both an amplitude profile and finer observed branch ownership, while treating gait and missing action intent separately. Q2/R1. |
| S3 | Agree | The quadruped-bound solver is not connected to the current family performance/capture path; generalize explicit stance/swing contacts, not all-feet-fixed-through-every-action. R2. |
| S4 | Agree | Pinch is unreachable through generic selectors; repair vocabulary, alias, scoped species declarations, capability/contact data and contact phase together. R3. |
| S5 | Needs Nick | Phone qualification and an intake cost limit are missing; propose a 2,000-field-vertex candidate cap subject to measured shape/performance and existing phone atlas/bone rules. Q4/R8. |
| A8 | Agree | `M/overlay.ts:124–126` omits projection scales **and signs**, and line 117 omits sway's seeded-period branch; use one timeline constructor. R1. |
| A9 | Agree | The legacy target applies per joint; an existing frame collector already provides batching, so migrate callers and remove/deprecate the unsafe export without inventing a second scheduler. R4. |
| A10 | Agree | Backend/fallback counters already exist at `G/creature-rig.ts:165`; surface them in the phone/scene report and qualify JS fallback separately. R8. |
| A11 | Agree | The tiny trunk polygon is only valid because trunk receives remainder pixels; document that exact intake rule and test it instead of hiding the placeholder. R5. |
| A12 | Agree | Preserve the reviewed template/contract parity and add new profile/contact metadata through one shared authority with parity controls. R1/R2. |
| A13 | Agree | Preserve the reviewed seeded/injected-time behavior; new contacts, profile compilation and refusal handling must not feed clocks/counters into world or combat RNG. All repairs. |

### Corrections to the proposed remedies, with exact evidence

These are disagreements with repair sufficiency, not reductions in defect severity:

1. **S2 is not established as the single cause of C1/C2/C3.** Fixed rotations are visible at [specialized-actions.ts:12](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/apps/game/src/motion/specialized-actions.ts:12), while [prepare-flora.mjs:15](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/tools/family-review/prepare-flora.mjs:15) constructs coarse observed groups and [creature-rig-contact.ts:19](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/apps/game/src/creature-rig-contact.ts:19) explicitly refuses nonquadrupeds. Scaling cannot supply absent stance phases, fix erroneous surface ownership, or create a collapse gesture. R1 is necessary, not promised sufficient; R2 and full-pose inspection remain mandatory. “All legs inside the carapace silhouette” is also unsuitable as a gait gate: walking legs legitimately extend outside the shell. Require correct ground contact and no inappropriate crossing through the shell.
2. **S3 must pin stance feet, not every foot throughout root travel.** A walking gait requires swing/lift/replant; kicks, flight and swimming also have intentional noncontact intervals. Preserve the reported no-slide requirement using explicit contact phases and reachable source chains. Do not replace gait with eight permanently pinned feet.
3. **S4 needs more than the suggested three edits.** Mapping `pinch` to the existing `claw` Weapon still leaves `resolveActionId` unable to find `pinch` without a brachyuran alias: [timeline.ts:41](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/apps/game/src/motion/timeline.ts:41), [family-actions.ts:302](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/apps/game/src/motion/family-actions.ts:302). Contact currently defaults to smear for every non-`claw` verb: [anatomy-attacks.ts:80](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/apps/game/src/anatomy-attacks.ts:80); pinch closure is keyed at strike in [specialized-actions.ts:29](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/apps/game/src/motion/specialized-actions.ts:29). Add explicit contact-phase data, preserving other verbs' current behavior.
4. **B1's Fiddler cannot immediately become another unchanged 44-joint crab.** [faunaoverrides.ts:855](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/packages/art/src/faunaoverrides.ts:855) paints three walking legs per side as quadratic curves, and lines 866–878 paint asymmetric separate pincers without the other owners' arm-chain declaration. Capture those actual features; do not invent a fourth pair, elbows or hidden paint. Six source-mask census rows can be an intermediate acceptance target; six complete shared-template bindings require further compatible source anatomy.
5. **I2 provenance is already recorded, despite the hard-coded adapter pin.** [native-runner.mjs:5](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/tools/animation-completion/native-runner.mjs:5) records bundle inputs and line 15 checks them again. `persimmon-native-03/report.json` and `devils-club-native-01/report.json` list entry hash `ef9276da87fae50c72ce6cfdff108b3c157b724bdc36449071580de9467f1437`; `cranberry-native-03/report.json` and `crab-native-02/report.json` list `98186d4d035a49a5984b621a6dc73c723190d4dca9f3e1db8fb26d40cf1988f7`. Do not relabel or regenerate them; new reports must retain this coverage and fix titles.
6. **A9 already has the replacement primitive.** [creature-rig-frame.ts:4](/Users/nick/Projects/celestial-frontier-openai-mac/port/v2/apps/game/src/creature-rig-frame.ts:4) collects and publishes once. Use it or the performance owner's collector; do not implement another per-frame queue.

## 2. Bounded repair register

### Order, shared exclusions and stop rules

Keep the requested order: **R1/S2 → R2/S3 → R3/S4 → R4/S1 → R5/A1,A2,A5,A6,I1 → R6/B1 → R7/I3 → R8/S5.** A8 is folded into R1 because two timeline constructors would invalidate the profile; A9 into R4; A3/A4/A7/B3/A11/I2 into the common integrity work; F1 into the final low-risk metadata/doc batch. A1's contact behavior is first handled by R2 and then regression-closed in R5. A2's chosen root policy is a dependency of R1, with explicit library acceptance closed in R5. These dependencies avoid implementing and then undoing a design choice.

**Exclusion G, binding for every row:** no kit wording; accepted master bytes; per-species clips, gains or solver branches; reserved `effects/`, `battle2/`, `soundkit/`, `worldlife/`; `main.ts`; version/save/seed or gameplay math; another worktree; historical evidence rewriting; admission pins/threshold waivers; GitHub operations. Source-authored geometry/mask declarations may vary by observed master; motion policies may not vary by species name. Only named files plus their matching tests/types/current reference entries are proposed. Any newly discovered need outside them stops that portion for a scope decision.

Line/file estimates are gross edited/added lines including tests, excluding generated bindings/PNGs/JSON evidence. They are sizing ranges, not time estimates or automatic authority to expand. Native proof follows static checks on a signed, unchanged producer; any unexpected failure stops that gate, is retained in a new folder, and is diagnosed before a changed-input attempt. No unchanged retry to obtain a better timing.

### R1 — S2 shared amplitude and plant ownership; A8 single interpreter

- **Files/functions:** `M/body-card.ts` (`ResolvedAnatomyRecord`, `BodyCard`, `compileBodyCard`); new `M/amplitude-profile.ts` (pure `compileAmplitudeProfile`); `M/timeline.ts` (`buildTimeline`, shared explicit-action constructor); `M/overlay.ts` (`buildActionTimeline` delegates); `M/family-actions.ts` (`plant`, `wsway`, `hsway`, only Nick-approved shared library amendment); `T/family-review/prepare-flora.mjs` and `flora/{persimmon,cranberry,devils-club}.json`; matching new `M/amplitude-profile.test.ts`/timeline parity tests and existing flora tests. Update `CREATURE_ANIMATION.md` after approval, not now.
- **Contract:** compile a versioned amplitude profile from actual parent/child lengths normalized by body length, hierarchy depth and explicitly observed role/material. Geometry ratios must be invariant under uniform source scaling and mirroring. If area is used, declare and hash measured part area from the binding; the current BodyCard has no such area—do not guess it from names. Prefer length/depth first. Compose amplitude, source-pincer scale and handedness exactly once, before existing post-easing limits. Contact-critical pincer closure stays tied to actual gape; reducing it arbitrarily would reopen S4's physical contact error. Profile coefficients are shared by anatomical role, with finite/clamped supported bounds, never fitted to a species screenshot. Preserve timing, action IDs and nonaffected family output; any intended library hash change is explicit.
- **Geometry scope:** recommend source-observed twig/branch groups for the three flora, preserving every accepted pixel and true proximal join. Foliage receives material-driven secondary motion, not a second full independent canopy swing. Do not subdivide purely to hide a tear. Keep current 40-part/64-joint implementation limits; refuse if enough real geometry cannot fit. A finer grouping is a source observation, not new paint or species-specific animation.
- **Controls, both directions:** the old fixed-angle profile on a long canopy sector must reproduce excessive strain; the repaired shared profile on the same geometry must pass independent shape/contact checks. A zero-motion “repair” must fail a minimum-action-intent outcome. Short/long, shallow/deep and mirrored fixtures must preserve deterministic ratios; uniform scaling must leave normalized output identical. Every unedited overlay must match the ordinary constructor's full timeline/hash, including sway, source-pincers and explicit absence; remove a scale/sign in a mutant and parity must fail.
- **Acceptance:** complete four-action flora geometry and rooted outcomes, no fold/seam regression, exact rest, bounded strain without smears or straight tears; every clip's p95 below 2 ms on the qualified desktop. Nick sees all three flora sway/disturb/harvest/grow at 25/50/75%, 2× crops and ten-second native films beside old failures. Crab pinch/scuttle stills ensure amplitude does not erase contact/intent. Passing geometry alone does not close C1/D1. If the first bounded combined repair still smears, report that failure and the missing geometry; do not undertake an unbounded solver rewrite.
- **Must not touch:** G; no new master/open-gape paint, no arbitrary per-master amplitude. **Size:** approximately 500–850 lines, 11–14 source/test/config files plus one current reference; generated candidates separate.

### R2 — S3 generic contact phases and stance; A1/C2/C3 behavior

- **Files/functions:** `G/creature-rig-contact.ts` (`poseMatrices`, generalize `createQuadrupedContactSolver` behind compatibility wrapper); `G/creature-rig-performance.ts` (`update` resolve context); `T/creature-animation/family-contracts.mjs` and `.d.mts`, `M/templates.ts`, `M/family-templates.ts`, `T/creature-animation/specialized-templates.mjs` and types (shared contact-chain/phase metadata); `M/specialized-actions.ts` (approach and stationary action intent); existing contact/performance/family parity tests and new gait outcome tests; `T/animation-completion/native-entry.mjs` (use the same owner/resolve path, not direct rig-only sampling).
- **Contract:** explicit source chain descriptors name hip/root, bend joint, endpoint and support role; optional terminal paw remains distinct from a Foot endpoint. Use the actual graph/body axis; no suffix-only or nearest-joint inference. Reuse two-bone math, preserve source bend sign and existing quadruped 8% bound, and refuse impossible reach/unsupported chains rather than clamping feet onto a target. A deterministic phase schedule specifies stance anchor, swing, lift and replant; derive anchors from action/world placement plus explicit elapsed time, not previous render cadence. Brachyuran locomotion uses alternating support groups; extend descriptors to all supported legged templates, with explicit exclusions for wings/fins/hidden or incompatible limbs. Zero-leg/anchored/swimming/flight actions do not acquire fictitious ground feet.
- **Controls:** old scuttle/dodge must move a declared stance foot; repaired world-space stance endpoints and visible contact paint stay fixed within the existing measurement tolerance while the body moves. A test mutant that freezes every leg throughout locomotion must fail swing clearance/replant progress. Legitimate flight, kick and swim must remain free of ground pinning. Test both bend signs, joint-limit/reach failures, transitions, repeated cycles and 30/60/120 Hz schedules at common times. Include at least quadruped, bird, insect, arachnid/myriapod and source crabs; a listed but untested template remains unsupported.
- **Acceptance:** native scuttle/dodge and stationary recoil/collapse outcomes through `createCreatureRigPerformance.update`, rendered stance-paint measurements plus bone measurements, exact rest and stable replay. Nick sees Crab/Mud Crab scuttle, dodge, hit and faint full cycles and the earlier quadruped/frog/Skink regressions where new contact metadata applies. Habitat study must show substrate-relative contact, not all feet flattened to one screen-space Y in a perspective painting.
- **Must not touch:** G; no production scene wiring, no blanket attenuation or contact-limit increase. **Size:** 650–1,000 lines, 12–16 files. This is a shared contact repair, not a promise that every family can use two-bone IK.

### R3 — S4 reachable attacks and explicit contact data; E2 study

- **Files/functions:** `M/body-card.ts` vocabulary; `M/family-actions.ts` `MELEE_ALIAS`; `G/earth-fauna-profiles.ts` profile groups; `G/anatomy-attacks.ts` `AnatomyAttack`, `ANATOMY_ATTACKS`, `attackRepertoire`, `compileAnatomyAttack`; corresponding tests; `T/habitat-battle/native-entry.mjs` and runner only for a source-bound crab/plant study using the existing `compileHabitatBattle` contract.
- **Contract:** reuse `Weapon='claw'`, map intended `pinch` to it and add `brachyuran:{claw:'pinch'}`. Split the broad crustacean species group to admit only the five actually bound owners; Crayfish/Lobster/Hermit/Fiddler remain unsupported until their own records qualify. Preserve Coconut Crab's ground-only adult medium. The capability row requires actual palm, fixed finger, moving finger and contact landmark; presence of a template label alone is insufficient. Add an explicit contact phase for pinch at the end of strike, preserving the current phase of all other attack rows. Do not broadly remap other specialized creatures.
- **Controls:** from each hash-verified source record, `compileBodyCard → resolveActionId('melee') → attackRepertoire → compileAnatomyAttack` must select pinch without a literal action bypass. Omitted aliases, absent dactyl/fixed tip, wrong template, unbound Fiddler and Coconut-in-water must fail; admitted ground/water cases must succeed. Check contactMs against the actual strike key and moving-tip closure, with a smear-phase mutant failing. Seed/genome changes cannot grant Earth anatomy; procedural use still requires its existing hash-bound weapon declaration.
- **Acceptance:** all five selectors and source contact outcomes; Nick sees a selected pinch with a strike/contact marker, ground and aquatic placement where admitted, and rooted plants attached to their substrate. This closes the data blocker and study-level E2 proof only. **E1 remains open:** damage/reward, effect/audio events and scene cancellation require a future reserved-owner integration test that drives a real battle; a resolver test is not a duel outcome.
- **Must not touch:** G; no edits to CombatCore or the reserved scene owners, no invented plant melee. **Size:** 180–300 lines in 6–8 code/test files, plus 120–220 lines in two study files.

### R4 — S1 recoverable frame refusal; A9 one publication

- **Files/functions:** `G/creature-rig-performance.ts` (`update`, diagnostics/reset/dispose); `G/creature-rig.ts` (`applyPose`, legacy `createCreatureRigPoseTarget` export); `G/creature-rig-frame.ts` (`sample`/`flush`, reuse or delegate); `T/creature-animation/paint-skin.mjs` and `.d.mts` (typed recoverable posed-fold error, preserving admission errors); matching three runtime tests; `T/animation-completion/native-entry.mjs` (failure injection/report).
- **Contract recommended:** hold the last successfully published pose for a recoverable *presentation-frame* refusal; keep explicit action time and game simulation advancing; increment a bounded counter with first/last reason and action/time. Do not publish any partially updated buffer. Next valid sample recovers without resetting action time. A sustained refusal is a reported fault, not an accepted frozen animation. Strict authoring/evidence mode records any unexpected refusal as FAIL. Bad recipes, invalid call order, unknown joints and disposed objects remain programming/admission errors, not silently caught successes. Distinguish them with error type/code rather than broad catch-all/string matching.
- **Batching:** migrate every discovered local call site of the old per-joint target to existing collect/sample publication. If compatibility requires the old symbol, make its explicit frame boundary contract clear; do not introduce a microtask or clock to guess when GSAP finished.
- **Controls:** inject a known folded frame at N through the real owner: no frame exception escapes the scene harness, displayed buffers equal frame N−1, counter increments once, event clock advances and valid N+1 recovers. Valid sequences must have zero counts and one apply per producer frame. A swallow-all mutant must fail malformed-producer tests; a partial-publication mutant must fail buffer equality; per-joint apply mutant must fail the 44/62-joint publication-count test.
- **Acceptance:** new short forced-refusal film/stills N−1/N/N+1 and action-time/counter readout for Nick; normal full-action captures must show **zero** refusals. This defines the owner boundary; it does not claim battle2 consumes it yet.
- **Must not touch:** G; do not disable shape checks or publish an unvalidated field-only pose. **Size:** 250–400 lines in 8–10 files.

### R5 — A1/A2/A5/A6/I1 closure and integrity findings

- **Files/functions:** `M/source-pincer.test.ts`, `M/flora-observed.test.ts`, new `M/fixtures/anatomy/` records plus hash manifest; `M/family-actions.ts` and library fixture tests for the approved A2 choice; `A/src/speciesoverrides.ts` `resolveOverrideCanvas`; `A/src/painter-prefix-replay.ts` `recordPainterPrefixes`; `A/src/painter-topology.ts` `DrawnFeature`/`emitPainterTopology`; emitting mark sites in `faunaoverrides4.ts`, `faunaoverrides5.ts`, `fungioverrides2.ts`; art tests; `T/creature-animation/split-observed-surfaces.mjs` receipt and test; `T/family-review/prepare-flora.mjs` remainder declaration; `T/animation-completion/native-entry.mjs`/runner; `T/fauna-coverage/parts-entry.mjs`/runner for compiler outcomes. New instrument test files belong beside their owners.
- **Contracts:** fixtures copied from current source records into test ownership only after approval, retaining exact recipe/binding/source identity and declared mirroring transformations; runtime capture compares complete primary/capture RGBA, not only alpha. Admit the capture context only when initial supported state is known/replayable; refuse unsupported gradients/pattern state. Marks gain an explicit kind and validation, remain in source provenance but never increase structural counts. Receipt distinguishes all observed excluded boundaries from welded ones. Document the remainder polygon exception and keep unclaimed-pixel conservation exact. Compiler census classifies successful compilable records and explicit unsupported/incomplete observations separately; do not force all 53 topology emissions into complete cards.
- **A2 closure:** if Nick confirms anchored root keys, record the woody/herb library amendment and expected action/count/hash fixture exactly once after R1 settles. Express approved grow/recoil above the root with trunk/stem motion; root preservation must not erase the action. If Nick chooses revert, restore original shared keys and measure which fits actually keep rendered roots fixed; no unconditional root-zero gate may pretend that variant is equivalent.
- **Controls:** current fixtures pass; one-byte changed recipe, stale hash and wrong fixture must fail. Capture lineWidth/color mutant must fail primary/capture equality while an identical replay passes. Correct nondefault supported context state passes; unsupported inherited state refuses. Pyrosome structural count is one tube with 190 marks; treating marks as bodies must fail. Old receipt mutant must fail while welded and untouched modes give honest counts. A deliberate root translation must fail the **rendered root** gate; a valid trunk-only bend must pass. Injecting only a pose key into an artificially unconditionally pinned mesh may not expose drift—also use a root-supported binding that actually moves. Remainder test reconstructs all source bytes and catches a wrong/absent remainder owner.
- **Acceptance:** current-source record compilation outcomes, exact ordinary/capture rest bytes and native parity; one labelled moving-root negative native run fails for the expected reason and its valid sibling passes. Nick sees root crops plus A1 dodge and A2 grow/recoil comparisons. Titles name the actual plant/action; reports include entry/runner/producer/record/binding hashes and command arguments. Every normal capture requires its complete expected action/subject inventory.
- **Must not touch:** G; fixture relocation cannot rewrite historical audits or change their recipe hashes. **Size:** 450–750 source/test lines in roughly 15–20 files, plus small immutable test fixtures. Share edits with R1/R2 rather than repeat them.

### R6 — B1 Fiddler source stage, not fabricated completeness

- **Files/functions:** `A/src/faunaoverrides.ts` `faunaFiddler`; `A/src/painter-topology.ts`/`topology-part-capture.ts` only if shared staging needs a documented primitive; `A/test/missing-fauna-observation.test.ts` and part-capture tests; `T/fauna-coverage/parts-entry.mjs` species list; `T/creature-animation/coverage-ledger.mjs` evidence classification. Do not change the eight-leg compiler merely to accept this painter.
- **Contract:** ordinary draw commands/seed use remain identical; emit actual six visible quadratic walking curves, carapace, eyes and asymmetric pincer source surfaces, with missing arm-chain/hidden anatomy explicit. Existing flat surfaces must not be relabelled as a fabricated elbow or a fourth leg pair. Staged masks and features are independently countable; binding admission remains a separate field.
- **Controls:** current ordinary render and captured render match channel-for-channel; distinct large/small claws retain source ownership. Missing stage/opaque ink fails; forged eight-leg inventory fails. Removing hidden/unobserved structures must not itself fail observation intake, but an attempt to compile missing mandatory joints must refuse.
- **Acceptance:** a sixth source-mask row with exact parity and anatomy inventory, plus labelled source/part stills for Nick. **Not acceptance:** another 44-joint binding without a valid observed graph. If a new shared variable-leg contract or new art is necessary, report that concrete follow-up; do not expand this bounded stage silently.
- **Must not touch:** G; no anatomy repaint in this row. **Size:** 130–230 lines, 4–7 files. The other 52 currently unbound targets remain on the existing complete roster; after these systemic fixes and phone proof they need owner-by-owner source/mask/fit/action batches. This plan does not falsely close B1 with one species.

### R7 — I3 current references and F1 metadata semantics

- **Files/functions:** `CREATURE_ANIMATION.md` as canonical current implementation/limits paragraph; `SPECIES_AND_GENOME.md`, `UI_TOOLCHAIN.md`, `celestial-frontier-codebase-reference.md`, `D/README.md`, `D/ANATOMY_STATUS.md` use concise links and corrected spacing; future repair README/report records each new outcome. `T/audio-production/wildlife-by.py` acquisition field, `report.py` `recording_credit`, `wildlife_test.py`/new metadata tests, and the current generated acquisition schema/ledger only through its controlled writer.
- **Contract:** distinguish `creatorProfileName` from exact recording `attribution`; read old field for historical manifests without rewriting source evidence. Credits and lock keep the sound's attribution unchanged. References state per-action and aggregate performance separately and link one current source of coverage truth. Historical logs, review packets and failed runs remain intact, with successor pointers where appropriate.
- **Controls:** both named F1 records emit exact existing sound attribution despite different profile strings; substituting profile names or omitting attribution must fail. Legacy metadata reader still works. Documentation link/count checks pass on correct paths; a missing target or false qualified count must fail. No audio rerender or download is necessary for a metadata correction.
- **Acceptance:** focused metadata outcomes and reviewed Markdown diff; Nick can compare the two displayed credits against their preserved license evidence. No new native media is needed for text-only I3/F1. No listening approval implied.
- **Must not touch:** G; preserve audio originals, rendered candidates and license attribution strings. **Size:** 150–250 edited lines in 8–12 files, largely documentation; no bulk reformat of historical evidence.

### R8 — S5 phone intake/performance, A10 reporting; last in sequence

- **Files/functions:** `T/creature-animation/build-paint-skin.mjs` `buildPaintSkin` and types; `T/creature-animation/paint-skin.mjs` intake validation; `T/family-review/prepare-flora.mjs` and `prepare-observed-crabs.mjs` explicit target-tier output; `G/creature-rig.ts` existing runtime diagnostics (expose cost fields only if absent); new `T/animation-completion/phone-entry.mjs` and `phone-runner.mjs` plus intake/report tests. Reuse existing arena/effect assets and public interfaces read-only; do not add production scene code.
- **Contract proposal:** separate phone candidate binding, initially at most 2,000 **field** vertices per rig, with part vertices/triangles, atlas bytes, solve passes and backend reported too. Use deterministic bounded `interiorStep` choices, preserving source-boundary/pin constraints and accepted source pixels; no arbitrary source-image downsampling or unconditional mesh decimation. If a fit cannot meet quality and cost together, refuse the tier; do not relax the shape gate. Keep desktop bindings separate. Vertex count is a screening limit, not a measured timing guarantee.
- **Controls:** over-budget input and a deceptive low-field/high-part-vertex case must be identified; conforming geometry passes. Coarsening that causes a fold, root/contact drift, gap or smear fails despite lower CPU. Force JS fallback to verify reporting and compare both backends without changing rig results. A probe missing the second rig, the effect or any plate must fail its scene-inventory check; slow seeded work must trip the frame/update budget while valid measured work passes.
- **Acceptance:** first one physical iPhone 17 Pro two-crab scene with one actual effect sequence and three arena plates, both attack/contact cycles and transitions, at least 30 fps and each rig update below the approved 4 ms phone limit; report per-clip tails, frame p95/p99, dropped frames, warm-up/measurement duration, device/OS/browser, backend/fallback counts, vertex/triangle/atlas sizes and source hashes. Proposed bounded measurement: 10-second warm-up plus 60 seconds measured, with an encoded full-scene review clip. Any recovery/thermal interruption is recorded, not silently excluded. Nick watches the phone clip. A desktop mobile viewport is not a substitute.
- **Then:** only after the two-crab probe passes, qualify the three repaired phone flora on the same tier, with per-action shape/CPU evidence. A pass on two crabs alone does not close H1 for flora or the remaining 58. If target device or a faithful read-only effect/plate harness is unavailable, stop the phone claim and name the missing dependency.
- **Must not touch:** G; no kit budget change or production fallback policy invented by the intake tool. **Size:** 350–600 lines in 8–11 files. See Q4 for existing atlas/bone budget conflict requiring resolution before this stage.

Approximate total: **2,800–4,600 gross source/test/doc lines**, before generated evidence; roughly **45–60 distinct files** after shared paths are deduplicated. This is a bounded shared-systems repair tranche, not implementation of the remaining 52+ species, an E1 scene integration, new paintings or a certification program. If source-specific work exceeds a row's upper bound materially, return with the concrete reason before expanding it.

## 3. Decisions for Nick

| Question | Recommendation | Alternatives and consequence |
|---|---|---|
| **Q1 / B2: accept subtle Crab/Coconut pinch or authorize painted open gape?** | Authorize a separate source-painted open-gape candidate for those two owners after shared contact/reachability fixes; preserve accepted originals and show 3× source/action crops before adoption. | Accepting subtle motion retains current art but knowingly leaves low strike readability. No solver gain can invent the absent paint. This plan itself authorizes neither art edits nor kit changes; resolve any required kit amendment separately. |
| **Q2 / S2: amplitude profile, twig groups or both?** | **Both**, bounded to one shared length/depth/role profile and actual finer branch ownership on the three current masters; foliage secondary only, no per-master gains. | Profile-only is smaller but cannot establish correct ownership or resolve every straight cut; twig-only leaves excessive shared angles. If true structure cannot fit the present joint/part limits, stop and show the limit rather than inventing a convenient graph. |
| **Q3 / S1: hold last pose or field-only degradation?** | **Hold last valid pose for recoverable frame refusal**, keep the action clock moving, count/report it, and fail qualification on any unexpected refusal. | Field-only output can itself fold or slide; it would need the same full validation and a second fallback if invalid. Do not publish unvalidated geometry. Sustained holding is a visible fault to repair, not an accepted animation mode. |
| **Q4 / S5: phone vertex budget?** | Approve **2,000 field vertices per rig as a provisional candidate cap**, not a new kit rule; retain 4 ms/30 fps plus the complete two-combatant scene and shape gates. Measure before revising the number. | Vertex count alone ignores part vertices, constraint topology and JS fallback. A tighter or higher cap must be justified by actual device evidence, not desktop timing. |
| **Q5 / A2: confirm root-key removal as a shared library change, or revert?** | **Confirm the anchored-root library amendment for woody and herb templates**, and require above-root growth/recoil so anchoring does not erase intent. Retire the old shared recipe under its date/reason and record the new hash/count once. | If Nick prefers revert, restore the original keys first and test root paint plus every affected action; do not assume `fixedJoints:['root']` alone proves correct grow/recoil. No retroactive claim that the prior removal was already approved. |

**Q4 prerequisite to resolve explicitly:** [MOTION_KIT.md:237](/Users/nick/Projects/celestial-frontier-openai-mac/MOTION_KIT.md:237) states 32 bones and a 1024-square phone atlas; [CREATURE_ANIMATION.md:43](/Users/nick/Projects/celestial-frontier-openai-mac/CREATURE_ANIMATION.md:43) describes the implemented 64-joint/2048-pixel admission budget. The five crab records have 44 joints; Coconut's atlas is 1219 pixels wide. Determine whether an already-approved amendment distinguishes bones/joints or supersedes those tier limits. Until established, do not call these phone-budget compliant. Recommendation: preserve kit wording and require an explicit compliant phone-tier mapping/binding; if that demands structural/art changes outside this plan, return to Nick rather than silently redefining “bone” or resizing an accepted master. This is not resolved by the proposed vertex cap.

## 4. Verification and evidence sequence after approval

Nothing below runs in this planning turn. Approval must identify the design choices above and the allowed repair tranche; no response is treated as approval.

1. **Plan commit only after Nick reads it.** Then implement R1–R4 in order, with their focused positive/negative outcome tests. Run the shared timeline matrix after R1 (all current actions/templates/count variants, not just the historic 169), then contact/reachability/refusal tests after their owners change. Do not regenerate fixtures solely to bless changed output: record every intended library delta and fail unexpected ones.
2. **R5–R7 integrity checks.** Current source/binding fixture hashes; mark-versus-structure counts; cross-render replay guards; native-root gate selftest; malformed input/remainder controls; metadata/attribution tests; reference links/counts. Compile actual complete observed records and classify incomplete emitters explicitly. Keep the original complete 58 roster and all unsupported dispositions.
3. **One relevant aggregate static battery on the finalized repair code.** `npm --prefix port/v2 run test` (its tool and unit owners run once), `npm --prefix port/v2 run typecheck`, then `node tools/validate.js`. The known I5 failure is expected to remain an explicit red until its separate measured admission work; do not waive it or call the full result green. Do not continue to an admission chain after red. Targeted local shape/phone studies require their independent prerequisites and a documented distinction from admission; if the aggregate finds any new regression, stop those studies too until repaired. No root smoke/layout unless actual production UI scope later changes—which this plan excludes.
4. **Sign the static-checked source and current repair status, then native studies on that unchanged producer.** Browser-owning commands use the existing macOS approved execution and workspace lock. First rest/parity, compiler inventory, dense full-action shapes/joins, stance endpoints **and rendered paint**, and explicit negative controls. Run the full eight-subject set (five crabs + three flora), not the ambiguous “six captures” in the review. Record every per-clip p95 alongside aggregate p95. Stop a failed gate before its dependent film; never reuse a PASS from an earlier producer. Sentinels for already-fitted quadruped, frog, bird, fish, Skink and Beetle cover any changed common timeline/contact path; only affected paths need a fresh native sentinel, but their static action matrix is mandatory.
5. **Native visual deliverables.** For each admitted subject capture every action at 25/50/75% plus a complete ten-second presentation, inspect extremes and continuous idle as well as attack. Run the source-selector pinch/habitat scene, rooted flora study, and the deliberate-refusal N−1/N/N+1 proof. Nick compares new and retained failed frames; zero tears is necessary, not sufficient if the paint smears or action intent disappears. Unexpected runtime refusals make the normal evidence fail.
6. **R8 phone work last.** After desktop shape/contact stability and Q4 budget resolution, build separate phone candidates and run the physical two-crab/effect/three-plate probe once on the unchanged admitted source. If it fails, retain it and diagnose; do not automatically retry. Only after passing proceed to flora phone evidence and, later, wider source fits.
7. **I5 remains a separate exact-source admission task.** After implementation and owner integration stabilize, prepare the measured Compendium refresh using the repository's required selftests and clean committed-source protocol. Do not edit pins to match the candidate. No Compendium/Slice/Glass/SceneMemory/Recovery chain, hosted attempt or production release is authorized by this plan; it needs its own readiness and authorization decision. E1's reserved-owner real battle/reward/cancellation outcome test is likewise a later integration dependency.

### Immutable history and new sibling allocation

All existing folders and files under `D/`, `FAUNA_FULL_PASS_20260916/`, `PAINTED_FITS_AND_58_20260916/`, `ANIMATION_COMPLETION_20260916/`, `COUNTED_ANATOMY_20260916/`, earlier C2/attack/habitat audits, audio packs and the anthropic review stay immutable **as evidence**. Current reference Markdown may be corrected under R7 only after approval; original failures, raw logs, master/record/binding artifacts, pre-commit manifests and old report hashes never get overwritten or re-labelled.

Proposed next unused numbered siblings (check existence at execution; if occupied, take the next number without overwrite):

| Existing evidence | New successor after approved repair |
|---|---|
| `D/persimmon-native-01..03` | `D/persimmon-native-04` |
| `D/cranberry-native-01..03` | `D/cranberry-native-04` |
| `D/devils-club-native-01` | `D/devils-club-native-02` |
| each of five `D/*crab-native-01..02` | each owner's `*-native-03` |
| `D/persimmon-03`, `cranberry-06`, `devils-club-04` bindings | next unused `persimmon-04`, `cranberry-07`, `devils-club-05` candidates; no overwrite |
| `D/crab-masks-01..05`, `crab-fits-01..02` | `D/crab-masks-06`, `D/crab-fits-03`; Fiddler observation status explicit |
| `D/native-census-01` | `D/native-census-02`, including actual compiler outcome inventory |
| Current root gate / refusal gaps | new `D/root-negative-01`, `D/root-positive-01`, `D/frame-refusal-01` |
| Existing habitat studies | new `D/crab-flora-habitat-01`; no mutation of prior mixed-family films |
| No physical phone proof | new `D/phone-two-crab-01`, then `D/phone-flora-01` if admitted |
| Current coverage/review summaries | new `D/repair-validation-01/` with its own checks, ledger, source/command manifest and outcome summary |

There is no rerun of audio production for F1, and no regeneration of the current Compendium certificate in this tranche. New evidence commits follow the already signed producer and state that producer explicitly; a documentation/evidence successor is not falsely presented as the code that rendered the film.

## 5. Signing, commits and authorization

1. **Now:** write only this document, unstaged. Preserve HEAD/index/implementation and every other file. No tests, signing, commit, network, branch operation or capture. The previous requested signed implementation commit is already done, so there is nothing to finish first.
2. **After Nick reads the plan and authorizes its signed checkpoint:** stage only this file and commit with the configured signer; verify the signature. Do not use that act as permission to repair. If Nick requests revisions, revise only the plan first and retain the unapproved status.
3. **Only after Nick explicitly approves the plan/design decisions:** start the specified repair rows. Any unresolved choice blocks its dependent edit; unrelated approved rows may proceed. Perform the appropriate future coding-session startup and required protocol checks then; do not run tool maintenance during this planning-only turn.
4. Sign completed static-checked source batches in the row order, keeping dependency changes together (R1/A8 and R2/A1), with references and negative-control outcomes. Sign new evidence after each exact-source native batch. No amend/rewrite of the signed implementation, no unsigned fallback, and no claim that the expected I5 red is a clean admission.
5. If signing fails, keep the exact prepared work and report the configured signer failure. Do not swap keys, change persistent signer settings or advance dependent exact-commit captures.

**Paired handoff:** OpenAI/Codex waits for Nick to read and approve this plan; implementation is unchanged. Anthropic/Claude owns the later E1 integration and needs no app switch or branch synchronization for this planning turn. Nick can share this plan for review, but no message or file copy to the other lane was made. No PR is needed for a plan; PR42 remains parked. No GitHub write or hosted attempt, no develop/main merge, no release/deployment, and no LFS/history rewrite is authorized.

**Actual local state at preparation:** HEAD `a184f73cc6c93d442fe22d77085e675d3cdb5f1c`, branch `openai/mac`, 128 ahead of cached `origin/openai/mac`, 239 ahead of cached `origin/develop`. The only new working-tree path is this unstaged plan; the pre-existing untracked `.DS_Store` remains. These numbers describe local refs only.
