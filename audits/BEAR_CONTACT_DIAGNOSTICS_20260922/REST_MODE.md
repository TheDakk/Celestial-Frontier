# REST-support comparison instrument

`fit-01/static.ts` accepts `CF_STATIC_SUPPORTS=rest|observed` and a fresh `CF_STATIC_OUTPUT` path. Default observed mode is unchanged. Rest mode replaces the contact solver with the family solver's default support model; it does not change the record, binding, source or production solver. Both retain the original family action sample grid and presentation sequence.

Rest mode records the actual published painted drift against the same0.25px gate, but continues sampling after that finding to distinguish stage solver admission from surface drift. A red painted gate remains RED in the static report. `solverCompleted` does not claim surface acceptance. Compression/limit/shape/seam throws still end their row; exact rest remains mandatory.

Both comparison tables identify unchanged solver/fit producer1ff30009. Retained observed evidence keeps its original producer and hashes; this new rest measurement also records its own tool producer. The original observed sweep is not repeated.
