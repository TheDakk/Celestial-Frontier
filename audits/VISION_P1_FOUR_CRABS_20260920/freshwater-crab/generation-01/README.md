# Freshwater Crab — generation 01, paint only

Exactly one built-in `image_gen.imagegen` call sent `sent-prompt.txt` verbatim with the approved Discovery Atlas and this species’ anatomy guide. Both references were visually inspected before sending; their original SHA-256 values are retained in `request.json`. The model and seed are not exposed.

Original tool output is retained as `freshwater-crab-master.png`, byte-identical to the original tool path in `result.json`: **1254 × 1254 RGBA**, SHA-256 `49645b5e951eaf4207e95ee2453f218abc56e49ba75451e188452e74b3cbce44`. The exact prompt requested 1024 × 1024 on magenta; the delivered size and alpha are recorded as findings without modifying any channel or making another generation.

`findings.json` records visual concerns and side-margin measurements. None is an inferred hidden/absent declaration or an admission verdict. No masks, landmarks, key/despill, split, binding, native capture or rig was attempted; intake waits for the authorized P1 CPU/root work.
