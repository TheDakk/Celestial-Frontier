# C45 — finished stage texture plus individual palette (2026-09-26)

C45(a) is implemented as an additive composition; original source pins remain exact. The loader accepts its existing `atlasPixels` morph callback alongside `{finishedAtlas:{token,identity}}`. Order is:

1. Complete original pinned record/alpha/binding/atlas preflight and semantic record admission.
2. Verify the private finished-atlas token against the exact pin and intended individual identity.
3. Project the privately admitted finished pixels (already source/conservation verified).
4. Apply the existing deterministic individual morph callback to those finished pixels.
5. Check output length and every alpha byte against the original decoded atlas.
6. Apply the existing opaque seam sampling guard, then upload the texture through the unchanged rig tail.

This preserves the individual's palette on stage in the same finish-before-morph order as the card. It does not create a morphed-source pin, change the original master, substitute a new atlas hash, weaken conservation, or infer that a finished original is quality-approved. The rig owns this texture; a `borrowedAtlas` finish still refuses. Claude owns stage routing and can now supply both existing options together.

## Additional guard fix

The prior ordinary morph path passed its baseline decoded RGBA buffer directly to a callback. An in-place alpha mutation could therefore mutate both sides of its comparison. `decodeMorphedAtlas` now gives the callback a copy and keeps its baseline private. Both in-place and returned-copy alpha changes refuse on ordinary and finished paths.

## Verification

`c45-focused-tests.log`: **15/15 PASS**, comprising the finished admission suite, existing pinned-loader suite and existing morph-individual suite. A real Crab source receives a deterministic, conserved one-pixel interior RGB change through a test inference function. Its admitted projected atlas is proven different from the original; the existing `individualFromGenomeV1` palette changes it again. The actual loaded rig's texture hashes match the remap followed by the unchanged seam guard on two runs. The morph callback sees the finished input, not the original atlas. Wrong individual identity refuses before remap or atlas decoding. Both alpha mutation styles and a truncated buffer refuse, followed by a valid control.

The first new texture assertion omitted the existing seam guard and found 452 differing channel bytes; the final assertion compares the complete guarded texture, rather than excluding those pixels. No production guard or threshold changed. App TypeScript and root TypeScript (`--noUnusedLocals`) exit 0; logs retained. The parent's integrated profile owns final batch acceptance.

## C45(b) — proposal for four missing label maps

Claude may produce **candidate derived-label audit packets**, without modifying signed fit evidence folders. The card builder already derives ownership from bound part frames when labels are absent (`tools/morph/build-card-masters.mjs:123–128`): retain `kind=part` order for label IDs, paint atlas-alpha ownership into each native cut-out box, and apply the existing stable far-before-near overwrite order. This is ownership derived from an admitted binding, not independent anatomy observation or restoration of a missing hand label map.

Before adding any candidate to the supplementary labels registry, retain and review:

- Exact genuine creature pin identity, original full-record/master/alpha/binding/atlas SHA values, dimensions and canonical source paths; original preflight must pass.
- Generator source hash, ordered label→part/joint/layer map, overlap/overwrite inventory, unowned painted-pixel counts, and both encoded labels PNG and decoded RGBA SHA values.
- Native unrotated frame/cut-out equality and in-bounds geometry. Refuse malformed geometry rather than silently clipping during new evidence production.
- Reproduction of the card builder's derived ownership pixels from the same original inputs, genuine control and changed-binding/atlas/dimension controls, then unchanged finisher conservation against that retained ownership.

The new audit packet must be called **derived ownership evidence**, not a manual label source or a new anatomical admission. A reviewed supplementary registry can subsequently point at the audit path and bind it to the same original full-record pin; until then Civet, Eel, Rat and Salamander remain unsupported for finished stage admission. No candidate map, registry extension or publication was made here.

Civet remains a separate limitation: its original master is opaque/keyed, while the card builder replaces alpha with the keyed cut-out before deriving card ownership. Producing a label map alone does not change that master alpha or make it card-eligible. Do not rebind the genuine source master or claim that labels resolve Civet's alpha issue.
